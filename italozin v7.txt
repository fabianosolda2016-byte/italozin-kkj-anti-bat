repeat task.wait() until game:IsLoaded()  
  
local Players,RunService,UIS,TS,Lighting,HS = game:GetService("Players"),game:GetService("RunService"),game:GetService("UserInputService"),game:GetService("TweenService"),game:GetService("Lighting"),game:GetService("HttpService")  
local LP = Players.LocalPlayer  

-- ── Anti Kick Logic (message 27) ─────────────────────────────────────────────
local ANTIKICK_BOXES = {
	left  = { position = Vector3.new(-474.108, 0.025, 113.668), size = Vector3.new(47.757, 17.695, 14.49) },
	right = { position = Vector3.new(-474.108, 0.341,   6.668), size = Vector3.new(47.757, 17.195, 14.49) },
}
local antiKickEnabled    = false
local antiKickSide       = "left"
local antiKickDuration   = 2.15
local antiKickRemaining  = 0
local antiKickRunning    = false
local antiKickBarriers   = {}
local antiKickAttrConn   = nil
local antiKickHBConn     = nil

local function akSpawnBarrier(side)
	for _, p in pairs(antiKickBarriers) do pcall(function() p:Destroy() end) end
	antiKickBarriers = {}
	local box = ANTIKICK_BOXES[side]
	local thickness = 2
	local walls = {
		{ size = Vector3.new(box.size.Z, box.size.Y, thickness), pos = Vector3.new(box.position.X, box.position.Y, box.position.Z - box.size.X/2 - thickness/2) },
		{ size = Vector3.new(box.size.Z, box.size.Y, thickness), pos = Vector3.new(box.position.X, box.position.Y, box.position.Z + box.size.X/2 + thickness/2) },
		{ size = Vector3.new(thickness, box.size.Y, box.size.X), pos = Vector3.new(box.position.X + box.size.Z/2 + thickness/2, box.position.Y, box.position.Z) },
		{ size = Vector3.new(thickness, box.size.Y, box.size.X), pos = Vector3.new(box.position.X - box.size.Z/2 - thickness/2, box.position.Y, box.position.Z) },
	}
	for _, w in pairs(walls) do
		local part = Instance.new("Part")
		part.Size = w.size; part.CFrame = CFrame.new(w.pos)
		part.Anchored = true; part.CanCollide = true; part.Transparency = 1
		part.Name = "AKBarrier"; part.Parent = workspace
		table.insert(antiKickBarriers, part)
	end
end

local function akRemoveBarrier()
	for _, p in pairs(antiKickBarriers) do pcall(function() p:Destroy() end) end
	antiKickBarriers = {}
end

local function startAntiKick()
	if antiKickAttrConn then antiKickAttrConn:Disconnect() end
	if antiKickHBConn   then antiKickHBConn:Disconnect() end
	antiKickAttrConn = LP.AttributeChanged:Connect(function(attr)
		if attr ~= "Stealing" then return end
		local val = LP:GetAttribute("Stealing")
		if val and val ~= false and val ~= 0 then
			if not antiKickRunning then
				antiKickRemaining = antiKickDuration
				antiKickRunning   = true
				akSpawnBarrier(antiKickSide)
			end
		else
			antiKickRunning   = false
			antiKickRemaining = 0
			akRemoveBarrier()
		end
	end)
	antiKickHBConn = RunService.Heartbeat:Connect(function(dt)
		if antiKickRunning then
			antiKickRemaining = math.max(0, antiKickRemaining - dt)
			if antiKickRemaining <= 0 then
				antiKickRunning = false
				akRemoveBarrier()
			end
		end
	end)
end

local function stopAntiKick()
	if antiKickAttrConn then antiKickAttrConn:Disconnect(); antiKickAttrConn = nil end
	if antiKickHBConn   then antiKickHBConn:Disconnect();   antiKickHBConn   = nil end
	antiKickRunning = false; antiKickRemaining = 0
	akRemoveBarrier()
end
-- ── End Anti Kick Logic ───────────────────────────────────────────────────────

;(function()
  
local DAS,DAD = 150,0.2
-- Speed mode system: "normal" | "lagger" | "desync"
local activeSpeedMode = "normal"  -- current mode
local carryActive = false          -- whether carry is on within the current mode

-- Normal mode speeds
local NS  = 59   -- normal speed
local CS  = 29   -- carry speed

-- Lagger mode speeds
local LS  = 34   -- lagger normal speed
local LS2 = 17   -- lagger carry speed

-- Desync mode speeds

-- Legacy aliases kept so existing references still work
local speedMode   = false
local laggerMode  = false
local laggerPhase = 1
  
local antiRagdollEnabled,infJumpEnabled = false,false  
local medusaCounterEnabled = false  
local ragdollTimerEnabled = false
local medusaMode = false  
local unwalkEnabled = false  
local unwalkAnimations = {}  
local lastHealth,medusaDebounce,medusaLastUsed,dropActive = 100,false,0,false  
local autoTPEnabled = false
local autoTPHeight  = 20
local autoTPConn    = nil
local setAutoTPVisual = nil
local _setAutoTPHeightBox = nil  -- set by UI to allow loadConfig to update height box
local stretchRezEnabled = false  
local removeAccessoriesEnabled = false  
local removeAccessoriesConn = nil  
local nightModeEnabled = false  
local defLightBrightness = Lighting.Brightness  
local defClockTime = Lighting.ClockTime  
local defOutdoorAmbient = Lighting.OutdoorAmbient  
local defExposureComp = Lighting.ExposureCompensation  
local uiLocked = false  -- Lock UI: disables dragging of main menu and all panels
local setLockUIVisual = nil  -- set by buildGui so loadConfig can update the toggle visual
local autoLeftEnabled,autoRightEnabled = false,false  
local autoLeftSetVisual,autoRightSetVisual = nil,nil  
local speedLabel = nil  
local _guiSpeedLbl = nil  
local _guiSpeedSubLbl = nil  
local medusaConns = {}  
local autoBatEnabled = false  
local autoBatSpeed = 58  -- chase speed used by bat aimbot  
local autoBatSetVisual = nil  
-- ── Body Lock ────────────────────────────────────────────────────────────────
local bodyLockEnabled   = false
local bodyLockRange     = 40      -- only face if target is within this distance
local _bodyLockConn     = nil
local bodyLockSetVisual = nil

local _anyKeyListening = false  
  
local KB = {  
	DropBrainrot={kb=Enum.KeyCode.X,gp=nil},  
	TPFloor     ={kb=Enum.KeyCode.F,gp=nil},  
	AutoLeft    ={kb=Enum.KeyCode.Z,gp=nil},  
	AutoRight   ={kb=Enum.KeyCode.C,gp=nil},  
	AutoBat     ={kb=Enum.KeyCode.E,gp=nil},  
	GuiHide     ={kb=Enum.KeyCode.LeftControl,gp=nil},  
	SpeedToggle  ={kb=Enum.KeyCode.Q,gp=nil},  -- carry toggle (same key works in all modes)
	ModeToggle   ={kb=Enum.KeyCode.N,gp=nil},  -- toggle Normal <-> Lagger mode
	italozinReset ={kb=Enum.KeyCode.R,gp=nil},  -- insta reset
}  
  
local progressRadLbl, progressFill, progressPct
local modeValLbl

local Steal = {
	AutoStealEnabled=false, StealRadius=20, StealDuration=1.3,
	-- Separate grab radii for Normal mode and Lagger mode. StealRadius above
	-- is the ACTIVE radius (kept in sync with whichever mode is currently on).
	StealRadiusNormal=20, StealRadiusLagger=20,
	Data={}, plotCache={}, plotCacheTime={}, cachedPrompts={}, promptCacheTime=0,
}

-- Keep Steal.StealRadius in sync with the active speed mode's radius.
-- Call this whenever activeSpeedMode changes or a radius value is edited.
local function syncStealRadius()
	if activeSpeedMode == "lagger" then
		Steal.StealRadius = Steal.StealRadiusLagger
	else
		Steal.StealRadius = Steal.StealRadiusNormal
	end
	Steal.cachedPrompts = {}; Steal.promptCacheTime = 0
	if progressRadLbl then progressRadLbl.Text = "Radius: "..Steal.StealRadius end
end

-- ── Auto Steal Logic (message 29) ────────────────────────────────────────────
local Conns = {autoSteal=nil, antiRag=nil, anchor={}, progress=nil}
local MEDUSA_COOLDOWN = 25

-- ── italozin Reset Logic ───────────────────────────────────────────────────────
local resetRemote = nil
local RESET_GUID  = "f888ee6e-c86d-46e1-93d7-0639d6635d42"

local o_hook; o_hook = hookfunction(Instance.new("RemoteEvent").FireServer, newcclosure(function(self, ...)
	if not resetRemote and self.Name:sub(1,3) == "RE/" then
		resetRemote = self
	end
	return o_hook(self, ...)
end))

task.spawn(function()
	task.wait(2)
	if not resetRemote then
		for _, d in pairs(game:GetDescendants()) do
			if d:IsA("RemoteEvent") and d.Name:sub(1,3) == "RE/" then
				resetRemote = d; break
			end
		end
	end
end)

local function instaReset()
	if not resetRemote then return end
	local character = LP.Character
	if not character then
		pcall(function() resetRemote:FireServer(RESET_GUID, LP, "balloon") end)
		return
	end
	local humanoid = character:FindFirstChild("Humanoid")
	if not humanoid then
		pcall(function() resetRemote:FireServer(RESET_GUID, LP, "balloon") end)
		return
	end
	if humanoid.Health <= 0 then
		pcall(function() resetRemote:FireServer(RESET_GUID, LP, "balloon") end)
		return
	end
	local resetDetected = false
	local conns = {}
	table.insert(conns, humanoid.Died:Connect(function() resetDetected = true end))
	table.insert(conns, character.AncestryChanged:Connect(function(_, parent)
		if not parent then resetDetected = true end
	end))
	table.insert(conns, humanoid:GetPropertyChangedSignal("Health"):Connect(function()
		if humanoid.Health <= 0 then resetDetected = true end
	end))
	task.spawn(function()
		local attempts = 0
		while not resetDetected and attempts < 50 do
			attempts = attempts + 1
			pcall(function() resetRemote:FireServer(RESET_GUID, LP, "balloon") end)
			task.wait()
		end
		for _, c in pairs(conns) do c:Disconnect() end
	end)
end
-- ── End italozin Reset Logic ───────────────────────────────────────────────────

local CONFIG = {
	HOLD_MIN    = 1.3,
	HOLD_MAX    = 2.6,
	ENTRY_DELAY = 0.3,
	COOLDOWN    = 0.05,
	STEAL_RANGE = 8,
	PRIME_RANGE = 70,
}

local S_RS = game:GetService("ReplicatedStorage")
local Packages   = S_RS:WaitForChild("Packages")
local Datas      = S_RS:WaitForChild("Datas")
local AnimalsData = require(Datas:WaitForChild("Animals"))
local plots = workspace:WaitForChild("Plots")

local syncRemotes = (function()
	local folder = Packages:WaitForChild("Synchronizer")
	return {
		channelFolder = folder:WaitForChild("Channel"),
		routeRemote   = folder:WaitForChild("CommunicationRoute"),
		requestData   = folder:FindFirstChild("RequestData"),
	}
end)()

local plotAnimalSync = { caches = {}, connections = {} }

local function splitSyncPath(path)
	if typeof(path) == "table" then return path end
	local out = {}
	for part in string.gmatch(tostring(path), "[^%.]+") do
		table.insert(out, tonumber(part) or part)
	end
	return out
end

local function resolveSyncPath(path, root)
	local current = root
	local parent = nil
	local key = nil
	for _, part in ipairs(splitSyncPath(path)) do
		parent = current
		key = part
		current = current and current[part] or nil
	end
	return current, parent, key
end

local function applyPlotSyncDiff(channelName, packet)
	local cache = plotAnimalSync.caches[channelName]
	if typeof(cache) ~= "table" then return end
	local path, action, a, b = packet[1], packet[2], packet[3], packet[4]
	local current, parent, key = resolveSyncPath(path, cache)
	if action == "Changed" then
		if parent ~= nil then parent[key] = a end
	elseif action == "ArrayInsert" then
		if current ~= nil then table.insert(current, b, a) end
	elseif action == "ArrayRemoved" then
		if current ~= nil then table.remove(current, b) end
	elseif action == "DictionaryInsert" then
		if current ~= nil then current[b] = a end
	elseif action == "DictionaryRemoved" then
		if current ~= nil then current[b] = nil end
	end
end

local function attachPlotChannel(remote)
	if plotAnimalSync.connections[remote] then return end
	local channelName = tostring(remote.Name)
	if not plots:FindFirstChild(channelName) then return end
	if syncRemotes.requestData and plotAnimalSync.caches[channelName] == nil then
		local ok, data = pcall(function()
			return syncRemotes.requestData:InvokeServer(channelName)
		end)
		if ok and typeof(data) == "table" then
			plotAnimalSync.caches[channelName] = data
		else
			plotAnimalSync.caches[channelName] = {}
		end
	elseif plotAnimalSync.caches[channelName] == nil then
		plotAnimalSync.caches[channelName] = {}
	end
	plotAnimalSync.connections[remote] = remote.OnClientEvent:Connect(function(queue)
		for _, packet in ipairs(queue) do
			applyPlotSyncDiff(channelName, packet)
		end
	end)
end

local function detachPlotChannel(channelName)
	for remote, conn in pairs(plotAnimalSync.connections) do
		if tostring(remote.Name) == tostring(channelName) then
			conn:Disconnect()
			plotAnimalSync.connections[remote] = nil
			plotAnimalSync.caches[tostring(channelName)] = nil
			break
		end
	end
end

task.spawn(function()
	for _, child in ipairs(syncRemotes.channelFolder:GetChildren()) do
		if child:IsA("RemoteEvent") then attachPlotChannel(child) end
	end
	syncRemotes.channelFolder.ChildAdded:Connect(function(child)
		if child:IsA("RemoteEvent") then attachPlotChannel(child) end
	end)
	syncRemotes.routeRemote.OnClientEvent:Connect(function(actions)
		for _, action in ipairs(actions) do
			local kind, channelName = action[1], tostring(action[2])
			if not plots:FindFirstChild(channelName) then continue end
			if kind == "ListenerAdded" then
				local remote = syncRemotes.channelFolder:FindFirstChild(channelName)
				if remote and remote:IsA("RemoteEvent") then attachPlotChannel(remote) end
			elseif kind == "ListenerRemoved" then
				detachPlotChannel(channelName)
			end
		end
	end)
end)

local function getPlotChannelData(plotName)
	return plotAnimalSync.caches[plotName]
end

local allAnimalsCache = {}
local PromptMemoryCache = {}
local InternalStealCache = {}
local stealConnection = nil

local StealState = { active = false, statusText = "READY", startTime = 0 }

local function getPlotOwner(plot)
	local sign = plot:FindFirstChild("PlotSign")
	local frame = sign and sign:FindFirstChild("SurfaceGui") and sign.SurfaceGui:FindFirstChild("Frame")
	local label = frame and frame:FindFirstChild("TextLabel")
	if not label or label.Text == "Empty Base" then return nil end
	return label.Text:gsub("'s [Bb]ase$", ""):gsub("%s+$", "")
end

local function isMyBaseAnimal(animalData)
	if not animalData or not animalData.plot then return false end
	local plot = plots:FindFirstChild(animalData.plot)
	if not plot then return false end
	return getPlotOwner(plot) == LP.DisplayName
end

local function findProximityPromptForAnimal(animalData)
	if not animalData then return nil end
	local cached = PromptMemoryCache[animalData.uid]
	if cached and cached.Parent then return cached end
	local plot = workspace.Plots:FindFirstChild(animalData.plot); if not plot then return nil end
	local podiums = plot:FindFirstChild("AnimalPodiums"); if not podiums then return nil end
	local podium = podiums:FindFirstChild(animalData.slot); if not podium then return nil end
	local base = podium:FindFirstChild("Base"); if not base then return nil end
	local spawn = base:FindFirstChild("Spawn"); if not spawn then return nil end
	local attach = spawn:FindFirstChild("PromptAttachment"); if not attach then return nil end
	for _, p in ipairs(attach:GetChildren()) do
		if p:IsA("ProximityPrompt") then
			PromptMemoryCache[animalData.uid] = p
			return p
		end
	end
	return nil
end

local function getAnimalPosition(animalData)
	local plot = workspace.Plots:FindFirstChild(animalData.plot); if not plot then return nil end
	local podiums = plot:FindFirstChild("AnimalPodiums"); if not podiums then return nil end
	local podium = podiums:FindFirstChild(animalData.slot); if not podium then return nil end
	return podium:GetPivot().Position
end

local function distToAnimal(animalData)
	local character = LP.Character; if not character then return math.huge end
	local hrp = character:FindFirstChild("HumanoidRootPart") or character:FindFirstChild("UpperTorso")
	if not hrp then return math.huge end
	local pos = getAnimalPosition(animalData)
	if not pos then return math.huge end
	return (hrp.Position - pos).Magnitude
end

local function pickClosest()
	local character = LP.Character; if not character then return nil end
	local hrp = character:FindFirstChild("HumanoidRootPart") or character:FindFirstChild("UpperTorso")
	if not hrp then return nil end
	local best, bestDist = nil, math.huge
	for _, animalData in ipairs(allAnimalsCache) do
		if isMyBaseAnimal(animalData) then continue end
		local pos = getAnimalPosition(animalData)
		if not pos then continue end
		local dist = (hrp.Position - pos).Magnitude
		if dist > CONFIG.PRIME_RANGE then continue end
		if dist < bestDist then bestDist = dist; best = animalData end
	end
	return best
end

local function buildStealCallbacks(prompt)
	if InternalStealCache[prompt] then return end
	local data = { holdCallbacks = {}, triggerCallbacks = {}, ready = true }
	local ok1, conns1 = pcall(getconnections, prompt.PromptButtonHoldBegan)
	if ok1 and type(conns1) == "table" then
		for _, conn in ipairs(conns1) do
			if type(conn.Function) == "function" then table.insert(data.holdCallbacks, conn.Function) end
		end
	end
	local ok2, conns2 = pcall(getconnections, prompt.Triggered)
	if ok2 and type(conns2) == "table" then
		for _, conn in ipairs(conns2) do
			if type(conn.Function) == "function" then table.insert(data.triggerCallbacks, conn.Function) end
		end
	end
	if (#data.holdCallbacks > 0) or (#data.triggerCallbacks > 0) then
		InternalStealCache[prompt] = data
	end
end

local function executeStealAsync(prompt, animalData)
	local data = InternalStealCache[prompt]
	if not data or not data.ready then return false end
	data.ready = false
	StealState.active = true
	StealState.startTime = tick()
	task.spawn(function()
		for _, fn in ipairs(data.holdCallbacks) do task.spawn(fn) end
		task.wait(CONFIG.HOLD_MIN)
		local alreadyInRange = distToAnimal(animalData) <= CONFIG.STEAL_RANGE
		local fired = false
		while true do
			local elapsed = tick() - StealState.startTime
			if elapsed > CONFIG.HOLD_MAX then break end
			if not prompt.Parent then break end
			if distToAnimal(animalData) <= CONFIG.STEAL_RANGE then
				if not alreadyInRange then task.wait(CONFIG.ENTRY_DELAY) end
				for _, fn in ipairs(data.triggerCallbacks) do task.spawn(fn) end
				fired = true
				break
			end
			task.wait()
		end
		StealState.active = false
		task.wait(CONFIG.COOLDOWN)
		data.ready = true
	end)
	return true
end

local function attemptSteal(prompt, animalData)
	if not prompt or not prompt.Parent then return false end
	buildStealCallbacks(prompt)
	if not InternalStealCache[prompt] then return false end
	return executeStealAsync(prompt, animalData)
end

local function scanAllPlots()
	local newCache = {}
	for _, plot in ipairs(plots:GetChildren()) do
		local cache = getPlotChannelData(plot.Name)
		if not cache then continue end
		local animalList = cache.AnimalList
		if typeof(animalList) ~= "table" then continue end
		for slot, animalData in pairs(animalList) do
			if type(animalData) == "table" then
				local animalName = animalData.Index
				local animalInfo = AnimalsData[animalName]
				if not animalInfo then continue end
				table.insert(newCache, {
					name = animalInfo.DisplayName or animalName,
					plot = plot.Name,
					slot = tostring(slot),
					uid  = plot.Name .. "_" .. tostring(slot),
				})
			end
		end
	end
	allAnimalsCache = newCache
	return #allAnimalsCache
end

local function startAutoSteal()
	if stealConnection then return end
	stealConnection = RunService.Heartbeat:Connect(function()
		if not Steal.AutoStealEnabled then return end
		if StealState.active then return end
		local target = pickClosest()
		if not target then return end
		local prompt = PromptMemoryCache[target.uid]
		if not prompt or not prompt.Parent then
			prompt = findProximityPromptForAnimal(target)
		end
		if prompt then attemptSteal(prompt, target) end
	end)
end

local function stopAutoSteal()
	if stealConnection then stealConnection:Disconnect(); stealConnection = nil end
	StealState.active = false; StealState.startTime = 0
	Steal.plotCache = {}; Steal.plotCacheTime = {}; Steal.cachedPrompts = {}
	allAnimalsCache = {}; PromptMemoryCache = {}; InternalStealCache = {}
end

local function updateUI()
	while not progressFill or not progressPct do task.wait(0.5) end
	while true do
		local character = LP.Character
		local inPrimeRange = false
		if character then
			local hrp = character:FindFirstChild("HumanoidRootPart") or character:FindFirstChild("UpperTorso")
			if hrp then
				local closest = pickClosest()
				if closest then
					local dist = distToAnimal(closest)
					inPrimeRange = dist <= CONFIG.PRIME_RANGE
				end
			end
		end
		if StealState.active and StealState.startTime > 0 then
			local elapsed = tick() - StealState.startTime
			local percent = math.clamp(elapsed / CONFIG.HOLD_MAX, 0, 1)
			progressFill.Size = UDim2.new(percent, 0, 1, 0)
			if elapsed >= CONFIG.HOLD_MIN then
				progressPct.Text = "READY"
			else
				progressPct.Text = "NOT READY"
			end
		elseif not inPrimeRange then
			progressFill.Size = UDim2.new(0, 0, 1, 0)
			progressPct.Text = "NOT READY"
		else
			progressFill.Size = UDim2.new(0, 0, 1, 0)
			progressPct.Text = "READY"
		end
		task.wait()
	end
end

task.spawn(updateUI)
task.spawn(function()
	while task.wait(5) do scanAllPlots() end
end)
scanAllPlots()
-- ── End Auto Steal Logic (message 29) ────────────────────────────────────────

  
local _collideTick = 0  
RunService.Stepped:Connect(function()  
	local now = tick()  
	if now - _collideTick < 0.5 then return end  
	_collideTick = now  
	for _,p in ipairs(Players:GetPlayers()) do  
		if p~=LP and p.Character then  
			for _,part in ipairs(p.Character:GetDescendants()) do  
				if part:IsA("BasePart") then part.CanCollide=false end  
			end  
		end  
	end  
end)  
  
-- ─── K7 Envy Speed Logic ─────────────────────────────────────────────────────  
local _lastMoveDir = Vector3.new(0,0,0)  
local _tpInProgress = false  
local MOVE_KEYS = {  
	[Enum.KeyCode.W]=true,[Enum.KeyCode.A]=true,[Enum.KeyCode.S]=true,[Enum.KeyCode.D]=true,  
	[Enum.KeyCode.Up]=true,[Enum.KeyCode.Left]=true,[Enum.KeyCode.Down]=true,[Enum.KeyCode.Right]=true,  
}  
RunService.RenderStepped:Connect(function()  
	local char=LP.Character; if not char then return end  
	local hum=char:FindFirstChildOfClass("Humanoid")  
	local hrp=char:FindFirstChild("HumanoidRootPart")  
	if not hum or not hrp then return end  
	if _tpInProgress then return end  
	if not autoBatEnabled and not autoLeftEnabled and not autoRightEnabled then  
		local md = hum.MoveDirection
		local spd = (activeSpeedMode == "lagger") and (carryActive and LS2 or LS) or (carryActive and CS or NS)
		if md.Magnitude > 0 then  
			_lastMoveDir = md  
			hrp.Velocity = Vector3.new(md.X*spd, hrp.Velocity.Y, md.Z*spd)  
		elseif antiRagdollEnabled and _lastMoveDir.Magnitude > 0 then  
			local anyHeld = false  
			for key in pairs(MOVE_KEYS) do if UIS:IsKeyDown(key) then anyHeld=true; break end end  
			if anyHeld then hrp.Velocity = Vector3.new(_lastMoveDir.X*spd, hrp.Velocity.Y, _lastMoveDir.Z*spd) end  
		end  
	end  
	pcall(function()  
		if speedLabel then  
			local hspd = Vector3.new(hrp.Velocity.X,0,hrp.Velocity.Z).Magnitude  
			speedLabel.Text = string.format("%.1f",hspd)  
			if _guiSpeedLbl then  
				_guiSpeedLbl.Text = string.format("%.1f",hspd)  
				if _guiSpeedSubLbl then  
					local _modeStr = activeSpeedMode:sub(1,1):upper()..activeSpeedMode:sub(2)
					_guiSpeedSubLbl.Text = "studs/s  ·  "..(_modeStr)..(carryActive and " Carry" or "")  
				end  
			end  
		end  
	end)  
end)  
-- ─── End K7 Envy Speed Logic ──────────────────────────────────────────────────  
  
local alConn,arConn = nil,nil  
local alPhase,arPhase = 1,1  
  
local AP_L1     = Vector3.new(-476.48, -6.28,  92.73)  
local AP_L2     = Vector3.new(-483.12, -4.95,  94.80)  
local AP_L_FACE = Vector3.new(-482.25, -4.96,  92.09)  
local AP_R1     = Vector3.new(-476.16, -6.52,  25.62)  
local AP_R2     = Vector3.new(-483.06, -5.03,  25.48)  
local AP_R_FACE = Vector3.new(-482.06, -6.93,  35.47)  
  
local function stopAutoLeft()  
	if alConn then alConn:Disconnect();alConn=nil end;alPhase=1  
	local char=LP.Character;if char then local h=char:FindFirstChildOfClass("Humanoid");if h then h:Move(Vector3.zero,false) end end  
end  
  
local function stopAutoRight()  
	if arConn then arConn:Disconnect();arConn=nil end;arPhase=1  
	local char=LP.Character;if char then local h=char:FindFirstChildOfClass("Humanoid");if h then h:Move(Vector3.zero,false) end end  
end  
  
local function startAutoLeft()  
	if alConn then alConn:Disconnect() end;alPhase=1  
	alConn=RunService.Heartbeat:Connect(function()  
		if not autoLeftEnabled then return end  
		local char=LP.Character;if not char then return end  
		local hrp2=char:FindFirstChild("HumanoidRootPart")  
		local hum=char:FindFirstChildOfClass("Humanoid")  
		if not hrp2 or not hum then return end  
		local spd=(activeSpeedMode=="lagger") and LS or NS  
		if alPhase==1 then  
			local tgt=Vector3.new(AP_L1.X,hrp2.Position.Y,AP_L1.Z)  
			if (tgt-hrp2.Position).Magnitude<1 then  
				alPhase=2  
				local d=AP_L2-hrp2.Position;local mv=Vector3.new(d.X,0,d.Z).Unit  
				hum:Move(mv,false);hrp2.AssemblyLinearVelocity=Vector3.new(mv.X*spd,hrp2.AssemblyLinearVelocity.Y,mv.Z*spd);return  
			end  
			local d=AP_L1-hrp2.Position;local mv=Vector3.new(d.X,0,d.Z).Unit  
			hum:Move(mv,false);hrp2.AssemblyLinearVelocity=Vector3.new(mv.X*spd,hrp2.AssemblyLinearVelocity.Y,mv.Z*spd)  
		elseif alPhase==2 then  
			local tgt=Vector3.new(AP_L2.X,hrp2.Position.Y,AP_L2.Z)  
			if (tgt-hrp2.Position).Magnitude<1 then  
				hum:Move(Vector3.zero,false);hrp2.AssemblyLinearVelocity=Vector3.zero  
				autoLeftEnabled=false;if alConn then alConn:Disconnect();alConn=nil end  
				alPhase=1;if autoLeftSetVisual then autoLeftSetVisual(false) end  
				if (AP_L_FACE-hrp2.Position).Magnitude>0.01 then  
					hrp2.CFrame=CFrame.new(hrp2.Position,Vector3.new(AP_L_FACE.X,hrp2.Position.Y,AP_L_FACE.Z))  
				end  
				return  
			end  
			local d=AP_L2-hrp2.Position;local mv=Vector3.new(d.X,0,d.Z).Unit  
			hum:Move(mv,false);hrp2.AssemblyLinearVelocity=Vector3.new(mv.X*spd,hrp2.AssemblyLinearVelocity.Y,mv.Z*spd)  
		end  
	end)  
end  
  
function startAutoRight()  
	if arConn then arConn:Disconnect() end;arPhase=1  
	arConn=RunService.Heartbeat:Connect(function()  
		if not autoRightEnabled then return end  
		local char=LP.Character;if not char then return end  
		local hrp2=char:FindFirstChild("HumanoidRootPart")  
		local hum=char:FindFirstChildOfClass("Humanoid")  
		if not hrp2 or not hum then return end  
		local spd=(activeSpeedMode=="lagger") and LS or NS  
		if arPhase==1 then  
			local tgt=Vector3.new(AP_R1.X,hrp2.Position.Y,AP_R1.Z)  
			if (tgt-hrp2.Position).Magnitude<1 then  
				arPhase=2  
				local d=AP_R2-hrp2.Position;local mv=Vector3.new(d.X,0,d.Z).Unit  
				hum:Move(mv,false);hrp2.AssemblyLinearVelocity=Vector3.new(mv.X*spd,hrp2.AssemblyLinearVelocity.Y,mv.Z*spd);return  
			end  
			local d=AP_R1-hrp2.Position;local mv=Vector3.new(d.X,0,d.Z).Unit  
			hum:Move(mv,false);hrp2.AssemblyLinearVelocity=Vector3.new(mv.X*spd,hrp2.AssemblyLinearVelocity.Y,mv.Z*spd)  
		elseif arPhase==2 then  
			local tgt=Vector3.new(AP_R2.X,hrp2.Position.Y,AP_R2.Z)  
			if (tgt-hrp2.Position).Magnitude<1 then  
				hum:Move(Vector3.zero,false);hrp2.AssemblyLinearVelocity=Vector3.zero  
				autoRightEnabled=false;if arConn then arConn:Disconnect();arConn=nil end  
				arPhase=1;if autoRightSetVisual then autoRightSetVisual(false) end  
				if (AP_R_FACE-hrp2.Position).Magnitude>0.01 then  
					hrp2.CFrame=CFrame.new(hrp2.Position,Vector3.new(AP_R_FACE.X,hrp2.Position.Y,AP_R_FACE.Z))  
				end  
				return  
			end  
			local d=AP_R2-hrp2.Position;local mv=Vector3.new(d.X,0,d.Z).Unit  
			hum:Move(mv,false);hrp2.AssemblyLinearVelocity=Vector3.new(mv.X*spd,hrp2.AssemblyLinearVelocity.Y,mv.Z*spd)  
		end  
	end)  
end  
  
-- K7-style speed billboard: shows raw speed number, color reflects current mode  
local function setupSpeedIndicator(char)  
	local head = char:WaitForChild("Head",5);if not head then return end  
	-- remove old billboard if respawning  
	local old=head:FindFirstChild("italozinSpeedBB");if old then old:Destroy() end  
	local oldBB=head:FindFirstChild("italozinSpeedBB"); if oldBB then oldBB:Destroy() end  
	local bb=Instance.new("BillboardGui",head); bb.Name="italozinSpeedBB"  
	bb.Size=UDim2.new(0,180,0,62); bb.StudsOffset=Vector3.new(0,3,0); bb.AlwaysOnTop=true  
	speedLabel=Instance.new("TextLabel",bb); speedLabel.Name="SpeedBillLbl"  
	speedLabel.Size=UDim2.new(1,0,0,24); speedLabel.Position=UDim2.new(0,0,0,0); speedLabel.BackgroundTransparency=1  
	speedLabel.Text="0.0"; speedLabel.TextColor3=Color3.fromRGB(255,182,215)  
	speedLabel.Font=Enum.Font.GothamBlack; speedLabel.TextScaled=true  
	speedLabel.TextStrokeTransparency=0; speedLabel.TextStrokeColor3=Color3.new(0,0,0)  
	local discordLbl=Instance.new("TextLabel",bb)  
	discordLbl.Size=UDim2.new(1,0,0,36); discordLbl.Position=UDim2.new(0,0,0,26)  
	discordLbl.BackgroundTransparency=1; discordLbl.Text="discord.gg/italozinhubb"  
	discordLbl.TextColor3=Color3.fromRGB(255,182,215); discordLbl.Font=Enum.Font.GothamBold  
	discordLbl.TextScaled=true; discordLbl.TextStrokeTransparency=0.1  
	discordLbl.TextStrokeColor3=Color3.new(0,0,0)  
end  
  
-- Bat-hit detection connections (used by bat counter)  
local _batHitDetectConns = {}  
local _lastHealth = 100  
local _healthDropTime = 0  
local BAT_HIT_HEALTH_WINDOW = 0.35  -- seconds: if health drops then ragdoll fires within this window, it's a bat hit  
  
local function startAntiRagdoll()  
	if Conns.antiRag then return end  
	local _wasRagdolled = false  
	Conns.antiRag = RunService.Heartbeat:Connect(function()  
		local char = LP.Character;if not char then return end  
		local hum = char:FindFirstChildOfClass("Humanoid");local root=char:FindFirstChild("HumanoidRootPart")  
		if hum then  
			local st = hum:GetState()  
			local isRag = st==Enum.HumanoidStateType.Physics or st==Enum.HumanoidStateType.Ragdoll or st==Enum.HumanoidStateType.FallingDown  
			if isRag then  
				if not _wasRagdolled then _wasRagdolled = true end  
				hum:ChangeState(Enum.HumanoidStateType.Running)  
				workspace.CurrentCamera.CameraSubject=hum  
				pcall(function() local pm=LP.PlayerScripts:FindFirstChild("PlayerModule");if pm then require(pm:FindFirstChild("ControlModule")):Enable() end end)  
				if root then root.Velocity=Vector3.zero;root.RotVelocity=Vector3.zero end  
			else  
				if _wasRagdolled then _wasRagdolled = false end  
			end  
		end  
		for _,obj in ipairs(char:GetDescendants()) do if obj:IsA("Motor6D") and not obj.Enabled then obj.Enabled=true end end  
	end)  
end  
  
local function stopAntiRagdoll()  
	if Conns.antiRag then Conns.antiRag:Disconnect();Conns.antiRag=nil end  
end  
  
-- ── Infinite Jump (GreenDuels) ───────────────────────────────────────────────
local _ijHoldPressed = false
local _ijHoldActive  = false
local _ijJumpConn    = nil
local _ijInputConn   = nil
local _ijInputEndConn = nil
local _ijHeartbeatConn = nil

local function applyInfJumpBoost()
	if not infJumpEnabled then return end
	local char = LP.Character; if not char then return end
	local root = char:FindFirstChild("HumanoidRootPart"); if not root then return end
	root.Velocity = Vector3.new(root.Velocity.X, 50, root.Velocity.Z)
end

local function startInfiniteJump()
	if _ijJumpConn then return end
	-- JumpRequest: fires on mobile tap, controller A, and PC space press
	_ijJumpConn = UIS.JumpRequest:Connect(function()
		applyInfJumpBoost()
	end)
	-- Space hold detection (PC / keyboard)
	_ijInputConn = UIS.InputBegan:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.Keyboard
		and input.KeyCode == Enum.KeyCode.Space
		and not UIS:GetFocusedTextBox() then
			_ijHoldPressed = true
			task.delay(0.12, function()
				if _ijHoldPressed then
					_ijHoldActive = true
					applyInfJumpBoost()
				end
			end)
		end
	end)
	_ijInputEndConn = UIS.InputEnded:Connect(function(input)
		if input.UserInputType == Enum.UserInputType.Keyboard
		and input.KeyCode == Enum.KeyCode.Space then
			_ijHoldPressed = false
			_ijHoldActive  = false
		end
	end)
	-- Heartbeat: continuously boost while space is held
	_ijHeartbeatConn = RunService.Heartbeat:Connect(function()
		if _ijHoldActive then applyInfJumpBoost() end
	end)
end

local function stopInfiniteJump()
	_ijHoldPressed = false
	_ijHoldActive  = false
	if _ijJumpConn       then _ijJumpConn:Disconnect();       _ijJumpConn = nil       end
	if _ijInputConn      then _ijInputConn:Disconnect();      _ijInputConn = nil      end
	if _ijInputEndConn   then _ijInputEndConn:Disconnect();   _ijInputEndConn = nil   end
	if _ijHeartbeatConn  then _ijHeartbeatConn:Disconnect();  _ijHeartbeatConn = nil  end
end



  
local function disableAnimations()
	local char = LP.Character; if not char then return end
	for _, obj in ipairs(char:GetDescendants()) do
		if obj:IsA("Humanoid") then
			pcall(function() obj.WalkSpeed = 0 end)
		end
		if obj:IsA("AnimationController") or obj:IsA("Animator") then
			for _, t in ipairs(obj:GetPlayingAnimationTracks()) do
				pcall(function() t:Stop(0) end)
			end
		end
	end
end

local function enableAnimations()
	local char = LP.Character; if not char then return end
	for _, obj in ipairs(char:GetDescendants()) do
		if obj:IsA("Humanoid") then
			pcall(function()
				local anim = obj:FindFirstChildOfClass("Animator")
				if anim then anim:LoadAnimation(Instance.new("Animation")):Play() end
			end)
		end
	end
	unwalkAnimations = {}
end

RunService.Heartbeat:Connect(function()  
	if not unwalkEnabled then return end  
	disableAnimations()  
end)  
  
  
local DROP_ASCEND_DURATION = 0.22
local DROP_ASCEND_SPEED    = 160
local _dropConn            = nil
local _dropActive          = false

local function runDrop()
	if _dropActive then return end
	local char = LP.Character; if not char then return end
	local root = char:FindFirstChild("HumanoidRootPart"); if not root then return end
	_dropActive = true
	local t0 = tick()
	if _dropConn then _dropConn:Disconnect() end
	_dropConn = RunService.Heartbeat:Connect(function()
		local c = LP.Character
		local r = c and c:FindFirstChild("HumanoidRootPart")
		if not r then
			if _dropConn then _dropConn:Disconnect(); _dropConn = nil end
			_dropActive = false
			return
		end
		if not _dropActive then
			if _dropConn then _dropConn:Disconnect(); _dropConn = nil end
			return
		end
		if tick() - t0 >= DROP_ASCEND_DURATION then
			if _dropConn then _dropConn:Disconnect(); _dropConn = nil end
			pcall(function()
				local rp = RaycastParams.new()
				rp.FilterDescendantsInstances = {c}
				rp.FilterType = Enum.RaycastFilterType.Exclude
				local rr = workspace:Raycast(r.Position, Vector3.new(0, -3000, 0), rp)
				if rr then
					local hum = c:FindFirstChildOfClass("Humanoid")
					local off = ((hum and hum.HipHeight) or 2) + (r.Size.Y / 2)
					r.CFrame = CFrame.new(r.Position.X, rr.Position.Y + off, r.Position.Z)
					r.AssemblyLinearVelocity = Vector3.zero
				end
			end)
			_dropActive = false
			return
		end
		local lv = r.AssemblyLinearVelocity
		r.AssemblyLinearVelocity = Vector3.new(lv.X, DROP_ASCEND_SPEED, lv.Z)
	end)
end

LP.CharacterRemoving:Connect(function()
	_dropActive = false
	if _dropConn then _dropConn:Disconnect(); _dropConn = nil end
end)

function runTPDown()  
	local char=LP.Character; if not char then return end  
	local root=char:FindFirstChild("HumanoidRootPart")  
	local hum=char:FindFirstChildOfClass("Humanoid")  
	if not root or not hum then return end  
	local rp=RaycastParams.new()  
	rp.FilterDescendantsInstances={char}  
	rp.FilterType=Enum.RaycastFilterType.Exclude  
	local result=workspace:Raycast(root.Position,Vector3.new(0,-1000,0),rp)  
	if result then  
		local hipOffset=hum.HipHeight+(root.Size.Y/2)  
		root.CFrame=CFrame.new(result.Position+Vector3.new(0,hipOffset,0))*root.CFrame.Rotation  
	end  
end  
  
  
  

-- ── Message 22 Auto TP Down ───────────────────────────────────────────────────
local function doAutoTPDown(force)
	local char=LP.Character; if not char then return end
	local hrp=char:FindFirstChild("HumanoidRootPart"); if not hrp then return end
	local hum2=char:FindFirstChildOfClass("Humanoid"); if not hum2 then return end
	if not force then
		if hum2.FloorMaterial~=Enum.Material.Air then return end
		if hrp.Position.Y<autoTPHeight then return end
	end
	hrp.CFrame=CFrame.new(hrp.Position.X,-7.00,hrp.Position.Z)
		*CFrame.Angles(0,select(2,hrp.CFrame:ToEulerAnglesYXZ()),0)
	hrp.AssemblyLinearVelocity=Vector3.zero
end
local function startAutoTP()
	if autoTPConn then task.cancel(autoTPConn); autoTPConn=nil end
	autoTPConn=task.spawn(function()
		while autoTPEnabled do
			task.wait(0.1)
			pcall(function() doAutoTPDown(false) end)
		end
	end)
end
local function stopAutoTP()
	autoTPEnabled=false
	if autoTPConn then task.cancel(autoTPConn); autoTPConn=nil end
end
-- ── End Message 22 Auto TP Down ───────────────────────────────────────────────

local stretchRezConn = nil  
local function enableStretchRez()  
	stretchRezEnabled=true  
	workspace.CurrentCamera.FieldOfView=120  
	if stretchRezConn then stretchRezConn:Disconnect() end  
	stretchRezConn=RunService.RenderStepped:Connect(function()  
		if not stretchRezEnabled then stretchRezConn:Disconnect();stretchRezConn=nil;return end  
		workspace.CurrentCamera.FieldOfView=120  
	end)  
end  
  
local function disableStretchRez()  
	stretchRezEnabled=false  
	if stretchRezConn then stretchRezConn:Disconnect();stretchRezConn=nil end  
	workspace.CurrentCamera.FieldOfView=70  
end  
  
local function enableRemoveAccessories()  
	removeAccessoriesEnabled=true  
	for _,p in pairs(Players:GetPlayers()) do  
		if p.Character then  
			for _,obj in ipairs(p.Character:GetDescendants()) do  
				if obj:IsA("Accessory") or obj:IsA("Hat") then pcall(function() obj:Destroy() end) end  
			end  
		end  
	end  
	if not removeAccessoriesConn then  
		removeAccessoriesConn=Players.PlayerAdded:Connect(function(pl)  
			pl.CharacterAdded:Connect(function(char)  
				task.wait(0.5)  
				if not removeAccessoriesEnabled then return end  
				for _,obj in ipairs(char:GetDescendants()) do  
					if obj:IsA("Accessory") or obj:IsA("Hat") then pcall(function() obj:Destroy() end) end  
				end  
			end)  
		end)  
	end  
end  
  
local function disableRemoveAccessories()  
	removeAccessoriesEnabled=false  
	if removeAccessoriesConn then removeAccessoriesConn:Disconnect();removeAccessoriesConn=nil end  
end  
  
-- ─── Cursed Hub Anti Lag ─────────────────────────────────────────────────────  
local antiLagEnabled = false  
local antiLagDescConn = nil  
local antiLagDefBrightness, antiLagDefFog, antiLagDefDiffuse, antiLagDefSpecular  
  
local function _applyAntiLagObj(obj)  
	pcall(function()  
		if obj:IsA("BasePart") then  
			obj.Material=Enum.Material.Plastic; obj.Reflectance=0; obj.CastShadow=false  
		elseif obj:IsA("Decal") or obj:IsA("Texture") then  
			obj.Transparency=1  
		elseif obj:IsA("ParticleEmitter") or obj:IsA("Trail") or obj:IsA("Beam")  
		or obj:IsA("Fire") or obj:IsA("Smoke") or obj:IsA("Sparkles") then  
			obj.Enabled=false  
		elseif obj:IsA("AnimationController") or obj:IsA("Animator") then  
			for _,t in ipairs(obj:GetPlayingAnimationTracks()) do pcall(function() t:Stop(0) end) end  
		end  
	end)  
end  
local function enableAntiLag()  
	antiLagEnabled=true  
	-- Save lighting state  
	antiLagDefBrightness = antiLagDefBrightness or Lighting.Brightness  
	antiLagDefFog = antiLagDefFog or Lighting.FogEnd  
	antiLagDefDiffuse = antiLagDefDiffuse or Lighting.EnvironmentDiffuseScale  
	antiLagDefSpecular = antiLagDefSpecular or Lighting.EnvironmentSpecularScale  
	-- Lighting tweaks (no dark mode — keep brightness/sky as is)  
	Lighting.GlobalShadows=false  
	Lighting.FogEnd=1e10  
	Lighting.EnvironmentDiffuseScale=0  
	Lighting.EnvironmentSpecularScale=0  
	for _,e in pairs(Lighting:GetChildren()) do  
		pcall(function()  
			if e:IsA("BlurEffect") or e:IsA("SunRaysEffect") or e:IsA("ColorCorrectionEffect")  
			or e:IsA("BloomEffect") or e:IsA("DepthOfFieldEffect") then e.Enabled=false end  
		end)  
	end  
	-- Derender workspace objects  
	for _,obj in ipairs(workspace:GetDescendants()) do _applyAntiLagObj(obj) end  
	if antiLagDescConn then antiLagDescConn:Disconnect() end  
	antiLagDescConn=workspace.DescendantAdded:Connect(function(obj)  
		if antiLagEnabled then _applyAntiLagObj(obj) end  
	end)  
end  
local function disableAntiLag()  
	antiLagEnabled=false  
	if antiLagDescConn then antiLagDescConn:Disconnect(); antiLagDescConn=nil end  
	pcall(function()  
		Lighting.GlobalShadows=true  
		if antiLagDefBrightness then Lighting.Brightness=antiLagDefBrightness end  
		if antiLagDefFog then Lighting.FogEnd=antiLagDefFog end  
		if antiLagDefDiffuse then Lighting.EnvironmentDiffuseScale=antiLagDefDiffuse end  
		if antiLagDefSpecular then Lighting.EnvironmentSpecularScale=antiLagDefSpecular end  
		for _,e in pairs(Lighting:GetChildren()) do  
			pcall(function()  
				if e:IsA("BlurEffect") or e:IsA("SunRaysEffect") or e:IsA("ColorCorrectionEffect")  
				or e:IsA("BloomEffect") or e:IsA("DepthOfFieldEffect") then e.Enabled=true end  
			end)  
		end  
	end)  
end  
-- ─── End Cursed Hub Anti Lag ──────────────────────────────────────────────────  
  
local nightSkyName = "italozinfySky"  
local function applyNightMode(on)  
	nightModeEnabled=on  
	if on then  
		local sky=Lighting:FindFirstChild(nightSkyName) or Instance.new("Sky")  
		sky.Name=nightSkyName  
		-- pink sky: use solid pink color via atmosphere + tint  
		sky.SkyboxBk="rbxassetid://159454299"  
		sky.SkyboxDn="rbxassetid://159454296"  
		sky.SkyboxFt="rbxassetid://159454293"  
		sky.SkyboxLf="rbxassetid://159454286"  
		sky.SkyboxRt="rbxassetid://159454289"  
		sky.SkyboxUp="rbxassetid://159454291"  
		sky.Parent=Lighting  
		Lighting.Brightness=1.8  
		Lighting.ClockTime=14  
		Lighting.ExposureCompensation=0  
		Lighting.OutdoorAmbient=Color3.fromRGB(255,160,195)  
		-- pink atmosphere  
		local atmo=Lighting:FindFirstChildOfClass("Atmosphere") or Instance.new("Atmosphere",Lighting)  
		atmo.Name="italozinAtmo"  
		atmo.Color=Color3.fromRGB(255,155,190)  
		atmo.Density=0.55  
		atmo.Offset=0.2  
		atmo.Glare=0.35  
		atmo.Haze=2  
		-- pink color correction  
		local cc=Lighting:FindFirstChildOfClass("ColorCorrectionEffect") or Instance.new("ColorCorrectionEffect",Lighting)  
		cc.Name="italozinCC"  
		cc.TintColor=Color3.fromRGB(255,195,220)  
		cc.Brightness=0.03  
		cc.Contrast=0.03  
		cc.Saturation=0.25  
	else  
		local s=Lighting:FindFirstChild(nightSkyName);if s then s:Destroy() end  
		local a=Lighting:FindFirstChild("italozonAtmo");if a then a:Destroy() end  
		local c=Lighting:FindFirstChild("italozinCC");if c then c:Destroy() end  
		Lighting.Brightness=defLightBrightness  
		Lighting.ClockTime=defClockTime  
		Lighting.ExposureCompensation=defExposureComp  
		Lighting.OutdoorAmbient=defOutdoorAmbient  
	end  
end  
  
local function findMedusa()  
	local char = LP.Character;if not char then return nil end  
	for _,t in ipairs(char:GetChildren()) do if t:IsA("Tool") and t.Name:lower():find("medusa") then return t end end  
	local bp = LP:FindFirstChild("Backpack")  
	if bp then for _,t in ipairs(bp:GetChildren()) do if t:IsA("Tool") and t.Name:lower():find("medusa") then return t end end end  
end  
  
local function useMedusa()  
	if medusaDebounce or tick()-medusaLastUsed<MEDUSA_COOLDOWN then return end  
	local char = LP.Character;if not char then return end  
	medusaDebounce=true  
	local med = findMedusa()  
	if med then  
		if med.Parent~=char then local h=char:FindFirstChildOfClass("Humanoid");if h then h:EquipTool(med) end end  
		pcall(function() med:Activate() end);medusaLastUsed=tick()  
	end  
	medusaDebounce=false  
end  
  
local function onAnchorChanged(part)  
	return part:GetPropertyChangedSignal("Anchored"):Connect(function()  
		if part.Anchored and part.Transparency==1 and medusaCounterEnabled then useMedusa() end  
	end)  
end  
  
local function setupMedusa(char)  
	for _,c in pairs(medusaConns) do pcall(function() c:Disconnect() end) end;medusaConns={}  
	if not char then return end  
	for _,part in ipairs(char:GetDescendants()) do if part:IsA("BasePart") then table.insert(medusaConns,onAnchorChanged(part)) end end  
	table.insert(medusaConns,char.DescendantAdded:Connect(function(part)  
		if part:IsA("BasePart") then table.insert(medusaConns,onAnchorChanged(part)) end  
	end))  
end  
  
local function getClosestPlayer()  
	local char = LP.Character;if not char then return nil,math.huge end  
	local hrp = char:FindFirstChild("HumanoidRootPart");if not hrp then return nil,math.huge end  
	local cp,cd = nil,math.huge  
	for _,p in pairs(Players:GetPlayers()) do  
		if p~=LP and p.Character then  
			local tr = p.Character:FindFirstChild("HumanoidRootPart")  
			if tr then local d=(hrp.Position-tr.Position).Magnitude;if d<cd then cd=d;cp=p end end  
		end  
	end  
	return cp,cd  
end  
  
  
-- ─── Envy Bat Aimbot ─────────────────────────────────────────────────────────  
local _aimbotTarget = nil  
  
local function findBat()  
	local char = LP.Character; if not char then return nil end  
	for _, tool in ipairs(char:GetChildren()) do  
		if tool:IsA("Tool") and (tool.Name:lower():find("bat") or tool.Name:lower():find("slap")) then return tool end  
	end  
	local bp = LP:FindFirstChild("Backpack")  
	if bp then  
		for _, tool in ipairs(bp:GetChildren()) do  
			if tool:IsA("Tool") and (tool.Name:lower():find("bat") or tool.Name:lower():find("slap")) then return tool end  
		end  
	end  
	return nil  
end  
  
local function getClosestTarget()  
	local root = LP.Character and LP.Character:FindFirstChild("HumanoidRootPart")  
	if not root then return nil end  
	local closest, minDist = nil, math.huge  
	for _, plr in ipairs(Players:GetPlayers()) do  
		if plr ~= LP and plr.Character then  
			local tRoot = plr.Character:FindFirstChild("HumanoidRootPart")  
			local hum = plr.Character:FindFirstChildOfClass("Humanoid")  
			if tRoot and hum and hum.Health > 0 then  
				local dist = (tRoot.Position - root.Position).Magnitude  
				if dist < minDist then minDist = dist; closest = tRoot end  
			end  
		end  
	end  
	return closest  
end  
  
local _batAimbotConn = nil  
  
startBatAimbot = function()  
	if _batAimbotConn then _batAimbotConn:Disconnect() end  
	if autoLeftEnabled  then autoLeftEnabled=false;  if autoLeftSetVisual  then autoLeftSetVisual(false)  end; stopAutoLeft()  end  
	if autoRightEnabled then autoRightEnabled=false; if autoRightSetVisual then autoRightSetVisual(false) end; stopAutoRight() end  
	local hum0 = LP.Character and LP.Character:FindFirstChildOfClass("Humanoid")  
	if hum0 then hum0.AutoRotate = false end  
	_batAimbotConn = RunService.RenderStepped:Connect(function()  
		if not autoBatEnabled then return end  
		local char = LP.Character; if not char then return end  
		local root = char:FindFirstChild("HumanoidRootPart"); if not root then return end  
		local hum  = char:FindFirstChildOfClass("Humanoid");  if not hum  then return end  
		if not char:FindFirstChildOfClass("Tool") then  
			local bat = findBat()  
			if bat then pcall(function() hum:EquipTool(bat) end) end  
		end  
		local target = getClosestTarget()  
		if not target then return end  
		_aimbotTarget = target  
		local targetVel = target.AssemblyLinearVelocity  
		local myPos     = root.Position  
		local targetPos = target.Position  
		local predictPos = targetPos + targetVel * 0.14  
		predictPos = predictPos + target.CFrame.LookVector * 0.3  
		local direction = predictPos - myPos  
		local flatDir = Vector3.new(direction.X, 0, direction.Z).Unit  
		local chaseSpeed = autoBatSpeed  
		local desiredHeight = targetPos.Y + 3.7  
		local yVel = (desiredHeight - myPos.Y) * 19.5 + targetVel.Y * 0.8  
		if hum.FloorMaterial ~= Enum.Material.Air then yVel = math.max(yVel, 13) end  
		yVel = math.clamp(yVel, -70, 110)  
		local desiredVel = Vector3.new(flatDir.X * chaseSpeed, yVel, flatDir.Z * chaseSpeed)  
		root.AssemblyLinearVelocity = root.AssemblyLinearVelocity:Lerp(desiredVel, 0.8)  
		local speed3 = targetVel.Magnitude  
		local predictTime = math.clamp(speed3 / 150, 0.05, 0.2)  
		local predictedPos = targetPos + targetVel * predictTime  
		local toPredict = predictedPos - myPos  
		if toPredict.Magnitude > 0.1 then  
			local goalCF = CFrame.lookAt(myPos, predictedPos)  
			local diffCF = root.CFrame:Inverse() * goalCF  
			local rx, ry, rz = diffCF:ToEulerAnglesXYZ()  
			rx = math.clamp(rx,-2.5,2.5); ry = math.clamp(ry,-2.5,2.5); rz = math.clamp(rz,-2.5,2.5)  
			root.AssemblyAngularVelocity = root.CFrame:VectorToWorldSpace(Vector3.new(rx*42, ry*42, rz*42))  
		end  
	end)  
end  
  
stopBatAimbot = function()  
	if _batAimbotConn then _batAimbotConn:Disconnect(); _batAimbotConn = nil end  
	_aimbotTarget = nil  
	local c    = LP.Character  
	local root = c and c:FindFirstChild("HumanoidRootPart")  
	if root then root.AssemblyLinearVelocity = Vector3.zero; root.AssemblyAngularVelocity = Vector3.zero end  
	local hum2 = c and c:FindFirstChildOfClass("Humanoid")  
	if hum2 then hum2.AutoRotate = true end  
end  
-- ─── End Envy Bat Aimbot ─────────────────────────────────────────────────────  

-- ── Body Lock ─────────────────────────────────────────────────────────────────
-- Faces nearest player within bodyLockRange.
-- When NO target is in range, AutoRotate is restored so you can still turn freely.
local function _bodyLockTick()
	local char = LP.Character; if not char then return end
	local root = char:FindFirstChild("HumanoidRootPart"); if not root then return end
	local hum  = char:FindFirstChildOfClass("Humanoid"); if not hum then return end
	local target = getClosestTarget()
	if not target then
		if not hum.AutoRotate then hum.AutoRotate = true end
		return
	end
	local dist = (target.Position - root.Position).Magnitude
	if dist > bodyLockRange then
		if not hum.AutoRotate then hum.AutoRotate = true end
		return
	end
	-- target found and in range — face them, Y-axis only (no tilt up/down)
	if hum.AutoRotate then hum.AutoRotate = false end
	local targetVel    = target.AssemblyLinearVelocity
	local speed3       = targetVel.Magnitude
	local predictTime  = math.clamp(speed3 / 150, 0.05, 0.2)
	-- flatten predicted position to our Y so we never pitch up or down
	local predictedPos = target.Position + targetVel * predictTime
	local flatTarget   = Vector3.new(predictedPos.X, root.Position.Y, predictedPos.Z)
	local toPredict    = flatTarget - root.Position
	if toPredict.Magnitude > 0.1 then
		local goalCF = CFrame.lookAt(root.Position, flatTarget)
		local diffCF = root.CFrame:Inverse() * goalCF
		local _, ry, _ = diffCF:ToEulerAnglesXYZ()
		ry = math.clamp(ry, -2.5, 2.5)
		root.AssemblyAngularVelocity = root.CFrame:VectorToWorldSpace(Vector3.new(0, ry * 42, 0))
	end
end

local function startBodyLock()
	if _bodyLockConn then _bodyLockConn:Disconnect() end
	_bodyLockConn = RunService.RenderStepped:Connect(function()
		if not bodyLockEnabled then return end
		_bodyLockTick()
	end)
end

local function stopBodyLock()
	if _bodyLockConn then _bodyLockConn:Disconnect(); _bodyLockConn = nil end
	local c    = LP.Character
	local root = c and c:FindFirstChild("HumanoidRootPart")
	if root then root.AssemblyAngularVelocity = Vector3.zero end
	local hum2 = c and c:FindFirstChildOfClass("Humanoid")
	if hum2 then hum2.AutoRotate = true end
end
-- ── End Body Lock ─────────────────────────────────────────────────────────────

  
  
  
-- ===== BAT COUNTER =====  
local batCounterEnabled=false  
local batCounterDebounce=false  
local batCounterLastUsed=0  
local BAT_COUNTER_COOLDOWN=0.8  -- prevent spam-firing  
local batCounterConns={}  
local setBatCounterVisual=nil  
  
local function _swingBatCounter()  
	if batCounterDebounce then return end  
	if tick()-batCounterLastUsed < BAT_COUNTER_COOLDOWN then return end  
	if not batCounterEnabled then return end  
	batCounterDebounce=true  
	batCounterLastUsed=tick()  
	task.spawn(function()  
		local char=LP.Character  
		if not char then batCounterDebounce=false;return end  
		local hum=char:FindFirstChildOfClass("Humanoid")  
		local hrp=char:FindFirstChild("HumanoidRootPart")  
		local bp=LP:FindFirstChild("Backpack")  
  
		-- 1) Re-enable all joints so the character stands up  
		for _,obj in ipairs(char:GetDescendants()) do  
			if obj:IsA("Motor6D") then pcall(function() obj.Enabled=true end) end  
		end  
  
		-- 2) Force running state WITHOUT touching PlatformStand (avoids reset loop)  
		if hum then  
			pcall(function() hum:ChangeState(Enum.HumanoidStateType.Running) end)  
		end  
  
		-- 3) Find bat already in hand first, then backpack — do NOT use EquipTool  
		--    (EquipTool triggers StateChanged → ragdoll → counter fires again → loop)  
		local bat = char:FindFirstChildOfClass("Tool")  
		if not bat or not bat.Name:lower():find("bat") then  
			bat = nil  
			if bp then  
				for _,t in ipairs(bp:GetChildren()) do  
					if t:IsA("Tool") and t.Name:lower():find("bat") then bat=t;break end  
				end  
			end  
			if bat then  
				-- Move directly to character without EquipTool to avoid state changes  
				bat.Parent=char  
				task.wait(0.03)  
			end  
		end  
  
		-- 4) Activate the bat  
		if bat then  
			pcall(function() bat:Activate() end)  
		end  
  
		task.wait(BAT_COUNTER_COOLDOWN)  
		batCounterDebounce=false  
	end)  
end  
  
-- ─── Bat counter setup ──────────────────────────────────────────────────────  
local _ragDetectCooldown = false  
  
local function _setupBatHitDetect(char)  
	for _,c in pairs(_batHitDetectConns) do pcall(function() c:Disconnect() end) end  
	_batHitDetectConns = {}  
	if not char then return end  
	local hum = char:FindFirstChildOfClass("Humanoid")  
	if not hum then return end  
	-- (placeholder: bat hit detect kept for bat counter state tracking)  
end  
  
local function _setupBatCounter(char)  
	for _,c in pairs(batCounterConns) do pcall(function() c:Disconnect() end) end  
	batCounterConns={}  
	if not char then return end  
	local hum=char:FindFirstChildOfClass("Humanoid")  
	if not hum then return end  
  
	-- Only listen to StateChanged — skip Motor6D listeners entirely  
	-- (Motor6D disabled fires way too often and causes re-entry loops)  
	table.insert(batCounterConns, hum.StateChanged:Connect(function(_,new)  
		if new==Enum.HumanoidStateType.Ragdoll  
		or new==Enum.HumanoidStateType.FallingDown  
		or new==Enum.HumanoidStateType.Physics then  
			if not batCounterEnabled then return end  
			task.delay(0.05, function()  
				if batCounterEnabled then _swingBatCounter() end  
			end)  
		end  
	end))  
end  
  
LP.CharacterAdded:Connect(function(char)  
	task.wait(0.5)  
	_ragDetectCooldown = false  
	_setupBatHitDetect(char)  
	if batCounterEnabled then _setupBatCounter(char) end  
end)  
if LP.Character then  
	_setupBatHitDetect(LP.Character)  
	_setupBatCounter(LP.Character)  
end  
  
setBatCounterVisual=function(enabled)  
	batCounterEnabled=enabled  
	if enabled then  
		_setupBatCounter(LP.Character)  
	else  
		for _,c in pairs(batCounterConns) do pcall(function() c:Disconnect() end) end  
		batCounterConns={}  
	end  
end  
  
-- ─── Envy Logic Additions ─────────────────────────────────────────────────────  
local getconnections = getconnections or get_signal_cons or getconnects or (syn and syn.get_signal_cons)  
  
  
  
local function doTpDown()  
	pcall(function()  
		local c=LP.Character; if not c then return end  
		local root=c:FindFirstChild("HumanoidRootPart"); if not root then return end  
		local rp=RaycastParams.new(); rp.FilterDescendantsInstances={c}; rp.FilterType=Enum.RaycastFilterType.Exclude  
		local res=workspace:Raycast(root.Position,Vector3.new(0,-1000,0),rp)  
		if res then root.CFrame=CFrame.new(res.Position+Vector3.new(0,root.Size.Y/2+0.5,0)); root.AssemblyLinearVelocity=Vector3.zero end  
	end)  
end  
  
local function faceSouth()  
	pcall(function() local c=LP.Character; if not c then return end; local root=c:FindFirstChild("HumanoidRootPart"); if root then root.CFrame=CFrame.new(root.Position)*CFrame.Angles(0,0,0) end end)  
end  
local function faceNorth()  
	pcall(function() local c=LP.Character; if not c then return end; local root=c:FindFirstChild("HumanoidRootPart"); if root then root.CFrame=CFrame.new(root.Position)*CFrame.Angles(0,math.rad(180),0) end end)  
end  
  
local function findAnyTool()  
	local c=LP.Character  
	if c then for _,v in ipairs(c:GetChildren()) do if v:IsA("Tool") then return v end end end  
	local bp=LP:FindFirstChildOfClass("Backpack")  
	if bp then for _,v in ipairs(bp:GetChildren()) do if v:IsA("Tool") then return v end end end  
	return nil  
end  
  
local SWING_COOLDOWN=0.08  
local function tryHitBat()  
	if _anyKeyListening then return end  
	pcall(function()  
		local c=LP.Character; if not c then return end  
		local hum2=c:FindFirstChildOfClass("Humanoid"); local tool=findAnyTool()  
		if tool then  
			if tool.Parent~=c and hum2 then pcall(function() hum2:EquipTool(tool) end) end  
			local remote=tool:FindFirstChildOfClass("RemoteEvent")  
			if remote then pcall(function() remote:FireServer() end)  
			else pcall(function() tool:Activate() end) end  
		end  
	end)  
end  
-- ─── End Envy Logic Additions ─────────────────────────────────────────────────  
  
LP.CharacterAdded:Connect(function(char)  
	lastHealth=100;task.wait(0.5)  
	setupSpeedIndicator(char)  
	if medusaCounterEnabled then setupMedusa(char) end  
	unwalkAnimations={}  
	if unwalkEnabled then task.wait(0.5);disableAnimations() end  
	-- Re-apply carry/lagger speed on respawn only if a mode is active  
	pcall(function()  
		local hum = char:FindFirstChildOfClass("Humanoid")  
		if hum then
			local _rspd
			if activeSpeedMode == "lagger" then _rspd = carryActive and LS2 or LS
			else _rspd = carryActive and CS or NS end
			hum.WalkSpeed = _rspd
		end
	end)  
	-- Reconnect features like Envy does  
	if autoBatEnabled then stopBatAimbot(); task.wait(0.2); pcall(startBatAimbot) end  
	if bodyLockEnabled then stopBodyLock(); task.wait(0.2); pcall(startBodyLock) end
	if batCounterEnabled then task.wait(0.3); _setupBatCounter(char) end  
	task.wait(0.3); _setupBatHitDetect(char)  
	if Steal.AutoStealEnabled then pcall(stopAutoSteal); task.wait(0.5); pcall(startAutoSteal) end  
	-- ── Void / death prevention ──────────────────────────────────────────  
	local lastSafePos = nil  
	local safeConn = RunService.Heartbeat:Connect(function()  
		local hrp = char:FindFirstChild("HumanoidRootPart"); if not hrp then return end  
		local hum = char:FindFirstChildOfClass("Humanoid"); if not hum then return end  
		if hrp.Position.Y > -50 and hum.Health > 0 then  
			lastSafePos = hrp.CFrame  
		end  
		if hrp.Position.Y < -80 and lastSafePos then  
			hrp.CFrame = lastSafePos + Vector3.new(0, 5, 0)  
			hrp.AssemblyLinearVelocity = Vector3.zero  
		end  
	end)  
	char.AncestryChanged:Connect(function()  
		if not char.Parent then safeConn:Disconnect() end  
	end)  
end)  
if LP.Character then setupSpeedIndicator(LP.Character) end  
  
-- Frame references exposed so saveConfig can read positions directly  
local _savedMainFrame, _savedMiniFrame, _savedPbFrame = nil, nil, nil  
  
local CONFIG_FILE = "italozinHub.json"  
local _isfile    = isfile   or (syn and syn.isfile)   or (getgenv and getgenv().isfile)   or function() return false end  
local _readfile  = readfile  or (syn and syn.readfile)  or (getgenv and getgenv().readfile)  or function() return nil end  
local _writefile = writefile or (syn and syn.writefile) or (getgenv and getgenv().writefile) or function() end  
  
local function saveConfig()  
	local function ks(e) return {kb=e.kb and e.kb.Name or nil,gp=e.gp and e.gp.Name or nil} end  
	local function cp(f) if f and f.Parent then return {sx=f.Position.X.Scale,ox=f.Position.X.Offset,sy=f.Position.Y.Scale,oy=f.Position.Y.Offset} end end  
	local mainPos = cp(_savedMainFrame)  
	local miniPos = cp(_savedMiniFrame)  
	local mbPos   = cp(mbContainer)  
	local pbPos   = cp(_savedPbFrame)
	-- save Panel 2 individual button positions
	local mb2Pos = nil
	if mb2Buttons and #mb2Buttons > 0 then
		mb2Pos = {}
		for i, entry in ipairs(mb2Buttons) do
			local p = cp(entry.fr)
			mb2Pos[i] = p
		end
	end
	local cfg = {  
		uiLocked=uiLocked,
		autoTPEnabled=autoTPEnabled,autoTPHeight=autoTPHeight,
		dropBrainrotKey=ks(KB.DropBrainrot),tpFloorKey=ks(KB.TPFloor),autoLeftKey=ks(KB.AutoLeft),autoRightKey=ks(KB.AutoRight),
		italozinResetKey=ks(KB.italozinReset),  
		autoBatKey=ks(KB.AutoBat),  
		guiHideKey=ks(KB.GuiHide),
		modeToggleKey=ks(KB.ModeToggle),
		speedToggleKey=ks(KB.SpeedToggle),
		normalSpeed=NS,carrySpeed=CS,laggerSpeed=LS,laggerCarrySpeed=LS2,
		activeSpeedMode=activeSpeedMode,carryActive=carryActive,
		grabRadius=Steal.StealRadius,grabRadiusNormal=Steal.StealRadiusNormal,grabRadiusLagger=Steal.StealRadiusLagger,stealDuration=Steal.StealDuration,  
		antiRagdoll=antiRagdollEnabled,autoStealEnabled=Steal.AutoStealEnabled,  
		infiniteJump=infJumpEnabled,medusaCounter=medusaCounterEnabled,  
		autoBat=autoBatEnabled,autoBatSpeed=autoBatSpeed,  
		batCounter=batCounterEnabled,
		ragdollTimer=ragdollTimerEnabled,  
		unwalkEnabled=unwalkEnabled,  
		stretchRez=stretchRezEnabled,  
		removeAccessories=removeAccessoriesEnabled,  
		nightMode=nightModeEnabled,  
		antiLag=antiLagEnabled,  
		autoLeft=autoLeftEnabled,autoRight=autoRightEnabled,  
		bodyLockEnabled=bodyLockEnabled,bodyLockRange=bodyLockRange,
		mainPos=mainPos,miniPos=miniPos,mbPos=mbPos,pbPos=pbPos,mb2Pos=mb2Pos,  
	}  
	pcall(function() _writefile(CONFIG_FILE, HS:JSONEncode(cfg)) end)  
end  
-- auto-save loop started after buildGui() below  
  
local setInstaGrab  
local setInfJumpVisual,setAntiRagVisual,setMedusaVisual  
local durationInput,normalBox,carryBox,radInput  
local laggerBox  
local grabRadiusBox, grabRadiusLaggerBox  
local _kbTPDown, _kbDrop, _kbAutoLeft, _kbAutoRight  
local KB_Speed  -- populated inside buildGui  
local setStretchRezVisual  
local setLaggerModeVisual  
local setRemoveAccsVisual  
local setNightModeVisual  
local setBatCtrVisual  -- exposed so loadConfig can restore bat counter state  
local updateModeButtons  -- exposed so loadConfig can refresh speed mode UI  
local switchMode, toggleCarry  -- exposed so mobile buttons / keybinds can use them  
  
-- ===== MURDER HUB STYLE COLORS =====  
local C = {  
	bg       = Color3.fromRGB(20, 5, 15),  
	glass    = Color3.fromRGB(28, 8, 22),  
	accent   = Color3.fromRGB(40, 10, 30),  
	accentLight = Color3.fromRGB(230, 80, 160),  
	accentDark  = Color3.fromRGB(28, 8, 22),  
	text     = Color3.fromRGB(255, 200, 230),  
	textDim  = Color3.fromRGB(180, 100, 145),  
	success  = Color3.fromRGB(55, 14, 40),  
	danger   = Color3.fromRGB(45, 12, 32),  
	row      = Color3.fromRGB(32, 8, 22),  
	rowHov   = Color3.fromRGB(55, 14, 40),  
	border   = Color3.fromRGB(80, 25, 55),  
	on       = Color3.fromRGB(230, 80, 160),  
	off      = Color3.fromRGB(45, 12, 32),  
	white    = Color3.fromRGB(255, 200, 230),  
}  
  
local _resetAllQuickBtns = nil  -- set inside buildGui  
local miscSetVisuals = {}  -- populated inside buildGui, used by loadConfig  
local mb2Buttons = nil  -- set inside buildGui, used by Button Size setting  
local mbContainer = nil -- set inside buildGui, used by Button Size setting  
  
local function loadConfig()  
	local hasFile = false; pcall(function() hasFile = _isfile(CONFIG_FILE) end)  
	if not hasFile then return end  
	local raw; pcall(function() raw = _readfile(CONFIG_FILE) end)  
	if not raw then return end  
	local ok,cfg = pcall(function() return HS:JSONDecode(raw) end)  
	if not ok or not cfg then return end  
  
	local function lk(entryName, data)  
		local entry=KB[entryName]; if not data then return end  
		if type(data)=="table" then  
			if data.kb and Enum.KeyCode[data.kb] then entry.kb=Enum.KeyCode[data.kb] end  
			if data.gp and Enum.KeyCode[data.gp] then entry.gp=Enum.KeyCode[data.gp] end  
		end  
	end  
  
	-- speeds  
	if cfg.normalSpeed       then NS =cfg.normalSpeed;       if _speedBoxNS  then _speedBoxNS.Text=tostring(NS)   end end
	if cfg.carrySpeed        then CS =cfg.carrySpeed;        if _speedBoxCS  then _speedBoxCS.Text=tostring(CS)   end end
	if cfg.laggerSpeed       then LS =cfg.laggerSpeed;       if _speedBoxLS  then _speedBoxLS.Text=tostring(LS)   end end
	if cfg.laggerCarrySpeed  then LS2=cfg.laggerCarrySpeed;  if _speedBoxLS2 then _speedBoxLS2.Text=tostring(LS2) end end
	if cfg.activeSpeedMode   then activeSpeedMode=cfg.activeSpeedMode end
	if cfg.carryActive~=nil  then carryActive=cfg.carryActive end
	-- sync legacy bool aliases from restored state
	laggerMode  = (activeSpeedMode == "lagger")
	speedMode   = carryActive
	laggerPhase = carryActive and 2 or 1
  
	-- grab radius (separate Normal / Lagger values)  
	if cfg.grabRadiusNormal then Steal.StealRadiusNormal=cfg.grabRadiusNormal end  
	if cfg.grabRadiusLagger then Steal.StealRadiusLagger=cfg.grabRadiusLagger end  
	-- backwards-compat: old configs only had a single "grabRadius" value  
	if cfg.grabRadius and not cfg.grabRadiusNormal and not cfg.grabRadiusLagger then  
		Steal.StealRadiusNormal=cfg.grabRadius  
		Steal.StealRadiusLagger=cfg.grabRadius  
	end  
	syncStealRadius()  
	if grabRadiusBox       then grabRadiusBox.Text=tostring(Steal.StealRadiusNormal) end  
	if grabRadiusLaggerBox then grabRadiusLaggerBox.Text=tostring(Steal.StealRadiusLagger) end  
  
	-- keybinds (KB table)  
	lk("DropBrainrot",cfg.dropBrainrotKey); lk("TPFloor",cfg.tpFloorKey)
	if cfg.autoTPHeight then
		autoTPHeight=cfg.autoTPHeight
		if _setAutoTPHeightBox then _setAutoTPHeightBox(autoTPHeight) end
	end
	if cfg.autoTPEnabled then
		autoTPEnabled=true
		task.defer(function()
			if setAutoTPVisual then setAutoTPVisual(true) end
			startAutoTP()
		end)
	end  
	lk("AutoLeft",cfg.autoLeftKey);         lk("AutoRight",cfg.autoRightKey)  
	lk("AutoBat",cfg.autoBatKey);           lk("GuiHide",cfg.guiHideKey)  
	-- sync batKey via _G so it works even before buildGui runs
	if not _G.__BH_batKey then _G.__BH_batKey={} end
	_G.__BH_batKey.kb  = KB.AutoBat.kb
	_G.__BH_batKey.gp  = KB.AutoBat.gp
	_G.__BH_batKey.key = KB.AutoBat.gp or KB.AutoBat.kb or Enum.KeyCode.E
	if _G.__BH_batKeyBox then
		local _k = KB.AutoBat.gp or KB.AutoBat.kb
		if _k then _G.__BH_batKeyBox.Text = _k.Name end
	end  
	lk("SpeedToggle",cfg.speedToggleKey)
	if KB.GuiHide.kb and KB.GuiHide._setBox then KB.GuiHide._setBox(KB.GuiHide.kb) end
  
	-- sync move-tab keyEntry objects and their UI boxes  
	if _kbTPDown    then _kbTPDown.key    = KB.TPFloor.kb      or _kbTPDown.key;    if _kbTPDown.setBox    then _kbTPDown.setBox(_kbTPDown.key)       end end  
	if cfg.italozinResetKey then
		if cfg.italozinResetKey.kb and Enum.KeyCode[cfg.italozinResetKey.kb] then KB.italozinReset.kb=Enum.KeyCode[cfg.italozinResetKey.kb] end
		if cfg.italozinResetKey.gp and Enum.KeyCode[cfg.italozinResetKey.gp] then KB.italozinReset.gp=Enum.KeyCode[cfg.italozinResetKey.gp] end
		if KB.italozinReset._setBox then
			KB.italozinReset._setBox((KB.italozinReset.gp and KB.italozinReset.gp.Name) or (KB.italozinReset.kb and KB.italozinReset.kb.Name) or "R")
		end
	end
	if _kbDrop      then _kbDrop.key      = KB.DropBrainrot.kb or _kbDrop.key;      if _kbDrop.setBox      then _kbDrop.setBox(_kbDrop.key)           end end  
	if _kbAutoLeft  then _kbAutoLeft.kb=KB.AutoLeft.kb; _kbAutoLeft.gp=KB.AutoLeft.gp; _kbAutoLeft.key=KB.AutoLeft.gp or KB.AutoLeft.kb or _kbAutoLeft.key; if _kbAutoLeft.setBox  then _kbAutoLeft.setBox(_kbAutoLeft.key)   end end  
	if _kbAutoRight then _kbAutoRight.kb=KB.AutoRight.kb; _kbAutoRight.gp=KB.AutoRight.gp; _kbAutoRight.key=KB.AutoRight.gp or KB.AutoRight.kb or _kbAutoRight.key; if _kbAutoRight.setBox then _kbAutoRight.setBox(_kbAutoRight.key) end end  
	-- refresh KB_Speed box display  
	if KB_Speed then  

		local function _lkSpeed(field, kbKey, kbEntryName)
			local data = cfg[field]; if not data or type(data)~="table" then return end
			local newKey = nil
			if data.gp and Enum.KeyCode[data.gp] then
				KB_Speed[kbKey].gp=Enum.KeyCode[data.gp]; KB_Speed[kbKey].kb=nil; newKey=KB_Speed[kbKey].gp
			elseif data.kb and Enum.KeyCode[data.kb] then
				KB_Speed[kbKey].kb=Enum.KeyCode[data.kb]; KB_Speed[kbKey].gp=nil; newKey=KB_Speed[kbKey].kb
			end
			if newKey then KB_Speed[kbKey].key=newKey; if KB_Speed[kbKey].setBox then KB_Speed[kbKey].setBox(newKey) end end
			if kbEntryName and KB[kbEntryName] then KB[kbEntryName].kb=KB_Speed[kbKey].kb; KB[kbEntryName].gp=KB_Speed[kbKey].gp end
		end
		_lkSpeed("speedToggleKey","SpeedToggle","SpeedToggle")
		-- also sync KB.SpeedToggle so the settings makeKbRow shows correct value
		if KB and KB.SpeedToggle then KB.SpeedToggle.kb=KB_Speed.SpeedToggle.kb; KB.SpeedToggle.gp=KB_Speed.SpeedToggle.gp end
		_lkSpeed("modeToggleKey", "ModeToggle", "ModeToggle")
	end
  
	-- toggles  
	if cfg.antiRagdoll then  
		antiRagdollEnabled=true  
		if miscSetVisuals and miscSetVisuals["Anti Ragdoll"] then miscSetVisuals["Anti Ragdoll"](true) end  
		startAntiRagdoll()  
	end  
	if cfg.autoStealEnabled then  
		Steal.AutoStealEnabled=true  
		if miscSetVisuals and miscSetVisuals["Auto Steal"] then miscSetVisuals["Auto Steal"](true) end  
		pcall(startAutoSteal)  
	end  
	if cfg.infiniteJump then  
		infJumpEnabled=true  
		if miscSetVisuals and miscSetVisuals["Infinite Jump"] then miscSetVisuals["Infinite Jump"](true) end  
		startInfiniteJump()  
	end  
	if cfg.medusaCounter then  
		medusaCounterEnabled=true  
		if miscSetVisuals and miscSetVisuals["Medusa Counter"] then miscSetVisuals["Medusa Counter"](true) end  
		setupMedusa(LP.Character)  
	end  
	if cfg.autoBat then  
		autoBatEnabled=true  
		if autoBatSetVisual then autoBatSetVisual(true) end  
		startBatAimbot()  
	end  
	if cfg.autoBatSpeed then autoBatSpeed = cfg.autoBatSpeed
		if _G.__BH_autoBatSpeedBox then _G.__BH_autoBatSpeedBox.Text = tostring(autoBatSpeed) end
	end  
	if cfg.batCounter then
		batCounterEnabled=true
		if miscSetVisuals and miscSetVisuals["Bat Counter"] then miscSetVisuals["Bat Counter"](true) end
		pcall(function() _setupBatCounter(LP.Character) end)
	end
	if cfg.ragdollTimer then
		ragdollTimerEnabled=true
		if miscSetVisuals and miscSetVisuals["Ragdoll Timer"] then miscSetVisuals["Ragdoll Timer"](true) end
	end
	if cfg.unwalkEnabled then  
		unwalkEnabled=true  
		if miscSetVisuals and miscSetVisuals["Unwalk"] then miscSetVisuals["Unwalk"](true) end  
		task.spawn(function() task.wait(0.5); disableAnimations() end)  
	end  
	if cfg.stretchRez then  
		stretchRezEnabled=true  
		if miscSetVisuals and miscSetVisuals["__stretchRez"] then miscSetVisuals["__stretchRez"](true) end  
		enableStretchRez()  
	end  
	if cfg.removeAccessories then  
		removeAccessoriesEnabled=true  
		if miscSetVisuals and miscSetVisuals["__removeAccs"] then miscSetVisuals["__removeAccs"](true) end  
		enableRemoveAccessories()  
	end  
	if cfg.nightMode then  
		nightModeEnabled=true  
		if miscSetVisuals and miscSetVisuals["__italozinfy"] then miscSetVisuals["__italozinfy"](true) end  
		applyNightMode(true)  
	end  
	if cfg.antiLag then  
		antiLagEnabled=true  
		if miscSetVisuals and miscSetVisuals["__antiLag"] then miscSetVisuals["__antiLag"](true) end  
		enableAntiLag()  
	end  
	if cfg.autoLeft then  
		autoLeftEnabled=true  
		if autoLeftSetVisual then autoLeftSetVisual(true) end  
		pcall(startAutoLeft)  
	end  
	if cfg.autoRight then  
		autoRightEnabled=true  
		if autoRightSetVisual then autoRightSetVisual(true) end  
		pcall(startAutoRight)  
	end  
	-- activeSpeedMode and carryActive already restored above
	-- sync legacy aliases so old references work
	laggerMode  = (activeSpeedMode == "lagger")
	speedMode   = carryActive
	laggerPhase = carryActive and 2 or 1
	if updateModeLabel then updateModeLabel() end
	if updateModeButtons then updateModeButtons() end
	-- restore uiLocked
	if cfg.uiLocked then
		uiLocked = true
		task.defer(function() if setLockUIVisual then setLockUIVisual(true) end end)
	end
	-- restore body lock
	if cfg.bodyLockRange then bodyLockRange = cfg.bodyLockRange end
	if cfg.bodyLockEnabled then
		bodyLockEnabled = true
		task.defer(function()
			if miscSetVisuals and miscSetVisuals["Body Lock"] then miscSetVisuals["Body Lock"](true) end
			startBodyLock()
		end)
	end

	-- restore saved frame positions (after UI is fully built)
	task.spawn(function()
		task.wait(0.5)
		local function lp(frame, d)
			if frame and type(d)=="table" and d.sx~=nil then
				frame.Position = UDim2.new(d.sx, d.ox, d.sy, d.oy)
			end
		end
		lp(_savedMainFrame, cfg.mainPos)
		lp(_savedMiniFrame, cfg.miniPos)
		lp(mbContainer,     cfg.mbPos)
		lp(_savedPbFrame,   cfg.pbPos)
		-- restore Panel 2 individual button positions
		if cfg.mb2Pos and mb2Buttons then
			for i, d in ipairs(cfg.mb2Pos) do
				local entry = mb2Buttons[i]
				if entry and entry.fr then lp(entry.fr, d) end
			end
		end
	end)
end  

	local function buildGui()  
local TweenService = TS  
local UserInputService = UIS  
local PG = LP:WaitForChild("PlayerGui")  
  
-- UI state  
local liveChatEnabled = true  
local menuToggleKey = nil  
local savedConfigs = {}  
local listeningForKey = false  
local activeTab = "Settings"  
  
-- ===== italozin HUB COLORS — F897AF theme =====  
-- Primary accent: #F897AF  (248, 151, 175)  
-- Outline/stroke uses the same pink  
local PINK     = Color3.fromRGB(248, 151, 175)   -- #F897AF  main accent  
local BG       = Color3.fromRGB(18,  5,  12)     -- very dark rose-black  
local BG2      = Color3.fromRGB(26,  8,  18)     -- topbar / footer tint  
local CARD     = Color3.fromRGB(34,  10, 24)     -- card background  
local BORDER   = Color3.fromRGB(248, 151, 175)   -- outline pink (same as accent)  
local TEXT     = Color3.fromRGB(255, 220, 232)   -- light pink-white  
local TEXTDIM  = Color3.fromRGB(200, 140, 165)   -- dimmed tab labels  
local PURPLE   = Color3.fromRGB(248, 151, 175)   -- scrollbar (reuse accent)  
local WHITE    = Color3.fromRGB(255, 235, 242)   -- near-white for active text  
  

	-- ── Message 22 exact controller helpers (inside buildGui) ──────────────────
	local GAMEPAD_KEYS={
		[Enum.KeyCode.ButtonA]=true,[Enum.KeyCode.ButtonB]=true,[Enum.KeyCode.ButtonX]=true,[Enum.KeyCode.ButtonY]=true,
		[Enum.KeyCode.ButtonL1]=true,[Enum.KeyCode.ButtonR1]=true,[Enum.KeyCode.ButtonL2]=true,[Enum.KeyCode.ButtonR2]=true,
		[Enum.KeyCode.ButtonL3]=true,[Enum.KeyCode.ButtonR3]=true,[Enum.KeyCode.ButtonStart]=true,[Enum.KeyCode.ButtonSelect]=true,
		[Enum.KeyCode.DPadUp]=true,[Enum.KeyCode.DPadDown]=true,[Enum.KeyCode.DPadLeft]=true,[Enum.KeyCode.DPadRight]=true
	}
	local function isGamepadInput(inp) return inp and inp.UserInputType and inp.UserInputType.Name:match("^Gamepad")~=nil end
	local function isBindableInput(inp)
		if not inp or inp.KeyCode==Enum.KeyCode.Unknown then return false end
		if inp.UserInputType==Enum.UserInputType.Keyboard then return true end
		return isGamepadInput(inp) and GAMEPAD_KEYS[inp.KeyCode]==true
	end
	local function kbMatch(entry,kc) return kc and (kc==entry.kb or (entry.gp and kc==entry.gp)) end
	-- ── End message 22 helpers ──────────────────────────────────────────────────

-- ScreenGui  
local sg = Instance.new("ScreenGui")  
sg.Name = "italozinUI"  
sg.ResetOnSpawn = false  
sg.ZIndexBehavior = Enum.ZIndexBehavior.Sibling  
sg.DisplayOrder = 20  
sg.IgnoreGuiInset = true  
pcall(function() if syn and syn.protect_gui then syn.protect_gui(sg) end end)  
if not pcall(function() sg.Parent = game:GetService("CoreGui") end) then sg.Parent = PG end  
  
-- Main frame — italozinHub shape (255×420, corner 20)  
local W, H, CORNER = 300, 460, 20  
local main = Instance.new("Frame", sg)  
_savedMainFrame = main  
main.Size = UDim2.new(0, W, 0, H)  
main.Position = UDim2.new(0.5, -W/2, 0.5, -H/2)  
main.BackgroundColor3 = BG  
main.BorderSizePixel = 0  
main.ClipsDescendants = true  
Instance.new("UICorner", main).CornerRadius = UDim.new(0, CORNER)  
  
local mainStroke = Instance.new("UIStroke", main)  
mainStroke.Color = BORDER  -- outline pink  
mainStroke.Thickness = 1.5  
  
-- Drag (topbar is the handle — set up after topbar is created below)  
local _mainDragging, _mainDragInput, _mainDragStart, _mainStartPos = false, nil, nil, nil  
  
-- Topbar — italozinHub style (rounded top corners only)  
local topbar = Instance.new("Frame", main)  
topbar.Size = UDim2.new(1, 0, 0, 44)  
topbar.BackgroundColor3 = BG2  
topbar.ZIndex = 5  
Instance.new("UICorner", topbar).CornerRadius = UDim.new(0, CORNER)  
-- patch bottom corners of topbar square  
local topPatch = Instance.new("Frame", topbar)  
topPatch.Size = UDim2.new(1, 0, 0, CORNER)  
topPatch.Position = UDim2.new(0, 0, 1, -CORNER)  
topPatch.BackgroundColor3 = BG2  
topPatch.BorderSizePixel = 0  
topPatch.ZIndex = 4  
local topStroke = Instance.new("UIStroke", topbar)  
topStroke.Color = BORDER; topStroke.Thickness = 1.5  
topStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border  
  
-- Centered "italozin HUB" title (italozinHub style)  
local titleLbl = Instance.new("TextLabel", topbar)  
titleLbl.Size = UDim2.new(1, -56, 1, 0)  
titleLbl.Position = UDim2.new(0, 10, 0, 0)  
titleLbl.BackgroundTransparency = 1  
titleLbl.Text = "italozin HUB"  
titleLbl.TextColor3 = WHITE  
titleLbl.Font = Enum.Font.GothamBlack  
titleLbl.TextSize = 15  
titleLbl.TextXAlignment = Enum.TextXAlignment.Center  
titleLbl.ZIndex = 12  
  
-- Divider under topbar  
local topDiv = Instance.new("Frame", main)  
topDiv.Size = UDim2.new(1, 0, 0, 1)  
topDiv.Position = UDim2.new(0, 0, 0, 44)  
topDiv.BackgroundColor3 = BORDER  
topDiv.BorderSizePixel = 0  
topDiv.ZIndex = 11  
  
-- Wire main GUI drag via topbar (supports mouse + touch, UIS pattern)  
topbar.InputBegan:Connect(function(inp)  
	if uiLocked then return end  
	if inp.UserInputType == Enum.UserInputType.MouseButton1  
	or inp.UserInputType == Enum.UserInputType.Touch then  
		_mainDragging = true; _mainDragStart = inp.Position; _mainStartPos = main.Position  
		inp.Changed:Connect(function()
			if inp.UserInputState == Enum.UserInputState.End then
				_mainDragging = false
				task.defer(function() pcall(saveConfig) end)
			end
		end)  
	end  
end)  
topbar.InputChanged:Connect(function(inp)  
	if uiLocked then _mainDragging = false; return end  
	if inp.UserInputType == Enum.UserInputType.MouseMovement  
	or inp.UserInputType == Enum.UserInputType.Touch then  
		_mainDragInput = inp  
	end  
end)  
UserInputService.InputChanged:Connect(function(inp)  
	if uiLocked then _mainDragging = false; return end  
	if inp == _mainDragInput and _mainDragging then  
		local d = inp.Position - _mainDragStart  
		main.Position = UDim2.new(_mainStartPos.X.Scale, _mainStartPos.X.Offset + d.X, _mainStartPos.Y.Scale, _mainStartPos.Y.Offset + d.Y)  
	end  
end)  
  
-- Tab bar — italozinHub style (flush below topbar+divider, no corners)  
local tabBar = Instance.new("Frame", main)  
tabBar.Size = UDim2.new(1, 0, 0, 34)  
tabBar.Position = UDim2.new(0, 0, 0, 45)  
tabBar.BackgroundColor3 = BG2  
tabBar.BorderSizePixel = 0  
local tabStroke = Instance.new("UIStroke", tabBar)  
tabStroke.Color = BORDER; tabStroke.Thickness = 1  
tabStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border  
  
-- Divider below tab bar  
local tabDiv = Instance.new("Frame", main)  
tabDiv.Size = UDim2.new(1, 0, 0, 1)  
tabDiv.Position = UDim2.new(0, 0, 0, 79)  
tabDiv.BackgroundColor3 = BORDER  
tabDiv.BorderSizePixel = 0  
tabDiv.ZIndex = 9  
  
-- UIGridLayout to lay tabs out evenly across the full bar width  
local tabGrid = Instance.new("UIGridLayout", tabBar)  
tabGrid.CellSize = UDim2.new(1/6, 0, 1, 0)  
tabGrid.CellPadding = UDim2.new(0, 0, 0, 0)  
tabGrid.SortOrder = Enum.SortOrder.LayoutOrder  
tabGrid.FillDirectionMaxCells = 6  
  
local TABS = {"Speed", "Combat", "Misc", "Move", "Performance", "Settings"}  
local tabBtns = {}  
local tabUnderlines = {}  
local pages = {}  
local tabCount = #TABS  
  
-- Use UDim-based equal-width tabs that always fill the bar  
for i, t in ipairs(TABS) do  
	local tbtn = Instance.new("TextButton", tabBar)  
	tbtn.Size = UDim2.new(1/tabCount, 0, 1, 0)  
	tbtn.BackgroundTransparency = 1  
	tbtn.Text = ""; tbtn.LayoutOrder = i; tbtn.ZIndex = 9  
	local lbl = Instance.new("TextLabel", tbtn)  
	lbl.Size = UDim2.new(1, 0, 1, -3)  
	lbl.BackgroundTransparency = 1  
	lbl.Text = t  
	lbl.TextColor3 = (t == "Settings") and WHITE or TEXTDIM  
	lbl.Font = Enum.Font.GothamBold; lbl.TextSize = 9  
	lbl.TextXAlignment = Enum.TextXAlignment.Center; lbl.ZIndex = 10  
	-- Active underline (F897AF pink)  
	local ul = Instance.new("Frame", tbtn)  
	ul.Size = UDim2.new(0.55, 0, 0, 2); ul.Position = UDim2.new(0.225, 0, 1, -2)  
	ul.BackgroundColor3 = PINK; ul.BorderSizePixel = 0; ul.ZIndex = 11  
	ul.BackgroundTransparency = (t == "Settings") and 0 or 1  
	Instance.new("UICorner", ul).CornerRadius = UDim.new(1, 0)  
	-- Separator line after each tab except last  
	if i < tabCount then  
		local sep = Instance.new("Frame", tbtn)  
		sep.Size = UDim2.new(0, 1, 0, 14)  
		sep.Position = UDim2.new(1, -1, 0.5, -7)  
		sep.BackgroundColor3 = Color3.fromRGB(200, 120, 145)  
		sep.BorderSizePixel = 0; sep.ZIndex = 10  
	end  
	tabBtns[t] = tbtn; tabUnderlines[t] = ul  
end  
  
  
-- Body area (below tab bar divider at 80px)  
local body = Instance.new("Frame", main)  
body.Size = UDim2.new(1, 0, 1, -116)  
body.Position = UDim2.new(0, 0, 0, 80)  
body.BackgroundTransparency = 1  
body.BorderSizePixel = 0  
  
-- Page system - each tab gets a left and right frame  
local pageFrames = {}  -- pageFrames[name] = {L=frame, R=frame}  
local pageLOs = {}  
  
local function makePage(name)  
	-- stub: tab pages use scrollPages, not leftCol/rightCol  
	pageFrames[name] = {L = nil, R = nil}  
	pageLOs[name] = {L = 0, R = 0}  
end  
  
local function pgL(name) return pageFrames[name] and pageFrames[name].L end  
local function pgR(name) return pageFrames[name] and pageFrames[name].R end  
local function loL(name) pageLOs[name].L = pageLOs[name].L + 1; return pageLOs[name].L end  
local function loR(name) pageLOs[name].R = pageLOs[name].R + 1; return pageLOs[name].R end  
  
-- Update switchTab to show/hide both columns  
local scrollPages = {}  -- filled after scroll frames are created  
local function switchTab(name)  
	activeTab = name  
	for _, t in ipairs(TABS) do  
		local tbtn = tabBtns[t]  
		local ul   = tabUnderlines[t]  
		local lbl2 = tbtn and tbtn:FindFirstChildOfClass("TextLabel")  
		if t == name then  
			if lbl2 then TS:Create(lbl2, TweenInfo.new(0.12), {TextColor3=WHITE}):Play() end  
			if ul then ul.BackgroundTransparency = 0 end  
		else  
			if lbl2 then TS:Create(lbl2, TweenInfo.new(0.12), {TextColor3=TEXTDIM}):Play() end  
			if ul then ul.BackgroundTransparency = 1 end  
		end  
	end  
	-- show/hide scroll pages  
	if scrollPages then  
		for n, sp in pairs(scrollPages) do  
			sp.Visible = (n == name)  
		end  
	end  
end  
  
-- Wire tab clicks now that switchTab is defined  
for _, t in ipairs(TABS) do  
	local tbtn = tabBtns[t]  
	if tbtn then tbtn.MouseButton1Click:Connect(function() switchTab(t) end) end  
end  
  
-- Row helper — plain row with bottom divider line (like right UI)  
local function makeRow(parent, lo_val, h)  
	local f = Instance.new("Frame", parent)  
	f.Size = UDim2.new(1, 0, 0, h or 38)  
	f.BackgroundColor3 = CARD  
	f.BorderSizePixel = 0  
	f.LayoutOrder = lo_val  
	Instance.new("UICorner", f).CornerRadius = UDim.new(0, 6)  
	Instance.new("UIStroke", f).Color = BORDER  
	return f  
end  
  
local function makeLabel(parent, txt)  
	local l = Instance.new("TextLabel", parent)  
	l.Size = UDim2.new(0.6, 0, 1, 0)  
	l.Position = UDim2.new(0, 10, 0, 0)  
	l.BackgroundTransparency = 1  
	l.Text = txt  
	l.TextColor3 = TEXT  
	l.Font = Enum.Font.Gotham  
	l.TextSize = 12  
	l.TextXAlignment = Enum.TextXAlignment.Left  
	return l  
end  
  
local function makeSecHdr(parent, txt, lo)  
	local f = Instance.new("Frame", parent)  
	f.Size = UDim2.new(1, 0, 0, 18)  
	f.BackgroundTransparency = 1  
	f.BorderSizePixel = 0  
	f.LayoutOrder = lo  
	local t = Instance.new("TextLabel", f)  
	t.Size = UDim2.new(1, 0, 1, 0)  
	t.BackgroundTransparency = 1  
	t.Text = txt:upper()  
	t.TextColor3 = WHITE  
	t.Font = Enum.Font.GothamBold  
	t.TextSize = 8  
	t.TextXAlignment = Enum.TextXAlignment.Left  
	local line = Instance.new("Frame", f)  
	line.Size = UDim2.new(1, 0, 0, 1)  
	line.Position = UDim2.new(0, 0, 1, -1)  
	line.BackgroundColor3 = BORDER  
	line.BorderSizePixel = 0  
end  
  
-- KB_Speed defined here so rows can reference it  
KB_Speed = {
	SpeedToggle = {key=Enum.KeyCode.Q, kb=Enum.KeyCode.Q, gp=nil},
	ModeToggle  = {key=Enum.KeyCode.N, kb=Enum.KeyCode.N, gp=nil},
}
  
do -- Settings + Speed page locals  
-- ===== SETTINGS PAGE =====  
local settingsPage = makePage("Settings")  
local settingsPage2 = Instance.new("ScrollingFrame", body)  
settingsPage2.Size = UDim2.new(1, 0, 1, 0); settingsPage2.Position = UDim2.new(0,0,0,0); settingsPage2.Visible = true; scrollPages["Settings"] = settingsPage2  
settingsPage2.BackgroundTransparency = 1  
settingsPage2.BorderSizePixel = 0  
settingsPage2.ScrollBarThickness = 3  
settingsPage2.ScrollBarImageColor3 = PURPLE  
settingsPage2.CanvasSize = UDim2.new(0, 0, 0, 0)  
settingsPage2.AutomaticCanvasSize = Enum.AutomaticSize.Y  
local stLayout = Instance.new("UIListLayout", settingsPage2)  
stLayout.Padding = UDim.new(0, 6)  
stLayout.SortOrder = Enum.SortOrder.LayoutOrder  
local stPad = Instance.new("UIPadding", settingsPage2)  
stPad.PaddingTop = UDim.new(0, 10)  
stPad.PaddingLeft = UDim.new(0, 10)  
stPad.PaddingRight = UDim.new(0, 10)  
  
makeSecHdr(settingsPage2, "Interface", 1)  
  
-- Hide / Show GUI keybind row  
do  
	local row = Instance.new("Frame", settingsPage2)  
	row.Size = UDim2.new(1, 0, 0, 38)  
	row.BackgroundColor3 = CARD  
	row.BorderSizePixel = 0  
	row.LayoutOrder = 2  
	Instance.new("UICorner", row).CornerRadius = UDim.new(0, 8)
	Instance.new("UIStroke", row).Color = BORDER  
	local lbl = Instance.new("TextLabel", row)  
	lbl.Size = UDim2.new(0.6, 0, 1, 0)  
	lbl.Position = UDim2.new(0, 10, 0, 0)  
	lbl.BackgroundTransparency = 1  
	lbl.Text = "Hide / Show GUI"  
	lbl.TextColor3 = TEXT  
	lbl.Font = Enum.Font.GothamBold  
	lbl.TextSize = 11  
	lbl.TextXAlignment = Enum.TextXAlignment.Left  
	local box = Instance.new("TextButton", row)  
	box.Size = UDim2.new(0, 36, 0, 26)  
	box.Position = UDim2.new(1, -46, 0.5, -13)  
	box.BackgroundColor3 = Color3.fromRGB(10, 10, 10)  
	box.BorderSizePixel = 0  
	box.Text = KB.GuiHide.kb and KB.GuiHide.kb.Name or "LC"  
	box.TextColor3 = WHITE  
	box.Font = Enum.Font.GothamBold  
	box.TextSize = 9  
	box.ZIndex = 5  
	Instance.new("UICorner", box).CornerRadius = UDim.new(0, 5)  
	Instance.new("UIStroke", box).Color = BORDER  
	local listening = false; local kc2  
	box.MouseButton1Click:Connect(function()  
		if listening then return end  
		listening = true; box.Text = "..."; box.TextColor3 = PINK  
		if kc2 then kc2:Disconnect() end  
		kc2 = UserInputService.InputBegan:Connect(function(inp)  
			if not isBindableInput(inp) then return end  
			if inp.UserInputType==Enum.UserInputType.Keyboard and UIS:GetFocusedTextBox() then return end  
			if true then  
				if inp.KeyCode == Enum.KeyCode.Escape then  
					listening = false; box.Text = KB.GuiHide.kb and KB.GuiHide.kb.Name or "LC"; box.TextColor3 = WHITE  
					if kc2 then kc2:Disconnect(); kc2 = nil end; return  
				end  
				if isGamepadInput(inp) then KB.GuiHide.gp=inp.KeyCode;KB.GuiHide.kb=nil else KB.GuiHide.kb=inp.KeyCode;KB.GuiHide.gp=nil end  
				box.Text = inp.KeyCode.Name; box.TextColor3 = WHITE  
				listening = false; if kc2 then kc2:Disconnect(); kc2 = nil end  
				saveConfig()  
			end  
		end)  
	end)  
	-- expose box so loadConfig can refresh it  
	KB.GuiHide._setBox = function(kc) box.Text = kc.Name end  
end  
  
-- GUI Size row  
do  
	local BASE_W, BASE_H = 300, 460  
	local guiScale = 1.0  -- current scale factor  
  
	local row = Instance.new("Frame", settingsPage2)  
	row.Size = UDim2.new(1, 0, 0, 38)  
	row.BackgroundColor3 = CARD  
	row.BorderSizePixel = 0  
	row.LayoutOrder = 3  
	Instance.new("UICorner", row).CornerRadius = UDim.new(0, 6)  
	Instance.new("UIStroke", row).Color = BORDER  
  
	local lbl = Instance.new("TextLabel", row)  
	lbl.Size = UDim2.new(0.45, 0, 1, 0)  
	lbl.Position = UDim2.new(0, 10, 0, 0)  
	lbl.BackgroundTransparency = 1  
	lbl.Text = "GUI Size"  
	lbl.TextColor3 = TEXT  
	lbl.Font = Enum.Font.GothamBold  
	lbl.TextSize = 11  
	lbl.TextXAlignment = Enum.TextXAlignment.Left  
  
	-- Size display label (shows percentage)  
	local sizeLbl = Instance.new("TextLabel", row)  
	sizeLbl.Size = UDim2.new(0, 36, 0, 24)  
	sizeLbl.Position = UDim2.new(1, -90, 0.5, -12)  
	sizeLbl.BackgroundTransparency = 1  
	sizeLbl.Text = "100%"  
	sizeLbl.TextColor3 = PINK  
	sizeLbl.Font = Enum.Font.GothamBold  
	sizeLbl.TextSize = 10  
	sizeLbl.TextXAlignment = Enum.TextXAlignment.Center  
  
	local function applyScale(s)  
		guiScale = math.clamp(s, 0.6, 1.5)  
		local newW = math.floor(BASE_W * guiScale)  
		local newH = math.floor(BASE_H * guiScale)  
		main.Size = UDim2.new(0, newW, 0, newH)  
		-- Keep centred  
		main.Position = UDim2.new(0.5, -newW/2, 0.5, -newH/2)  
		sizeLbl.Text = math.floor(guiScale * 100) .. "%"  
	end  
  
	-- Minus button  
	local minusBtn = Instance.new("TextButton", row)  
	minusBtn.Size = UDim2.new(0, 24, 0, 24)  
	minusBtn.Position = UDim2.new(1, -118, 0.5, -12)  
	minusBtn.BackgroundColor3 = Color3.fromRGB(35, 10, 22)  
	minusBtn.BorderSizePixel = 0  
	minusBtn.Text = "−"  
	minusBtn.TextColor3 = WHITE  
	minusBtn.Font = Enum.Font.GothamBlack  
	minusBtn.TextSize = 14  
	minusBtn.ZIndex = 5  
	Instance.new("UICorner", minusBtn).CornerRadius = UDim.new(0, 5)  
	Instance.new("UIStroke", minusBtn).Color = BORDER  
	minusBtn.MouseButton1Click:Connect(function()  
		applyScale(guiScale - 0.1)  
	end)  
  
	-- Plus button  
	local plusBtn = Instance.new("TextButton", row)  
	plusBtn.Size = UDim2.new(0, 24, 0, 24)  
	plusBtn.Position = UDim2.new(1, -50, 0.5, -12)  
	plusBtn.BackgroundColor3 = Color3.fromRGB(35, 10, 22)  
	plusBtn.BorderSizePixel = 0  
	plusBtn.Text = "+"  
	plusBtn.TextColor3 = WHITE  
	plusBtn.Font = Enum.Font.GothamBlack  
	plusBtn.TextSize = 14  
	plusBtn.ZIndex = 5  
	Instance.new("UICorner", plusBtn).CornerRadius = UDim.new(0, 5)  
	Instance.new("UIStroke", plusBtn).Color = BORDER  
	plusBtn.MouseButton1Click:Connect(function()  
		applyScale(guiScale + 0.1)  
	end)  
end  
  
for _, t in ipairs(TABS) do  
	if t ~= "Settings" and t ~= "Speed" and t ~= "Combat" and t ~= "Misc" and t ~= "Move" then  
		makePage(t)  
	end  
end  
  
-- ===== SPEED PAGE =====  
makePage("Speed") -- register right col  
local speedPage = Instance.new("ScrollingFrame", body)  
speedPage.Size = UDim2.new(1, 0, 1, 0); speedPage.Position = UDim2.new(0,0,0,0); speedPage.Visible = false; scrollPages["Speed"] = speedPage  
speedPage.BackgroundTransparency = 1  
speedPage.BorderSizePixel = 0  
speedPage.ScrollBarThickness = 3  
speedPage.ScrollBarImageColor3 = PURPLE  
speedPage.CanvasSize = UDim2.new(0, 0, 0, 0)  
speedPage.AutomaticCanvasSize = Enum.AutomaticSize.Y  
local spLayout = Instance.new("UIListLayout", speedPage)  
spLayout.Padding = UDim.new(0, 6)  
spLayout.SortOrder = Enum.SortOrder.LayoutOrder  
local spPad = Instance.new("UIPadding", speedPage)  
spPad.PaddingTop = UDim.new(0, 10)  
spPad.PaddingLeft = UDim.new(0, 10)  
spPad.PaddingRight = UDim.new(0, 10)  
  
-- Input row helper  
local function makeInputRow(parent, labelTxt, defaultVal, lo, onChange)  
	local row = Instance.new("Frame", parent)  
	row.Size = UDim2.new(1, 0, 0, 38)  
	row.BackgroundColor3 = CARD  
	row.BorderSizePixel = 0  
	row.LayoutOrder = lo  
	Instance.new("UICorner", row).CornerRadius = UDim.new(0, 8)
	Instance.new("UIStroke", row).Color = BORDER  
	local lbl = Instance.new("TextLabel", row)  
	lbl.Size = UDim2.new(0.55, 0, 1, 0)  
	lbl.Position = UDim2.new(0, 10, 0, 0)  
	lbl.BackgroundTransparency = 1  
	lbl.Text = labelTxt  
	lbl.TextColor3 = TEXT  
	lbl.Font = Enum.Font.GothamBold  
	lbl.TextSize = 11  
	lbl.TextXAlignment = Enum.TextXAlignment.Left  
	local box = Instance.new("TextBox", row)  
	box.Size = UDim2.new(0, 64, 0, 24)  
	box.Position = UDim2.new(1, -74, 0.5, -12)  
	box.BackgroundColor3 = Color3.fromRGB(10, 10, 10)  
	box.BorderSizePixel = 0  
	box.Text = tostring(defaultVal)  
	box.TextColor3 = WHITE  
	box.Font = Enum.Font.GothamBold  
	box.TextSize = 11  
	box.ClearTextOnFocus = false  
	Instance.new("UICorner", box).CornerRadius = UDim.new(0, 5)  
	Instance.new("UIStroke", box).Color = BORDER  
	box.FocusLost:Connect(function()  
		local v = tonumber(box.Text)  
		if v and v > 0 then  
			onChange(v); saveConfig()  
			local orig = box.BackgroundColor3  
			TweenService:Create(box, TweenInfo.new(0.1), {BackgroundColor3 = Color3.fromRGB(15, 40, 20)}):Play()  
			task.delay(0.5, function() TweenService:Create(box, TweenInfo.new(0.2), {BackgroundColor3 = orig}):Play() end)  
		else box.Text = tostring(defaultVal) end  
	end)  
	return box  
end  
  
-- Section header helper  
makeSecHdr(speedPage, "Speed Configuration", 1)  
  
local function makeKbBox(parent, entry, xOffset)  
	local box = Instance.new("TextButton", parent)  
	box.Size = UDim2.new(0, 36, 0, 26)  
	box.Position = UDim2.new(1, xOffset, 0.5, -13)  
	box.BackgroundColor3 = Color3.fromRGB(10, 10, 10)  
	box.BorderSizePixel = 0  
	box.Text = entry.key.Name  
	box.TextColor3 = WHITE  
	box.Font = Enum.Font.GothamBold  
	box.TextSize = 11  
	box.ZIndex = 5  
	Instance.new("UICorner", box).CornerRadius = UDim.new(0, 5)  
	Instance.new("UIStroke", box).Color = BORDER  
	local listening = false; local kc2  
	box.MouseButton1Click:Connect(function()  
		if listening then return end  
		listening = true; box.Text = "..."; box.TextColor3 = PINK  
		if kc2 then kc2:Disconnect() end  
		kc2 = UserInputService.InputBegan:Connect(function(inp)  
			if not isBindableInput(inp) then return end  
			if inp.UserInputType==Enum.UserInputType.Keyboard and UIS:GetFocusedTextBox() then return end  
			if true then  
				if inp.KeyCode == Enum.KeyCode.Escape then  
					listening = false; box.Text = entry.key.Name; box.TextColor3 = WHITE  
					if kc2 then kc2:Disconnect(); kc2 = nil end; return  
				end  
				if isGamepadInput(inp) then entry.gp=inp.KeyCode;entry.kb=nil else entry.kb=inp.KeyCode;entry.gp=nil end
				entry.key = inp.KeyCode; box.Text = inp.KeyCode.Name; box.TextColor3 = WHITE  
				if KB_Speed and KB then
					if entry==KB_Speed.ModeToggle  then KB.ModeToggle.kb=entry.kb;KB.ModeToggle.gp=entry.gp end
					if entry==KB_Speed.SpeedToggle then KB.SpeedToggle.kb=entry.kb;KB.SpeedToggle.gp=entry.gp;KB_Speed.SpeedToggle.kb=entry.kb;KB_Speed.SpeedToggle.gp=entry.gp;KB_Speed.SpeedToggle.key=entry.gp or entry.kb end
				end
				listening = false; if kc2 then kc2:Disconnect(); kc2 = nil end
				pcall(saveConfig)
			end  
		end)  
	end)  
	return box  
end  
  
local function makeSpeedRow(parent, labelTxt, defaultVal, lo, onChange)  
	local row = Instance.new("Frame", parent)  
	row.Size = UDim2.new(1, 0, 0, 38); row.BackgroundColor3 = CARD  
	row.BorderSizePixel = 0; row.LayoutOrder = lo  
	Instance.new("UICorner", row).CornerRadius = UDim.new(0, 8)
	Instance.new("UIStroke", row).Color = BORDER  
	local lbl = Instance.new("TextLabel", row)  
	lbl.Size = UDim2.new(0.55, 0, 1, 0); lbl.Position = UDim2.new(0, 10, 0, 0)  
	lbl.BackgroundTransparency = 1; lbl.Text = labelTxt; lbl.TextColor3 = TEXT  
	lbl.Font = Enum.Font.GothamBold; lbl.TextSize = 11; lbl.TextXAlignment = Enum.TextXAlignment.Left  
	local valBox = Instance.new("TextBox", row)  
	valBox.Size = UDim2.new(0, 64, 0, 24); valBox.Position = UDim2.new(1, -74, 0.5, -12)  
	valBox.BackgroundColor3 = Color3.fromRGB(10,10,10); valBox.BorderSizePixel = 0  
	valBox.Text = tostring(defaultVal); valBox.TextColor3 = WHITE  
	valBox.Font = Enum.Font.GothamBold; valBox.TextSize = 11; valBox.ClearTextOnFocus = false  
	Instance.new("UICorner", valBox).CornerRadius = UDim.new(0, 5)  
	Instance.new("UIStroke", valBox).Color = BORDER  
	valBox.FocusLost:Connect(function()  
		local v=tonumber(valBox.Text)  
		if v and v>0 then  
			onChange(v); saveConfig()  
			local orig = valBox.BackgroundColor3  
			TweenService:Create(valBox,TweenInfo.new(0.1),{BackgroundColor3=Color3.fromRGB(15,40,20)}):Play()  
			task.delay(0.5,function() TweenService:Create(valBox,TweenInfo.new(0.2),{BackgroundColor3=orig}):Play() end)  
		else valBox.Text=tostring(defaultVal) end  
	end)  
	return row  
end  
  
-- ===== SPEED PAGE ROWS =====
-- Each mode has: Normal speed box, Carry speed box, Mode keybind button
-- Plus one shared Carry toggle keybind row at the top

-- Declare loadConfig-accessible speed box refs

-- Helper: make a mode-section with normal+carry speed boxes and mode-switch keybind
local function makeModeSection(parent, sectionLabel, loBase, getNS, setNS, getCS, setCS, kbEntry, modeName)
	makeSecHdr(parent, sectionLabel, loBase)
	-- Normal speed row (no keybind here anymore)
	do
		local row = makeSpeedRow(parent, "Normal Speed", getNS(), loBase+1, function(v) setNS(v) end)
		local box = row:FindFirstChildOfClass("TextBox")
		if sectionLabel == "Normal Mode" then _speedBoxNS = box
		elseif sectionLabel == "Lagger Mode" then _speedBoxLS = box
		end
	end
	-- Carry speed row
	do
		local row = makeSpeedRow(parent, "Carry Speed", getCS(), loBase+2, function(v) setCS(v) end)
		local box = row:FindFirstChildOfClass("TextBox")
		if sectionLabel == "Normal Mode" then _speedBoxCS = box
		elseif sectionLabel == "Lagger Mode" then _speedBoxLS2 = box
		end
	end
	-- Mode Toggle row: click to activate this mode, keybind on right
	if modeName then
		local row = Instance.new("Frame", parent)
		row.Size = UDim2.new(1, 0, 0, 38); row.BackgroundColor3 = CARD
		row.BorderSizePixel = 0; row.LayoutOrder = loBase+3
		Instance.new("UICorner", row).CornerRadius = UDim.new(0, 8)
		Instance.new("UIStroke", row).Color = BORDER
		Instance.new("UICorner", row).CornerRadius = UDim.new(0, 6)
		-- Label
		local lbl = Instance.new("TextLabel", row)
		lbl.Size = UDim2.new(0.5, 0, 1, 0); lbl.Position = UDim2.new(0, 10, 0, 0)
		lbl.BackgroundTransparency = 1; lbl.Text = "Mode Toggle"; lbl.TextColor3 = TEXT
		lbl.Font = Enum.Font.GothamBold; lbl.TextSize = 11; lbl.TextXAlignment = Enum.TextXAlignment.Left
		-- Active indicator dot
		local dot = Instance.new("Frame", row)
		dot.Size = UDim2.new(0, 8, 0, 8); dot.Position = UDim2.new(0.5, -4, 0.5, -4)
		dot.BackgroundColor3 = activeSpeedMode == modeName and PINK or BORDER
		dot.BorderSizePixel = 0
		Instance.new("UICorner", dot).CornerRadius = UDim.new(1, 0)
		-- Keybind box
		local kbBox = nil
		if kbEntry then
			kbBox = makeKbBox(row, kbEntry, -46)
			kbEntry.setBox = function(kc) kbBox.Text = kc.Name end
		end
		-- Clickable area to switch mode
		local clk = Instance.new("TextButton", row)
		clk.Size = UDim2.new(1, kbEntry and -60 or 0, 1, 0); clk.Position = UDim2.new(0, 0, 0, 0)
		clk.BackgroundTransparency = 1; clk.Text = ""; clk.ZIndex = 6
		clk.MouseButton1Click:Connect(function()
			if _anyKeyListening then return end
			switchMode(modeName)
			dot.BackgroundColor3 = PINK
		end)
		-- Hook updateModeButtons to refresh dot
		local _prev = updateModeButtons
		updateModeButtons = function()
			if _prev then _prev() end
			dot.BackgroundColor3 = activeSpeedMode == modeName and PINK or BORDER
		end
	end
end

makeModeSection(speedPage, "Normal Mode",  3,  function() return NS  end, function(v) NS =v end, function() return CS  end, function(v) CS =v end, nil, nil)
makeModeSection(speedPage, "Lagger Mode",  7,  function() return LS  end, function(v) LS =v end, function() return LS2 end, function(v) LS2=v end, nil, nil)

-- Mode + Carry keybind rows
do
	-- Normal Mode Speeds / Lagger Mode Speeds toggle key
	makeSecHdr(speedPage, "Normal Mode Speeds / Lagger Mode Speeds", 11)
	local rowM = Instance.new("Frame", speedPage)
	rowM.Size = UDim2.new(1, 0, 0, 38); rowM.BackgroundColor3 = CARD
	rowM.BorderSizePixel = 0; rowM.LayoutOrder = 12
	Instance.new("UICorner", rowM).CornerRadius = UDim.new(0, 8)
	Instance.new("UIStroke", rowM).Color = BORDER
	Instance.new("UICorner", rowM).CornerRadius = UDim.new(0, 6)
	local lblM = Instance.new("TextLabel", rowM)
	lblM.Size = UDim2.new(0.55, 0, 1, 0); lblM.Position = UDim2.new(0, 10, 0, 0)
	lblM.BackgroundTransparency = 1; lblM.Text = "Normal / Lagger"
	lblM.TextColor3 = TEXT; lblM.Font = Enum.Font.GothamBold
	lblM.TextSize = 11; lblM.TextXAlignment = Enum.TextXAlignment.Left
	local kbBoxM = makeKbBox(rowM, KB_Speed.ModeToggle, -46)
	KB_Speed.ModeToggle.setBox = function(kc)
		if kbBoxM and kbBoxM.Parent then kbBoxM.Text = kc.Name end
	end
	-- Toggle Speed (Normal / Carry) key
	makeSecHdr(speedPage, "Toggle Speed", 13)
	local rowS = Instance.new("Frame", speedPage)
	rowS.Size = UDim2.new(1, 0, 0, 38); rowS.BackgroundColor3 = CARD
	rowS.BorderSizePixel = 0; rowS.LayoutOrder = 14
	Instance.new("UICorner", rowS).CornerRadius = UDim.new(0, 8)
	Instance.new("UIStroke", rowS).Color = BORDER
	Instance.new("UICorner", rowS).CornerRadius = UDim.new(0, 6)
	local lblS = Instance.new("TextLabel", rowS)
	lblS.Size = UDim2.new(0.55, 0, 1, 0); lblS.Position = UDim2.new(0, 10, 0, 0)
	lblS.BackgroundTransparency = 1; lblS.Text = "Normal / Carry"
	lblS.TextColor3 = TEXT; lblS.Font = Enum.Font.GothamBold
	lblS.TextSize = 11; lblS.TextXAlignment = Enum.TextXAlignment.Left
	local kbBoxS = makeKbBox(rowS, KB_Speed.SpeedToggle, -46)
	local _prev = KB_Speed.SpeedToggle.setBox
	KB_Speed.SpeedToggle.setBox = function(kc)
		if kbBoxS and kbBoxS.Parent then kbBoxS.Text = kc.Name end
		if _prev then _prev(kc) end
	end
end

-- Active Mode display row
do
	makeSecHdr(speedPage, "Active Mode", 15)
	local row = Instance.new("Frame", speedPage)
	row.Size = UDim2.new(1, 0, 0, 38)
	row.BackgroundColor3 = CARD
	row.BorderSizePixel = 0
	row.LayoutOrder = 16
	Instance.new("UICorner", row).CornerRadius = UDim.new(0, 8)
	Instance.new("UIStroke", row).Color = BORDER
	local lbl = Instance.new("TextLabel", row)
	lbl.Size = UDim2.new(0.45, 0, 1, 0)
	lbl.Position = UDim2.new(0, 10, 0, 0)
	lbl.BackgroundTransparency = 1
	lbl.Text = "Mode"
	lbl.TextColor3 = TEXT
	lbl.Font = Enum.Font.GothamBold
	lbl.TextSize = 11
	lbl.TextXAlignment = Enum.TextXAlignment.Left
	local modeLbl = Instance.new("TextLabel", row)
	modeLbl.Size = UDim2.new(0.55, 0, 1, 0)
	modeLbl.Position = UDim2.new(0.45, 0, 0, 0)
	modeLbl.BackgroundTransparency = 1
	modeLbl.Text = "Normal"
	modeLbl.TextColor3 = PINK
	modeLbl.Font = Enum.Font.GothamBold
	modeLbl.TextSize = 10
	modeLbl.TextXAlignment = Enum.TextXAlignment.Left

	local function getModeDisplayName()
		local m = activeSpeedMode:sub(1,1):upper()..activeSpeedMode:sub(2)
		return m .. (carryActive and " Carry" or "")
	end

	updateModeLabel = function()
		modeLbl.Text = getModeDisplayName()
	end
end

-- Helper: switch active speed mode (Normal <-> Lagger) while PRESERVING
-- whatever carry state is currently active. Also keeps legacy alias vars
-- in sync so nothing using laggerMode/laggerPhase/speedMode breaks.
function switchMode(newMode)
	activeSpeedMode = newMode
	laggerMode  = (newMode == "lagger")
	laggerPhase = carryActive and 2 or 1
	speedMode   = carryActive
	syncStealRadius()
	if updateModeLabel   then updateModeLabel()   end
	if updateModeButtons then updateModeButtons() end
	saveConfig()
end

-- Helper: toggle carry on/off within the CURRENT active speed mode
-- (does not change activeSpeedMode at all)
function toggleCarry()
	carryActive = not carryActive
	speedMode   = carryActive
	laggerPhase = carryActive and 2 or 1
	if updateModeLabel   then updateModeLabel()   end
	if updateModeButtons then updateModeButtons() end
	saveConfig()
end

-- Global keybind listener — exact blossom_pc logic
UIS.InputBegan:Connect(function(inp,gpe)
	if _anyKeyListening then return end
	if inp.UserInputType==Enum.UserInputType.Keyboard then if gpe or UIS:GetFocusedTextBox() then return end elseif not isGamepadInput(inp) then return end
	if not isBindableInput(inp) then return end
	local kc=inp.KeyCode
	if menuToggleKey and kc==menuToggleKey then
		main.Visible = not main.Visible
	elseif kbMatch(KB.SpeedToggle,kc) or kbMatch(KB_Speed.SpeedToggle,kc) or kc==KB_Speed.SpeedToggle.key or kc==KB.SpeedToggle.kb or (KB.SpeedToggle.gp and kc==KB.SpeedToggle.gp) or (KB_Speed.SpeedToggle.gp and kc==KB_Speed.SpeedToggle.gp) then
		-- Toggle carry ON/OFF for whichever mode (Normal or Lagger) is currently active.
		-- Does NOT change activeSpeedMode.
		toggleCarry()
	elseif kbMatch(KB.ModeToggle,kc) or kbMatch(KB_Speed.ModeToggle,kc) or kc==KB_Speed.ModeToggle.key then
		-- Toggle between Normal and Lagger mode. Carry state carries over.
		switchMode(activeSpeedMode=="normal" and "lagger" or "normal")
	elseif kbMatch(KB.italozinReset,kc) then
		instaReset()
	end
end)

-- ── Lock UI & Save Config (bottom of Settings page) ─────────────────────────
do
	local _PINK   = Color3.fromRGB(248, 151, 175)
	local _CARD   = Color3.fromRGB(34,  10, 24)
	local _WHITE  = Color3.fromRGB(255, 235, 242)
	local _DIM    = Color3.fromRGB(200, 140, 165)
	local _BORDER = Color3.fromRGB(248, 151, 175)
	local _BG2    = Color3.fromRGB(26,  8, 18)

	-- section header helper (inline, uses settingsPage2 directly)
	local function addHdr(txt, lo_val)
		local f = Instance.new("Frame", settingsPage2)
		f.Size = UDim2.new(1, 0, 0, 18); f.BackgroundTransparency = 1
		f.BorderSizePixel = 0; f.LayoutOrder = lo_val; f.ZIndex = 4
		local t = Instance.new("TextLabel", f)
		t.Size = UDim2.new(1, 0, 1, 0); t.BackgroundTransparency = 1
		t.Text = txt:upper(); t.TextColor3 = _WHITE
		t.Font = Enum.Font.GothamBold; t.TextSize = 8
		t.TextXAlignment = Enum.TextXAlignment.Left; t.ZIndex = 5
		local line = Instance.new("Frame", f)
		line.Size = UDim2.new(1, 0, 0, 1); line.Position = UDim2.new(0, 0, 1, -1)
		line.BackgroundColor3 = _BORDER; line.BorderSizePixel = 0; line.ZIndex = 4
	end

	-- ── Lock UI pill ─────────────────────────────────────────────────────────
	addHdr("UI Lock", 900)
	local lockCard = Instance.new("Frame", settingsPage2)
	lockCard.Size = UDim2.new(1, 0, 0, 38); lockCard.BackgroundColor3 = _CARD
	lockCard.BorderSizePixel = 0; lockCard.LayoutOrder = 901; lockCard.ZIndex = 4
	Instance.new("UICorner", lockCard).CornerRadius = UDim.new(0, 7)
	Instance.new("UIStroke", lockCard).Color = _BORDER
	local lockLbl = Instance.new("TextLabel", lockCard)
	lockLbl.Size = UDim2.new(0, 160, 1, 0); lockLbl.Position = UDim2.new(0, 10, 0, 0)
	lockLbl.BackgroundTransparency = 1; lockLbl.Text = "Lock UI (No Dragging)"
	lockLbl.TextColor3 = _WHITE; lockLbl.Font = Enum.Font.GothamBold
	lockLbl.TextSize = 11; lockLbl.TextXAlignment = Enum.TextXAlignment.Left; lockLbl.ZIndex = 10
	local lockPbg = Instance.new("Frame", lockCard)
	lockPbg.Size = UDim2.new(0, 36, 0, 19); lockPbg.Position = UDim2.new(1, -46, 0.5, -9)
	lockPbg.BackgroundColor3 = Color3.fromRGB(30, 30, 42); lockPbg.BorderSizePixel = 0; lockPbg.ZIndex = 8
	Instance.new("UICorner", lockPbg).CornerRadius = UDim.new(0, 10)
	local lockPs = Instance.new("UIStroke", lockPbg); lockPs.Color = _BORDER
	local lockDot = Instance.new("Frame", lockPbg)
	lockDot.Size = UDim2.new(0, 13, 0, 13); lockDot.Position = UDim2.new(0, 2, 0.5, -6)
	lockDot.BackgroundColor3 = _DIM; lockDot.BorderSizePixel = 0; lockDot.ZIndex = 9
	Instance.new("UICorner", lockDot).CornerRadius = UDim.new(0, 4)
	local lockOn = false
	setLockUIVisual = function(on)
		lockOn = on
		TweenService:Create(lockPbg, TweenInfo.new(0.18), {BackgroundColor3 = on and _PINK or Color3.fromRGB(30,30,42)}):Play()
		TweenService:Create(lockPs,  TweenInfo.new(0.18), {Color = on and _PINK or _BORDER}):Play()
		TweenService:Create(lockDot, TweenInfo.new(0.18, Enum.EasingStyle.Back), {
			Position = on and UDim2.new(1,-15,0.5,-6) or UDim2.new(0,2,0.5,-6),
			BackgroundColor3 = on and _WHITE or _DIM
		}):Play()
	end
	local lockClk = Instance.new("TextButton", lockCard)
	lockClk.Size = UDim2.new(1, 0, 1, 0); lockClk.BackgroundTransparency = 1
	lockClk.Text = ""; lockClk.ZIndex = 6
	lockClk.MouseButton1Click:Connect(function()
		lockOn = not lockOn; uiLocked = lockOn
		setLockUIVisual(lockOn); saveConfig()
	end)

	-- ── Save Config button ────────────────────────────────────────────────────
	addHdr("Save", 902)
	local saveCard = Instance.new("Frame", settingsPage2)
	saveCard.Size = UDim2.new(1, 0, 0, 38); saveCard.BackgroundColor3 = _CARD
	saveCard.BorderSizePixel = 0; saveCard.LayoutOrder = 903; saveCard.ZIndex = 4
	Instance.new("UICorner", saveCard).CornerRadius = UDim.new(0, 7)
	Instance.new("UIStroke", saveCard).Color = _BORDER
	local saveLbl = Instance.new("TextLabel", saveCard)
	saveLbl.Size = UDim2.new(0, 160, 1, 0); saveLbl.Position = UDim2.new(0, 10, 0, 0)
	saveLbl.BackgroundTransparency = 1; saveLbl.Text = "Save Config"
	saveLbl.TextColor3 = _WHITE; saveLbl.Font = Enum.Font.GothamBold
	saveLbl.TextSize = 11; saveLbl.TextXAlignment = Enum.TextXAlignment.Left; saveLbl.ZIndex = 10
	local saveBtn = Instance.new("TextButton", saveCard)
	saveBtn.Size = UDim2.new(0, 60, 0, 22); saveBtn.Position = UDim2.new(1, -70, 0.5, -11)
	saveBtn.BackgroundColor3 = _PINK; saveBtn.BorderSizePixel = 0
	saveBtn.Text = "SAVE"; saveBtn.TextColor3 = Color3.fromRGB(20, 5, 15)
	saveBtn.Font = Enum.Font.GothamBlack; saveBtn.TextSize = 9; saveBtn.ZIndex = 11
	Instance.new("UICorner", saveBtn).CornerRadius = UDim.new(0, 5)
	saveBtn.MouseButton1Click:Connect(function()
		saveConfig()
		saveBtn.Text = "✓ Saved!"; saveBtn.BackgroundColor3 = Color3.fromRGB(60, 180, 100)
		task.delay(1.5, function()
			if saveBtn and saveBtn.Parent then
				saveBtn.Text = "SAVE"; saveBtn.BackgroundColor3 = _PINK
			end
		end)
	end)
end

end -- end Settings+Speed do block
  
do -- Combat page locals  
-- ===== COMBAT PAGE =====  
makePage("Combat") -- register right col  
local combatPage = Instance.new("ScrollingFrame", body)  
combatPage.Size = UDim2.new(1, 0, 1, 0); combatPage.Position = UDim2.new(0,0,0,0); combatPage.Visible = false; scrollPages["Combat"] = combatPage  
combatPage.BackgroundTransparency = 1  
combatPage.BorderSizePixel = 0  
combatPage.ScrollBarThickness = 3  
combatPage.ScrollBarImageColor3 = PURPLE  
combatPage.CanvasSize = UDim2.new(0, 0, 0, 0)  
combatPage.AutomaticCanvasSize = Enum.AutomaticSize.Y  
local cpLayout = Instance.new("UIListLayout", combatPage)  
cpLayout.Padding = UDim.new(0, 6)  
cpLayout.SortOrder = Enum.SortOrder.LayoutOrder  
local cpPad = Instance.new("UIPadding", combatPage)  
cpPad.PaddingTop = UDim.new(0, 10)  
cpPad.PaddingLeft = UDim.new(0, 10)  
cpPad.PaddingRight = UDim.new(0, 10)  
  
local function makeCombatToggle(parent, labelTxt, lo, onToggle)  
	local row = Instance.new("Frame", parent)  
	row.Size = UDim2.new(1, 0, 0, 38)  
	row.BackgroundColor3 = CARD  
	row.BorderSizePixel = 0  
	row.LayoutOrder = lo  
	Instance.new("UICorner", row).CornerRadius = UDim.new(0, 8)
	Instance.new("UIStroke", row).Color = BORDER  
	local lbl = Instance.new("TextLabel", row)  
	lbl.Size = UDim2.new(0.6, 0, 1, 0)  
	lbl.Position = UDim2.new(0, 10, 0, 0)  
	lbl.BackgroundTransparency = 1  
	lbl.Text = labelTxt  
	lbl.TextColor3 = TEXT  
	lbl.Font = Enum.Font.GothamBold  
	lbl.TextSize = 11  
	lbl.TextXAlignment = Enum.TextXAlignment.Left  
	local pillBg2 = Instance.new("Frame", row)  
	pillBg2.Size = UDim2.new(0, 36, 0, 20)  
	pillBg2.Position = UDim2.new(1, -46, 0.5, -10)  
	pillBg2.BackgroundColor3 = Color3.fromRGB(38, 10, 26)  
	pillBg2.BorderSizePixel = 0  
	Instance.new("UICorner", pillBg2).CornerRadius = UDim.new(1, 0)  
	local dot2 = Instance.new("Frame", pillBg2)  
	dot2.Size = UDim2.new(0, 14, 0, 14)  
	dot2.Position = UDim2.new(0, 3, 0.5, -7)  
	dot2.BackgroundColor3 = Color3.fromRGB(85, 85, 104)  
	dot2.BorderSizePixel = 0  
	Instance.new("UICorner", dot2).CornerRadius = UDim.new(1, 0)  
	local isOn = false  
	local function setV(on)  
		isOn = on  
		TweenService:Create(pillBg2, TweenInfo.new(0.18), {BackgroundColor3 = on and PINK or Color3.fromRGB(38,10,26)}):Play()  
		TweenService:Create(dot2, TweenInfo.new(0.18, Enum.EasingStyle.Back), {  
			Position = on and UDim2.new(1,-17,0.5,-7) or UDim2.new(0,3,0.5,-7),  
			BackgroundColor3 = on and WHITE or Color3.fromRGB(85,85,104)  
		}):Play()  
	end  
	local clk = Instance.new("TextButton", row)  
	clk.Size = UDim2.new(1, 0, 1, 0)  
	clk.BackgroundTransparency = 1  
	clk.Text = ""  
	clk.ZIndex = 5  
	clk.MouseButton1Click:Connect(function()  
		isOn = not isOn  
		setV(isOn)  
		if onToggle then onToggle(isOn) end  
	end)  
	return setV  
end  
  
makeSecHdr(combatPage, "Bat Aimbot", 1)  
local setAimbotV = makeCombatToggle(combatPage, "Bat Aimbot", 2, function(on)  
	autoBatEnabled = on  
	if on then startBatAimbot() else stopBatAimbot() end  
end)  
autoBatSetVisual = setAimbotV  
  
-- Keybind row for bat aimbot  
do  
	local row = Instance.new("Frame", combatPage)  
	row.Size = UDim2.new(1, 0, 0, 38)  
	row.BackgroundColor3 = CARD  
	row.BorderSizePixel = 0  
	row.LayoutOrder = 3  
	Instance.new("UICorner", row).CornerRadius = UDim.new(0, 8)
	Instance.new("UIStroke", row).Color = BORDER  
	local lbl = Instance.new("TextLabel", row)  
	lbl.Size = UDim2.new(0.6, 0, 1, 0)  
	lbl.Position = UDim2.new(0, 10, 0, 0)  
	lbl.BackgroundTransparency = 1  
	lbl.Text = "Bat Keybind"  
	lbl.TextColor3 = TEXT  
	lbl.Font = Enum.Font.GothamBold  
	lbl.TextSize = 11  
	lbl.TextXAlignment = Enum.TextXAlignment.Left  
	if not _G.__BH_batKey then _G.__BH_batKey={key=Enum.KeyCode.E,kb=Enum.KeyCode.E,gp=nil} end
	local batKey = _G.__BH_batKey
	if not batKey.key then batKey.key=Enum.KeyCode.E; batKey.kb=Enum.KeyCode.E; batKey.gp=nil end
	local batKeyBox; _G.__BH_batKeyBox = nil  
	local box = Instance.new("TextButton", row)  
	batKeyBox = box; _G.__BH_batKeyBox = box
	box.Size = UDim2.new(0, 36, 0, 26)  
	box.Position = UDim2.new(1, -46, 0.5, -13)  
	box.BackgroundColor3 = Color3.fromRGB(10, 10, 10)  
	box.BorderSizePixel = 0  
	box.Text = (batKey.gp and batKey.gp.Name) or batKey.key.Name  
	box.TextColor3 = WHITE  
	box.Font = Enum.Font.GothamBold  
	box.TextSize = 11  
	box.ZIndex = 5  
	Instance.new("UICorner", box).CornerRadius = UDim.new(0, 5)  
	Instance.new("UIStroke", box).Color = BORDER  
	local listening = false  
	local kc2  
	box.MouseButton1Click:Connect(function()  
		if listening then return end  
		listening = true; box.Text = "..."; box.TextColor3 = PINK  
		if kc2 then kc2:Disconnect() end  
		kc2 = UserInputService.InputBegan:Connect(function(inp)  
			if not isBindableInput(inp) then return end  
			if inp.UserInputType==Enum.UserInputType.Keyboard and UIS:GetFocusedTextBox() then return end  
			if true then  
				if inp.KeyCode == Enum.KeyCode.Escape then  
					listening = false; box.Text = batKey.key.Name; box.TextColor3 = WHITE  
					if kc2 then kc2:Disconnect(); kc2 = nil end; return  
				end  
				local isGp=isGamepadInput(inp)
				if isGp then batKey.gp=inp.KeyCode;batKey.kb=nil else batKey.kb=inp.KeyCode;batKey.gp=nil end
				batKey.key=inp.KeyCode
				KB.AutoBat.kb=batKey.kb; KB.AutoBat.gp=batKey.gp
				box.Text = inp.KeyCode.Name; box.TextColor3 = WHITE  
				listening = false; if kc2 then kc2:Disconnect(); kc2 = nil end
				saveConfig()
			end  
		end)  
	end)  
	-- wire keybind to toggle aimbot  
	UIS.InputBegan:Connect(function(inp,gpe)  
		if _anyKeyListening or listening then return end  
		if inp.UserInputType==Enum.UserInputType.Keyboard then if gpe or UIS:GetFocusedTextBox() then return end elseif not isGamepadInput(inp) then return end  
		if not isBindableInput(inp) then return end  
		if kbMatch(batKey,inp.KeyCode) or inp.KeyCode==batKey.key then  
			autoBatEnabled = not autoBatEnabled  
			if autoBatSetVisual then autoBatSetVisual(autoBatEnabled) end  
			if autoBatEnabled then startBatAimbot() else stopBatAimbot() end  
		end  
	end)  
end  
  
-- Auto Bat Speed input row  
do  
	local row = Instance.new("Frame", combatPage)  
	row.Size = UDim2.new(1, 0, 0, 38)  
	row.BackgroundColor3 = CARD  
	row.BorderSizePixel = 0  
	row.LayoutOrder = 3.5  -- between keybind (3) and bat counter header (4)  
	Instance.new("UICorner", row).CornerRadius = UDim.new(0, 8)
	Instance.new("UIStroke", row).Color = BORDER  
	local lbl = Instance.new("TextLabel", row)  
	lbl.Size = UDim2.new(0.6, 0, 1, 0)  
	lbl.Position = UDim2.new(0, 10, 0, 0)  
	lbl.BackgroundTransparency = 1  
	lbl.Text = "Auto Bat Speed"  
	lbl.TextColor3 = TEXT  
	lbl.Font = Enum.Font.GothamBold  
	lbl.TextSize = 11  
	lbl.TextXAlignment = Enum.TextXAlignment.Left  
	local speedBox = Instance.new("TextBox", row)  
	speedBox.Size = UDim2.new(0, 64, 0, 24)  
	speedBox.Position = UDim2.new(1, -74, 0.5, -12)  
	speedBox.BackgroundColor3 = Color3.fromRGB(10, 10, 10)  
	speedBox.BorderSizePixel = 0  
	speedBox.Text = tostring(autoBatSpeed)  
	_G.__BH_autoBatSpeedBox = speedBox
	speedBox.TextColor3 = WHITE  
	speedBox.Font = Enum.Font.GothamBold  
	speedBox.TextSize = 11  
	speedBox.ClearTextOnFocus = false  
	Instance.new("UICorner", speedBox).CornerRadius = UDim.new(0, 5)  
	Instance.new("UIStroke", speedBox).Color = BORDER  
	speedBox.FocusLost:Connect(function()  
		local v = tonumber(speedBox.Text)  
		if v and v > 0 then  
			autoBatSpeed = v  
			saveConfig()  
			local orig = speedBox.BackgroundColor3  
			TweenService:Create(speedBox, TweenInfo.new(0.1), {BackgroundColor3 = Color3.fromRGB(20, 40, 25)}):Play()  
			task.delay(0.5, function() TweenService:Create(speedBox, TweenInfo.new(0.2), {BackgroundColor3 = orig}):Play() end)  
		else  
			speedBox.Text = tostring(autoBatSpeed)  
	_G.__BH_autoBatSpeedBox = speedBox
		end  
	end)  
end  
  
  
  
makeSecHdr(combatPage, "Bat Counter", 4)  
miscSetVisuals["Bat Counter"] = makeCombatToggle(combatPage, "Bat Counter", 5, function(on)  
	batCounterEnabled = on  
	if on then _setupBatCounter(LP.Character)  
	else  
		for _,c in pairs(batCounterConns) do pcall(function() c:Disconnect() end) end  
		batCounterConns = {}  
	end  
end)  

-- ── Body Lock section ─────────────────────────────────────────────────────────
makeSecHdr(combatPage, "Body Lock", 5.6)
miscSetVisuals["Body Lock"] = makeCombatToggle(combatPage, "Body Lock", 5.7, function(on)
	bodyLockEnabled = on
	if on then
		startBodyLock()
	else
		stopBodyLock()
	end
end)
bodyLockSetVisual = miscSetVisuals["Body Lock"]

-- Body Lock Range row
do
	local rangeRow = Instance.new("Frame", combatPage)
	rangeRow.Size = UDim2.new(1, 0, 0, 38)
	rangeRow.BackgroundColor3 = CARD
	rangeRow.BorderSizePixel = 0
	rangeRow.LayoutOrder = 5.8
	Instance.new("UICorner", rangeRow).CornerRadius = UDim.new(0, 7)
	Instance.new("UIStroke", rangeRow).Color = BORDER
	local rangeLbl = Instance.new("TextLabel", rangeRow)
	rangeLbl.Size = UDim2.new(0.6, 0, 1, 0)
	rangeLbl.Position = UDim2.new(0, 10, 0, 0)
	rangeLbl.BackgroundTransparency = 1
	rangeLbl.Text = "Lock Range"
	rangeLbl.TextColor3 = TEXT
	rangeLbl.Font = Enum.Font.GothamBold
	rangeLbl.TextSize = 11
	rangeLbl.TextXAlignment = Enum.TextXAlignment.Left
	local rangeBox = Instance.new("TextBox", rangeRow)
	rangeBox.Size = UDim2.new(0, 52, 0, 24)
	rangeBox.Position = UDim2.new(1, -62, 0.5, -12)
	rangeBox.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
	rangeBox.BorderSizePixel = 0
	rangeBox.Text = tostring(bodyLockRange)
	rangeBox.TextColor3 = WHITE
	rangeBox.Font = Enum.Font.GothamBold
	rangeBox.TextSize = 11
	rangeBox.ClearTextOnFocus = false
	Instance.new("UICorner", rangeBox).CornerRadius = UDim.new(0, 5)
	Instance.new("UIStroke", rangeBox).Color = BORDER
	rangeBox.FocusLost:Connect(function()
		local v = tonumber(rangeBox.Text)
		if v and v > 0 then
			bodyLockRange = math.clamp(math.floor(v), 5, 200)
			rangeBox.Text = tostring(bodyLockRange)
		else
			rangeBox.Text = tostring(bodyLockRange)
		end
	end)
end
-- ── End Body Lock section ─────────────────────────────────────────────────────

  
-- ───────────────────────────────────────────────────────────────  
-- RAGDOLL TP — positions from tp_rag reference  
-- ───────────────────────────────────────────────────────────────  
local _spdHintUpdate   = nil  -- set after the speed modes panel is built
local rtPanel     = nil  -- ragdoll timer panel frame (outer scope for toggle)
local rtCountLbl  = nil  -- ragdoll timer count label (outer scope for toggle)
  
-- ─── RAGDOLL TIMER GUI ───────────────────────────────────────────────────────
makeSecHdr(combatPage, "Ragdoll Timer", 6)
miscSetVisuals["Ragdoll Timer"] = makeCombatToggle(combatPage, "Ragdoll Timer", 7, function(on)
	ragdollTimerEnabled = on
	if rtPanel then
		rtPanel.Visible = on
		if not on and rtCountLbl then rtCountLbl.Text = "" end
	end
end)
-- wrap to also run logic on load
do local _sv=miscSetVisuals["Ragdoll Timer"]; miscSetVisuals["Ragdoll Timer"]=function(on) _sv(on); ragdollTimerEnabled=on; if rtPanel then rtPanel.Visible=on end end end

do local _sv=miscSetVisuals["Bat Counter"]; miscSetVisuals["Bat Counter"]=function(on) _sv(on); batCounterEnabled=on; if on then pcall(function() _setupBatCounter(LP.Character) end) end end end

-- Ragdoll Timer floating panel
do
	local _rtGui = Instance.new("ScreenGui")
	_rtGui.Name = "italozinRagTimer"
	_rtGui.ResetOnSpawn = false
	_rtGui.DisplayOrder = 26
	_rtGui.IgnoreGuiInset = true
	if not pcall(function() _rtGui.Parent = game:GetService("CoreGui") end) then
		_rtGui.Parent = LP:WaitForChild("PlayerGui")
	end

	rtPanel = Instance.new("Frame", _rtGui)
	rtPanel.Name = "RagTimerPanel"
	rtPanel.Size = UDim2.new(0, 130, 0, 70)
	rtPanel.Position = UDim2.new(0.5, 120, 0, 160)
	rtPanel.BackgroundColor3 = Color3.fromRGB(10, 4, 8)
	rtPanel.BorderSizePixel = 0
	rtPanel.Active = true
	rtPanel.ZIndex = 65
	rtPanel.Visible = false  -- shown/hidden by toggle
	Instance.new("UICorner", rtPanel).CornerRadius = UDim.new(0, 8)
	local rtStroke = Instance.new("UIStroke", rtPanel)
	rtStroke.Color = Color3.fromRGB(255, 100, 160)
	rtStroke.Thickness = 1.4

	local rtTitleBar = Instance.new("Frame", rtPanel)
	rtTitleBar.Size = UDim2.new(1, 0, 0, 22)
	rtTitleBar.Position = UDim2.new(0, 0, 0, 0)
	rtTitleBar.BackgroundColor3 = Color3.fromRGB(35, 10, 22)
	rtTitleBar.BorderSizePixel = 0
	rtTitleBar.ZIndex = 66
	rtTitleBar.Active = true
	Instance.new("UICorner", rtTitleBar).CornerRadius = UDim.new(0, 8)
	local rtTitleFix = Instance.new("Frame", rtTitleBar)
	rtTitleFix.Size = UDim2.new(1, 0, 0.5, 0)
	rtTitleFix.Position = UDim2.new(0, 0, 0.5, 0)
	rtTitleFix.BackgroundColor3 = Color3.fromRGB(35, 10, 22)
	rtTitleFix.BorderSizePixel = 0
	rtTitleFix.ZIndex = 66

	local rtTitleLbl = Instance.new("TextLabel", rtTitleBar)
	rtTitleLbl.Size = UDim2.new(1, 0, 1, 0)
	rtTitleLbl.BackgroundTransparency = 1
	rtTitleLbl.Text = "RAGDOLL TIMER"
	rtTitleLbl.TextColor3 = Color3.fromRGB(255, 130, 180)
	rtTitleLbl.Font = Enum.Font.GothamBlack
	rtTitleLbl.TextSize = 9
	rtTitleLbl.ZIndex = 67

	rtCountLbl = Instance.new("TextLabel", rtPanel)
	rtCountLbl.Size = UDim2.new(1, 0, 0, 36)
	rtCountLbl.Position = UDim2.new(0, 0, 0, 24)
	rtCountLbl.BackgroundTransparency = 1
	rtCountLbl.Text = ""
	rtCountLbl.TextColor3 = Color3.fromRGB(255, 80, 80)
	rtCountLbl.Font = Enum.Font.GothamBlack
	rtCountLbl.TextScaled = true
	rtCountLbl.ZIndex = 66

	-- Drag logic
	local _rtDrag, _rtDragInput, _rtDragStart, _rtStartPos = false, nil, nil, nil
	rtTitleBar.InputBegan:Connect(function(inp)
		if inp.UserInputType == Enum.UserInputType.MouseButton1
		or inp.UserInputType == Enum.UserInputType.Touch then
			_rtDrag = true; _rtDragStart = inp.Position; _rtStartPos = rtPanel.Position
			inp.Changed:Connect(function()
				if inp.UserInputState == Enum.UserInputState.End then _rtDrag = false end
			end)
		end
	end)
	rtTitleBar.InputChanged:Connect(function(inp)
		if inp.UserInputType == Enum.UserInputType.MouseMovement
		or inp.UserInputType == Enum.UserInputType.Touch then
			_rtDragInput = inp
		end
	end)
	UIS.InputChanged:Connect(function(inp)
		if inp == _rtDragInput and _rtDrag then
			local d = inp.Position - _rtDragStart
			rtPanel.Position = UDim2.new(_rtStartPos.X.Scale, _rtStartPos.X.Offset + d.X, _rtStartPos.Y.Scale, _rtStartPos.Y.Offset + d.Y)
		end
	end)

	-- Ragdoll timer logic: decimal countdown 3.0 → 0.0, panel always visible when enabled
	local _rtTimerActive = false

	local function getTimerColor(t)
		if t > 2.0 then return Color3.fromRGB(255, 60, 60)
		elseif t > 1.0 then return Color3.fromRGB(255, 140, 40)
		else return Color3.fromRGB(255, 220, 40) end
	end

	local function startRagTimerGui(char)
		if _rtTimerActive then return end
		_rtTimerActive = true
		task.spawn(function()
			local t = 3.0
			while t >= 0.0 do
				rtCountLbl.Text = string.format("%.1f", t)
				rtCountLbl.TextColor3 = getTimerColor(t)
				task.wait(0.1)
				t = math.round((t - 0.1) * 10) / 10
			end
			rtCountLbl.Text = "STEAL!"
			rtCountLbl.TextColor3 = Color3.fromRGB(80, 255, 120)
			-- Wait until no longer ragdolled
			repeat task.wait(0.1) until (function()
				local c = LP.Character
				local hum = c and c:FindFirstChildOfClass("Humanoid")
				if not hum then return true end
				local st = hum:GetState()
				return st ~= Enum.HumanoidStateType.Physics
					and st ~= Enum.HumanoidStateType.Ragdoll
					and st ~= Enum.HumanoidStateType.FallingDown
			end)()
			rtCountLbl.Text = ""
			_rtTimerActive = false
		end)
	end

	local function startRagTimerDetection(char)
		RunService.Heartbeat:Connect(function()
			if not ragdollTimerEnabled then return end
			local hum = char and char:FindFirstChildOfClass("Humanoid")
			if not hum then return end
			local st = hum:GetState()
			if st == Enum.HumanoidStateType.Physics
			or st == Enum.HumanoidStateType.Ragdoll
			or st == Enum.HumanoidStateType.FallingDown then
				startRagTimerGui(char)
			end
		end)
	end

	local function onRagTimerChar(char)
		_rtTimerActive = false
		rtCountLbl.Text = ""
		task.wait(0.1)
		startRagTimerDetection(char)
	end

	LP.CharacterAdded:Connect(onRagTimerChar)
	if LP.Character then
		task.spawn(function() onRagTimerChar(LP.Character) end)
	end
end
-- ─── End RAGDOLL TIMER GUI ─────────────────────────────────────────────────

  

  
-- Minimize button (top right of topbar) — italozinHub style  
local minBtn = Instance.new("TextButton", topbar)  
minBtn.Size = UDim2.new(0, 22, 0, 22)  
minBtn.Position = UDim2.new(1, -28, 0.5, -11)  
minBtn.BackgroundColor3 = Color3.fromRGB(50, 12, 30)  
minBtn.BorderSizePixel = 0  
minBtn.Text = "–"  
minBtn.TextColor3 = TEXTDIM  
minBtn.Font = Enum.Font.GothamBlack  
minBtn.TextSize = 13  
minBtn.ZIndex = 13  
Instance.new("UICorner", minBtn).CornerRadius = UDim.new(0, 5)  
local minStroke = Instance.new("UIStroke", minBtn); minStroke.Color = BORDER  
  
-- Mini pill (shown when UI is hidden) — italozinHub style  
local mini = Instance.new("TextButton", sg)  
_savedMiniFrame = mini  
mini.Size = UDim2.new(0, 120, 0, 28)  
mini.Position = UDim2.new(0.5, -60, 0, 20)  
mini.BackgroundColor3 = BG2  
mini.BorderSizePixel = 0  
mini.Text = "italozin HUB"  
mini.TextColor3 = WHITE  
mini.Font = Enum.Font.GothamBlack  
mini.TextSize = 10  
mini.Visible = false  
mini.ZIndex = 50  
Instance.new("UICorner", mini).CornerRadius = UDim.new(0, 8)  
local miniStroke = Instance.new("UIStroke", mini)  
miniStroke.Color = BORDER; miniStroke.Thickness = 1  
  
-- Drag mini pill  
local _mDrag, _mDragStart, _mStartPos  
mini.InputBegan:Connect(function(inp)  
	if inp.UserInputType == Enum.UserInputType.MouseButton1 then  
		_mDrag = true; _mDragStart = inp.Position; _mStartPos = mini.Position  
		inp.Changed:Connect(function() if inp.UserInputState == Enum.UserInputState.End then _mDrag = false end end)  
	end  
end)  
UserInputService.InputChanged:Connect(function(inp)  
	if _mDrag and (inp.UserInputType == Enum.UserInputType.MouseMovement) then  
		local d = inp.Position - _mDragStart  
		mini.Position = UDim2.new(_mStartPos.X.Scale, _mStartPos.X.Offset+d.X, _mStartPos.Y.Scale, _mStartPos.Y.Offset+d.Y)  
	end  
end)  
  
local guiVisible = true  
local function hideGui() main.Visible=false; mini.Visible=true; guiVisible=false end  
local function showGui() main.Visible=true; mini.Visible=false; guiVisible=true end  
  
-- Hide/Show GUI keybind listener (wired here so hideGui/showGui exist)  
UIS.InputBegan:Connect(function(inp,gpe)  
	if _anyKeyListening then return end  
	if inp.UserInputType==Enum.UserInputType.Keyboard then if gpe or UIS:GetFocusedTextBox() then return end elseif not isGamepadInput(inp) then return end  
	if not isBindableInput(inp) then return end  
	if kbMatch(KB.GuiHide,inp.KeyCode) then if guiVisible then hideGui() else showGui() end end  
end)  
  
minBtn.MouseButton1Click:Connect(hideGui)  
mini.MouseButton1Click:Connect(showGui)  
-- Bottom player bar — italozinHub style (rounded bottom corners)  
local footerFrame = Instance.new("Frame", main)  
footerFrame.Size = UDim2.new(1, 0, 0, 36)  
footerFrame.Position = UDim2.new(0, 0, 1, -36)  
footerFrame.BackgroundColor3 = BG2  
footerFrame.BorderSizePixel = 0  
footerFrame.ZIndex = 10  
Instance.new("UICorner", footerFrame).CornerRadius = UDim.new(0, CORNER)  
-- patch top corners of footerFrame square  
local botPatch = Instance.new("Frame", footerFrame)  
botPatch.Size = UDim2.new(1, 0, 0, CORNER)  
botPatch.Position = UDim2.new(0, 0, 0, 0)  
botPatch.BackgroundColor3 = BG2  
botPatch.BorderSizePixel = 0  
botPatch.ZIndex = 9  
local footerStroke = Instance.new("UIStroke", footerFrame)  
footerStroke.Color = BORDER; footerStroke.Thickness = 1  
footerStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border  
-- Divider line above footer  
local botDiv = Instance.new("Frame", main)  
botDiv.Size = UDim2.new(1, 0, 0, 1)  
botDiv.Position = UDim2.new(0, 0, 1, -36)  
botDiv.BackgroundColor3 = BORDER  
botDiv.BorderSizePixel = 0  
botDiv.ZIndex = 9  
  
local dotLbl = Instance.new("Frame", footerFrame)  
dotLbl.Size = UDim2.new(0, 8, 0, 8)  
dotLbl.Position = UDim2.new(0, 14, 0.5, -4)  
dotLbl.BackgroundColor3 = WHITE  
dotLbl.BorderSizePixel = 0  
dotLbl.ZIndex = 11  
Instance.new("UICorner", dotLbl).CornerRadius = UDim.new(1, 0)  
  
local userLbl = Instance.new("TextLabel", footerFrame)  
userLbl.Size = UDim2.new(1, -30, 1, 0)  
userLbl.Position = UDim2.new(0, 28, 0, 0)  
userLbl.BackgroundTransparency = 1  
userLbl.Text = LP.Name  
userLbl.TextColor3 = WHITE  
userLbl.Font = Enum.Font.GothamBold  
userLbl.TextSize = 11  
userLbl.TextXAlignment = Enum.TextXAlignment.Left  
userLbl.ZIndex = 11  
  
-- Shrink body to make room for footer  
body.Size = UDim2.new(1, 0, 1, -116)  
  
end -- end Combat do block  
  
do -- Misc page locals  
makePage("Misc") -- register right col  
local miscPage = Instance.new("ScrollingFrame", body)  
miscPage.Size = UDim2.new(1, 0, 1, 0); miscPage.Position = UDim2.new(0,0,0,0); miscPage.Visible = false; scrollPages["Misc"] = miscPage  
miscPage.BackgroundTransparency = 1  
miscPage.BorderSizePixel = 0  
miscPage.ScrollBarThickness = 3  
miscPage.ScrollBarImageColor3 = PURPLE  
miscPage.CanvasSize = UDim2.new(0, 0, 0, 0)  
miscPage.AutomaticCanvasSize = Enum.AutomaticSize.Y  
local mpLayout = Instance.new("UIListLayout", miscPage)  
mpLayout.Padding = UDim.new(0, 6)  
mpLayout.SortOrder = Enum.SortOrder.LayoutOrder  
local mpPad = Instance.new("UIPadding", miscPage)  
mpPad.PaddingTop = UDim.new(0, 10)  
mpPad.PaddingLeft = UDim.new(0, 10)  
mpPad.PaddingRight = UDim.new(0, 10)  
  
makeSecHdr(miscPage, "Utilities", 0)  
  
-- Grab Radius input rows: separate radius for Normal mode and Lagger mode.
-- Whichever mode is active determines which radius is actually used
-- (Steal.StealRadius is kept in sync via syncStealRadius()).
local function makeGrabRadiusRow(loOrder, labelTxt, getVal, setVal)  
	local row = Instance.new("Frame", miscPage)  
	row.Size = UDim2.new(1, 0, 0, 38)  
	row.BackgroundColor3 = CARD  
	row.BorderSizePixel = 0  
	row.LayoutOrder = loOrder  
	Instance.new("UICorner", row).CornerRadius = UDim.new(0, 8)
	Instance.new("UIStroke", row).Color = BORDER  
	local lbl = Instance.new("TextLabel", row)  
	lbl.Size = UDim2.new(0.55, 0, 1, 0)  
	lbl.Position = UDim2.new(0, 10, 0, 0)  
	lbl.BackgroundTransparency = 1  
	lbl.Text = labelTxt  
	lbl.TextColor3 = TEXT  
	lbl.Font = Enum.Font.GothamBold  
	lbl.TextSize = 11  
	lbl.TextXAlignment = Enum.TextXAlignment.Left  
	local box = Instance.new("TextBox", row)  
	box.Size = UDim2.new(0, 64, 0, 24)  
	box.Position = UDim2.new(1, -74, 0.5, -12)  
	box.BackgroundColor3 = Color3.fromRGB(10, 10, 10)  
	box.BorderSizePixel = 0  
	box.Text = tostring(getVal())  
	box.TextColor3 = WHITE  
	box.Font = Enum.Font.GothamBold  
	box.TextSize = 11  
	box.ClearTextOnFocus = false  
	Instance.new("UICorner", box).CornerRadius = UDim.new(0, 5)  
	Instance.new("UIStroke", box).Color = BORDER  
	box.FocusLost:Connect(function()  
		local v = tonumber(box.Text)  
		if v and v >= 5 and v <= 300 then  
			setVal(math.floor(v))  
			box.Text = tostring(getVal())  
			syncStealRadius()  
			saveConfig()  
		else box.Text = tostring(getVal()) end  
	end)  
	return box  
end  

grabRadiusBox      = makeGrabRadiusRow(1, "Grab Radius (Normal)", function() return Steal.StealRadiusNormal end, function(v) Steal.StealRadiusNormal=v end)  
grabRadiusLaggerBox = makeGrabRadiusRow(2, "Grab Radius (Lagger)", function() return Steal.StealRadiusLagger end, function(v) Steal.StealRadiusLagger=v end)  
  
-- Misc toggle rows  
local miscToggles = {  
	{label="Auto Steal",     lo=3, onToggle=function(on) Steal.AutoStealEnabled=on; if on then startAutoSteal() else stopAutoSteal() end end},  
	{label="Infinite Jump",  lo=4, onToggle=function(on) infJumpEnabled=on; if on then startInfiniteJump() else stopInfiniteJump() end end},  
	{label="Anti Ragdoll",   lo=5, onToggle=function(on) antiRagdollEnabled=on; if on then startAntiRagdoll() else stopAntiRagdoll() end end},  
	{label="Medusa Counter", lo=6, onToggle=function(on) medusaCounterEnabled=on; if on then setupMedusa(LP.Character) else for _,c in pairs(medusaConns) do pcall(function() c:Disconnect() end) end; medusaConns={} end end},  
	{label="Unwalk",         lo=7, onToggle=function(on) unwalkEnabled=on; if not on then enableAnimations() end end},  
}  
  
for _, t in ipairs(miscToggles) do  
	local row = Instance.new("Frame", miscPage)  
	row.Size = UDim2.new(1, 0, 0, 38)  
	row.BackgroundColor3 = CARD  
	row.BorderSizePixel = 0  
	row.LayoutOrder = t.lo  
	Instance.new("UICorner", row).CornerRadius = UDim.new(0, 8)
	Instance.new("UIStroke", row).Color = BORDER  
	local lbl = Instance.new("TextLabel", row)  
	lbl.Size = UDim2.new(0.6, 0, 1, 0)  
	lbl.Position = UDim2.new(0, 10, 0, 0)  
	lbl.BackgroundTransparency = 1  
	lbl.Text = t.label  
	lbl.TextColor3 = TEXT  
	lbl.Font = Enum.Font.GothamBold  
	lbl.TextSize = 11  
	lbl.TextXAlignment = Enum.TextXAlignment.Left  
	local pillBg3 = Instance.new("Frame", row)  
	pillBg3.Size = UDim2.new(0, 36, 0, 20)  
	pillBg3.Position = UDim2.new(1, -46, 0.5, -10)  
	pillBg3.BackgroundColor3 = Color3.fromRGB(38, 10, 26)  
	pillBg3.BorderSizePixel = 0  
	Instance.new("UICorner", pillBg3).CornerRadius = UDim.new(1, 0)  
	local dot3 = Instance.new("Frame", pillBg3)  
	dot3.Size = UDim2.new(0, 14, 0, 14)  
	dot3.Position = UDim2.new(0, 3, 0.5, -7)  
	dot3.BackgroundColor3 = Color3.fromRGB(85, 85, 104)  
	dot3.BorderSizePixel = 0  
	Instance.new("UICorner", dot3).CornerRadius = UDim.new(1, 0)  
	local isOn = false  
	local clk = Instance.new("TextButton", row)  
	clk.Size = UDim2.new(1, 0, 1, 0)  
	clk.BackgroundTransparency = 1  
	clk.Text = ""  
	clk.ZIndex = 5  
	clk.MouseButton1Click:Connect(function()  
		isOn = not isOn  
		TweenService:Create(pillBg3, TweenInfo.new(0.18), {BackgroundColor3 = isOn and PINK or Color3.fromRGB(38,10,26)}):Play()  
		TweenService:Create(dot3, TweenInfo.new(0.18, Enum.EasingStyle.Back), {  
			Position = isOn and UDim2.new(1,-17,0.5,-7) or UDim2.new(0,3,0.5,-7),  
			BackgroundColor3 = isOn and WHITE or Color3.fromRGB(85,85,104)  
		}):Play()  
		t.onToggle(isOn)  
		saveConfig()  
	end)  
	miscSetVisuals[t.label] = function(on)  
		isOn = on  
		TweenService:Create(pillBg3, TweenInfo.new(0.18), {BackgroundColor3 = on and PINK or Color3.fromRGB(38,10,26)}):Play()  
		TweenService:Create(dot3, TweenInfo.new(0.18, Enum.EasingStyle.Back), {  
			Position = on and UDim2.new(1,-17,0.5,-7) or UDim2.new(0,3,0.5,-7),  
			BackgroundColor3 = on and WHITE or Color3.fromRGB(85,85,104)  
		}):Play()  
	end  
end  
  



-- italozin Reset row (below Unwalk, lo=8)
do
	local row = Instance.new("Frame", miscPage)
	row.Size = UDim2.new(1, 0, 0, 38); row.BackgroundColor3 = CARD
	row.BorderSizePixel = 0; row.LayoutOrder = 8
	Instance.new("UICorner", row).CornerRadius = UDim.new(0, 8)
	Instance.new("UIStroke", row).Color = BORDER
	local lbl = Instance.new("TextLabel", row)
	lbl.Size = UDim2.new(0.55, 0, 1, 0); lbl.Position = UDim2.new(0, 10, 0, 0)
	lbl.BackgroundTransparency = 1; lbl.Text = "italozin Reset"
	lbl.TextColor3 = TEXT; lbl.Font = Enum.Font.GothamBold
	lbl.TextSize = 11; lbl.TextXAlignment = Enum.TextXAlignment.Left
	-- Keybind box (keyboard + controller)
	local kbBox = Instance.new("TextButton", row)
	kbBox.Size = UDim2.new(0, 46, 0, 26)
	kbBox.Position = UDim2.new(1, -56, 0.5, -13)
	kbBox.BackgroundColor3 = Color3.fromRGB(10,10,10)
	kbBox.BorderSizePixel = 0
	kbBox.Text = (KB.BlossomReset.gp and KB.BlossomReset.gp.Name) or (KB.BlossomReset.kb and KB.BlossomReset.kb.Name) or "R"
	kbBox.TextColor3 = WHITE; kbBox.Font = Enum.Font.GothamBold
	kbBox.TextSize = 11; kbBox.ZIndex = 5
	Instance.new("UICorner", kbBox).CornerRadius = UDim.new(0, 5)
	Instance.new("UIStroke", kbBox).Color = BORDER
	KB.BlossomReset._setBox = function(name) kbBox.Text = name end
	local listening = false; local conn
	kbBox.MouseButton1Click:Connect(function()
		if listening then return end
		listening = true; kbBox.Text = "..."; kbBox.TextColor3 = PINK
		_anyKeyListening = true
		if conn then conn:Disconnect() end
		conn = UIS.InputBegan:Connect(function(inp)
			if not listening then return end
			local isGpad = inp.UserInputType.Name:match("^Gamepad") ~= nil
			if not isGpad and inp.UserInputType ~= Enum.UserInputType.Keyboard then return end
			if inp.KeyCode == Enum.KeyCode.Escape then
				listening = false; _anyKeyListening = false
				kbBox.Text = (KB.BlossomReset.gp and KB.BlossomReset.gp.Name) or (KB.BlossomReset.kb and KB.BlossomReset.kb.Name) or "R"
				kbBox.TextColor3 = WHITE
				if conn then conn:Disconnect(); conn = nil end
				return
			end
			if isGpad then KB.BlossomReset.gp = inp.KeyCode; KB.BlossomReset.kb = nil
			else KB.BlossomReset.kb = inp.KeyCode; KB.BlossomReset.gp = nil end
			kbBox.Text = inp.KeyCode.Name; kbBox.TextColor3 = WHITE
			listening = false; _anyKeyListening = false
			if conn then conn:Disconnect(); conn = nil end
			pcall(saveConfig)
		end)
	end)
end

-- Auto TP Down (message 22) in Misc tab
do
	local row = Instance.new("Frame", miscPage)
	row.Size = UDim2.new(1, 0, 0, 38); row.BackgroundColor3 = CARD
	row.BorderSizePixel = 0; row.LayoutOrder = 10
	Instance.new("UICorner", row).CornerRadius = UDim.new(0, 8)
	Instance.new("UIStroke", row).Color = BORDER
	local lbl = Instance.new("TextLabel", row)
	lbl.Size = UDim2.new(0.6, 0, 1, 0); lbl.Position = UDim2.new(0, 10, 0, 0)
	lbl.BackgroundTransparency = 1; lbl.Text = "Auto TP Down"
	lbl.TextColor3 = TEXT; lbl.Font = Enum.Font.GothamBold
	lbl.TextSize = 11; lbl.TextXAlignment = Enum.TextXAlignment.Left
	local heightBox = Instance.new("TextBox", row)
	heightBox.Size = UDim2.new(0, 36, 0, 22); heightBox.Position = UDim2.new(1, -92, 0.5, -11)
	heightBox.BackgroundColor3 = Color3.fromRGB(10, 10, 10); heightBox.BorderSizePixel = 0
	heightBox.Text = tostring(autoTPHeight); heightBox.TextColor3 = WHITE
	heightBox.Font = Enum.Font.GothamBold; heightBox.TextSize = 9; heightBox.ZIndex = 5
	heightBox.ClearTextOnFocus = false
	Instance.new("UICorner", heightBox).CornerRadius = UDim.new(0, 5)
	Instance.new("UIStroke", heightBox).Color = BORDER
	heightBox.FocusLost:Connect(function()
		local v = tonumber(heightBox.Text)
		if v then autoTPHeight = v else heightBox.Text = tostring(autoTPHeight) end
		saveConfig()
	end)
	_setAutoTPHeightBox = function(v) heightBox.Text = tostring(v) end
	local pillBg = Instance.new("Frame", row)
	pillBg.Size = UDim2.new(0, 36, 0, 20); pillBg.Position = UDim2.new(1, -46, 0.5, -10)
	pillBg.BackgroundColor3 = Color3.fromRGB(38, 10, 26); pillBg.BorderSizePixel = 0
	Instance.new("UICorner", pillBg).CornerRadius = UDim.new(1, 0)
	local dot = Instance.new("Frame", pillBg)
	dot.Size = UDim2.new(0, 14, 0, 14); dot.Position = UDim2.new(0, 3, 0.5, -7)
	dot.BackgroundColor3 = Color3.fromRGB(160, 160, 180); dot.BorderSizePixel = 0
	Instance.new("UICorner", dot).CornerRadius = UDim.new(1, 0)
	local isOn = false
	local function setV(on)
		isOn = on
		TweenService:Create(pillBg, TweenInfo.new(0.18), {BackgroundColor3 = on and PINK or Color3.fromRGB(38,10,26)}):Play()
		TweenService:Create(dot, TweenInfo.new(0.18, Enum.EasingStyle.Back), {
			Position = on and UDim2.new(1,-17,0.5,-7) or UDim2.new(0,3,0.5,-7),
			BackgroundColor3 = on and WHITE or Color3.fromRGB(160,160,180)
		}):Play()
	end
	setAutoTPVisual = setV
	-- clickable overlay on the pill (message 22 style)
	local clk = Instance.new("TextButton", pillBg)
	clk.Size = UDim2.new(1, 0, 1, 0); clk.BackgroundTransparency = 1; clk.Text = ""; clk.ZIndex = 6
	clk.Activated:Connect(function()
		isOn = not isOn; setV(isOn)
		autoTPEnabled = isOn
		if isOn then startAutoTP() else stopAutoTP() end
		saveConfig()
	end)
	-- also allow tapping the label side
	local rowClk = Instance.new("TextButton", row)
	rowClk.Size = UDim2.new(0.6, 0, 1, 0); rowClk.BackgroundTransparency = 1; rowClk.Text = ""; rowClk.ZIndex = 4
	rowClk.Activated:Connect(function()
		isOn = not isOn; setV(isOn)
		autoTPEnabled = isOn
		if isOn then startAutoTP() else stopAutoTP() end
		saveConfig()
	end)
end

-- Anti Kick row (message 27) in Misc tab
do
	local akSideLbl = nil
	local row = Instance.new("Frame", miscPage)
	row.Size = UDim2.new(1, 0, 0, 38); row.BackgroundColor3 = CARD
	row.BorderSizePixel = 0; row.LayoutOrder = 11
	Instance.new("UICorner", row).CornerRadius = UDim.new(0, 8)
	Instance.new("UIStroke", row).Color = BORDER
	local lbl = Instance.new("TextLabel", row)
	lbl.Size = UDim2.new(0.45, 0, 1, 0); lbl.Position = UDim2.new(0, 10, 0, 0)
	lbl.BackgroundTransparency = 1; lbl.Text = "Anti Kick"
	lbl.TextColor3 = TEXT; lbl.Font = Enum.Font.GothamBold
	lbl.TextSize = 11; lbl.TextXAlignment = Enum.TextXAlignment.Left
	-- Side toggle button
	local sideBtn = Instance.new("TextButton", row)
	sideBtn.Size = UDim2.new(0, 52, 0, 22); sideBtn.Position = UDim2.new(1, -106, 0.5, -11)
	sideBtn.BackgroundColor3 = Color3.fromRGB(50, 12, 35); sideBtn.BorderSizePixel = 0
	sideBtn.Text = "LEFT"; sideBtn.TextColor3 = PINK
	sideBtn.Font = Enum.Font.GothamBold; sideBtn.TextSize = 10; sideBtn.ZIndex = 6
	Instance.new("UICorner", sideBtn).CornerRadius = UDim.new(0, 5)
	Instance.new("UIStroke", sideBtn).Color = PINK
	sideBtn.MouseButton1Click:Connect(function()
		antiKickSide = antiKickSide == "left" and "right" or "left"
		sideBtn.Text = antiKickSide == "left" and "LEFT" or "RIGHT"
	end)
	-- Toggle pill
	local pillBg = Instance.new("Frame", row)
	pillBg.Size = UDim2.new(0, 36, 0, 20); pillBg.Position = UDim2.new(1, -46, 0.5, -10)
	pillBg.BackgroundColor3 = Color3.fromRGB(38, 10, 26); pillBg.BorderSizePixel = 0
	Instance.new("UICorner", pillBg).CornerRadius = UDim.new(1, 0)
	local dot = Instance.new("Frame", pillBg)
	dot.Size = UDim2.new(0, 14, 0, 14); dot.Position = UDim2.new(0, 3, 0.5, -7)
	dot.BackgroundColor3 = Color3.fromRGB(85, 85, 104); dot.BorderSizePixel = 0
	Instance.new("UICorner", dot).CornerRadius = UDim.new(1, 0)
	local isOn = false
	local function setAK(on)
		isOn = on
		TS:Create(pillBg, TweenInfo.new(0.18), {BackgroundColor3 = on and PINK or Color3.fromRGB(38,10,26)}):Play()
		TS:Create(dot, TweenInfo.new(0.18, Enum.EasingStyle.Back), {
			Position = on and UDim2.new(1,-17,0.5,-7) or UDim2.new(0,3,0.5,-7),
			BackgroundColor3 = on and WHITE or Color3.fromRGB(85,85,104)
		}):Play()
		antiKickEnabled = on
		if on then startAntiKick() else stopAntiKick() end
	end
	local clk = Instance.new("TextButton", pillBg)
	clk.Size = UDim2.new(1, 0, 1, 0); clk.BackgroundTransparency = 1; clk.Text = ""; clk.ZIndex = 6
	clk.Activated:Connect(function() setAK(not isOn) end)
	local rowClk = Instance.new("TextButton", row)
	rowClk.Size = UDim2.new(0.45, 0, 1, 0); rowClk.BackgroundTransparency = 1; rowClk.Text = ""; rowClk.ZIndex = 4
	rowClk.Activated:Connect(function() setAK(not isOn) end)
end

end -- end Misc do block  
  
  
do -- Move Logic + Move page locals  
-- ─── Move Logic ───────────────────────────────────────────────────────────────  
autoLeftEnabled  = false  
autoRightEnabled = false  
local alConn,arConn = nil,nil  
local alPhase,arPhase = 1,1  
  
local AP_L1     = Vector3.new(-476.48, -6.28,  92.73)  
local AP_L2     = Vector3.new(-483.12, -4.95,  94.80)  
local AP_L_FACE = Vector3.new(-482.25, -4.96,  92.09)  
local AP_R1     = Vector3.new(-476.16, -6.52,  25.62)  
local AP_R2     = Vector3.new(-483.06, -5.03,  25.48)  
local AP_R_FACE = Vector3.new(-482.06, -6.93,  35.47)  
  
function stopAutoLeft()  
	if alConn then alConn:Disconnect();alConn=nil end;alPhase=1  
	local char=LP.Character;if char then local h=char:FindFirstChildOfClass("Humanoid");if h then h:Move(Vector3.zero,false) end end  
end  
function stopAutoRight()  
	if arConn then arConn:Disconnect();arConn=nil end;arPhase=1  
	local char=LP.Character;if char then local h=char:FindFirstChildOfClass("Humanoid");if h then h:Move(Vector3.zero,false) end end  
end  
function startAutoLeft()  
	if alConn then alConn:Disconnect() end;alPhase=1  
	alConn=RunService.Heartbeat:Connect(function()  
		if not autoLeftEnabled then return end  
		local char=LP.Character;if not char then return end  
		local hrp=char:FindFirstChild("HumanoidRootPart")  
		local hum=char:FindFirstChildOfClass("Humanoid")  
		if not hrp or not hum then return end  
		local spd=(activeSpeedMode=="lagger") and LS or NS  
		if alPhase==1 then  
			local tgt=Vector3.new(AP_L1.X,hrp.Position.Y,AP_L1.Z)  
			if (tgt-hrp.Position).Magnitude<1 then  
				alPhase=2  
				local d=AP_L2-hrp.Position;local mv=Vector3.new(d.X,0,d.Z).Unit  
				hum:Move(mv,false);hrp.AssemblyLinearVelocity=Vector3.new(mv.X*spd,hrp.AssemblyLinearVelocity.Y,mv.Z*spd);return  
			end  
			local d=AP_L1-hrp.Position;local mv=Vector3.new(d.X,0,d.Z).Unit  
			hum:Move(mv,false);hrp.AssemblyLinearVelocity=Vector3.new(mv.X*spd,hrp.AssemblyLinearVelocity.Y,mv.Z*spd)  
		elseif alPhase==2 then  
			local tgt=Vector3.new(AP_L2.X,hrp.Position.Y,AP_L2.Z)  
			if (tgt-hrp.Position).Magnitude<1 then  
				hum:Move(Vector3.zero,false);hrp.AssemblyLinearVelocity=Vector3.zero  
				autoLeftEnabled=false;if alConn then alConn:Disconnect();alConn=nil end  
				alPhase=1;if autoLeftSetVisual then autoLeftSetVisual(false) end  
				if (AP_L_FACE-hrp.Position).Magnitude>0.01 then  
					hrp.CFrame=CFrame.new(hrp.Position,Vector3.new(AP_L_FACE.X,hrp.Position.Y,AP_L_FACE.Z))  
				end;return  
			end  
			local d=AP_L2-hrp.Position;local mv=Vector3.new(d.X,0,d.Z).Unit  
			hum:Move(mv,false);hrp.AssemblyLinearVelocity=Vector3.new(mv.X*spd,hrp.AssemblyLinearVelocity.Y,mv.Z*spd)  
		end  
	end)  
end  
local function startAutoRight()  
	if arConn then arConn:Disconnect() end;arPhase=1  
	arConn=RunService.Heartbeat:Connect(function()  
		if not autoRightEnabled then return end  
		local char=LP.Character;if not char then return end  
		local hrp=char:FindFirstChild("HumanoidRootPart")  
		local hum=char:FindFirstChildOfClass("Humanoid")  
		if not hrp or not hum then return end  
		local spd=(activeSpeedMode=="lagger") and LS or NS  
		if arPhase==1 then  
			local tgt=Vector3.new(AP_R1.X,hrp.Position.Y,AP_R1.Z)  
			if (tgt-hrp.Position).Magnitude<1 then  
				arPhase=2  
				local d=AP_R2-hrp.Position;local mv=Vector3.new(d.X,0,d.Z).Unit  
				hum:Move(mv,false);hrp.AssemblyLinearVelocity=Vector3.new(mv.X*spd,hrp.AssemblyLinearVelocity.Y,mv.Z*spd);return  
			end  
			local d=AP_R1-hrp.Position;local mv=Vector3.new(d.X,0,d.Z).Unit  
			hum:Move(mv,false);hrp.AssemblyLinearVelocity=Vector3.new(mv.X*spd,hrp.AssemblyLinearVelocity.Y,mv.Z*spd)  
		elseif arPhase==2 then  
			local tgt=Vector3.new(AP_R2.X,hrp.Position.Y,AP_R2.Z)  
			if (tgt-hrp.Position).Magnitude<1 then  
				hum:Move(Vector3.zero,false);hrp.AssemblyLinearVelocity=Vector3.zero  
				autoRightEnabled=false;if arConn then arConn:Disconnect();arConn=nil end  
				arPhase=1;if autoRightSetVisual then autoRightSetVisual(false) end  
				if (AP_R_FACE-hrp.Position).Magnitude>0.01 then  
					hrp.CFrame=CFrame.new(hrp.Position,Vector3.new(AP_R_FACE.X,hrp.Position.Y,AP_R_FACE.Z))  
				end;return  
			end  
			local d=AP_R2-hrp.Position;local mv=Vector3.new(d.X,0,d.Z).Unit  
			hum:Move(mv,false);hrp.AssemblyLinearVelocity=Vector3.new(mv.X*spd,hrp.AssemblyLinearVelocity.Y,mv.Z*spd)  
		end  
	end)  
end  
  
local function runTPDown()  
	local char=LP.Character;if not char then return end  
	local root=char:FindFirstChild("HumanoidRootPart")  
	local hum=char:FindFirstChildOfClass("Humanoid")  
	if not root or not hum then return end  
	local rp=RaycastParams.new()  
	rp.FilterDescendantsInstances={char}  
	rp.FilterType=Enum.RaycastFilterType.Exclude  
	local result=workspace:Raycast(root.Position,Vector3.new(0,-1000,0),rp)  
	if result then  
		local hipOffset=hum.HipHeight+(root.Size.Y/2)  
		root.CFrame=CFrame.new(result.Position+Vector3.new(0,hipOffset,0))*root.CFrame.Rotation  
	end  
end  
  
-- Drop logic (full Dark Hub drop)  
local _dhDropConns={}  
local _dhDropState=false  
local _dhDropPulseThread=nil  
local _dhLastDrop=0  
local DROP_WALKFLING_SEC=0.15  
local DROP_WALKFLING_AIR_EXTRA=0.12  
  
local function _dhWalkfling(enabled,dropOpts)  
	dropOpts=dropOpts or {}  
	local walkflinging=false  
	if enabled then  
		_dhDropState=true;walkflinging=true  
		local airBoost=dropOpts.airBoost==true  
		local safer=dropOpts.saferPhysics==true  
		local velMult=safer and 520 or 10000  
		local spikeBase=safer and 2100 or 10000  
		local spikeAirScale=safer and 52 or 120  
		local spikeAirMax=safer and 6000 or 14000  
		local downPullBase=safer and 92 or 120  
		local downPullAirMul=safer and 1.65 or 2.2  
		local downPullAirCap=safer and 360 or 520  
		local floorAir=safer and -78 or -95  
		local floorGround=safer and -58 or -65  
		local floorUpwardBias=safer and -58 or -65  
		local floorUpwardScale=safer and 0.68 or 0.85  
		local floorUpwardMax=safer and 155 or 220  
		local oscAmp=safer and (airBoost and 0.14 or 0.07) or (airBoost and 0.24 or 0.1)  
		local char0=LP.Character  
		local hum0=char0 and char0:FindFirstChildOfClass("Humanoid")  
		if hum0 then  
			local dc=hum0.Died:Connect(function() walkflinging=false end)  
			table.insert(_dhDropConns,dc)  
		end  
		local flingThread=task.spawn(function()  
			local movel=oscAmp  
			repeat  
				RunService.Heartbeat:Wait()  
				if not _dhDropState then break end  
				local character=LP.Character  
				local root=character and character:FindFirstChild("HumanoidRootPart")  
				while not(character and character.Parent and root and root.Parent) do  
					RunService.Heartbeat:Wait()  
					if not _dhDropState then break end  
					character=LP.Character; root=character and character:FindFirstChild("HumanoidRootPart")  
				end  
				if not _dhDropState then break end  
				local vel=root.AssemblyLinearVelocity  
				local upward=math.max(0,vel.Y)  
				local spikeY=spikeBase+(airBoost and math.clamp(upward*spikeAirScale,0,spikeAirMax) or 0)  
				root.AssemblyLinearVelocity=vel*velMult+Vector3.new(0,spikeY,0)  
				RunService.RenderStepped:Wait()  
				if character and character.Parent and root and root.Parent then  
					local downPull=downPullBase+(airBoost and math.clamp(upward*downPullAirMul,0,downPullAirCap) or 0)  
					local floorCap=airBoost and floorAir or floorGround  
					if upward>1 then floorCap=math.min(floorCap,floorUpwardBias-math.min(upward*floorUpwardScale,floorUpwardMax)) end  
					root.AssemblyLinearVelocity=Vector3.new(vel.X,math.min(vel.Y-downPull,floorCap),vel.Z)  
				end  
				RunService.Stepped:Wait()  
				if character and character.Parent and root and root.Parent then  
					root.AssemblyLinearVelocity=vel+Vector3.new(0,movel,0); movel=movel*-1  
				end  
			until walkflinging==false or not _dhDropState  
		end)  
		table.insert(_dhDropConns,flingThread)  
	else  
		walkflinging=false;_dhDropState=false  
		for _,conn in ipairs(_dhDropConns) do  
			if conn then  
				if typeof(conn)=="RBXScriptConnection" then conn:Disconnect()  
				elseif typeof(conn)=="thread" then pcall(task.cancel,conn) end  
			end  
		end  
		_dhDropConns={}  
	end  
end  
  
local function _dhAirBoost(root,hum)  
	if not root or not hum then return false end  
	if root.AssemblyLinearVelocity.Y>12 then return true end  
	if hum.FloorMaterial==Enum.Material.Air then return true end  
	local st=hum:GetState()  
	return st==Enum.HumanoidStateType.Jumping or st==Enum.HumanoidStateType.Freefall  
end  
  
-- ─── End Move Logic ─────────────────────────────────────────────    



-- ===== MOVE PAGE =====  
makePage("Move") -- register right col  
local movePage = Instance.new("ScrollingFrame", body)  
movePage.Size = UDim2.new(1, 0, 1, 0); movePage.Position = UDim2.new(0,0,0,0); movePage.Visible = false; scrollPages["Move"] = movePage  
movePage.BackgroundTransparency = 1  
movePage.BorderSizePixel = 0  
movePage.ScrollBarThickness = 3  
movePage.ScrollBarImageColor3 = PURPLE  
movePage.CanvasSize = UDim2.new(0, 0, 0, 0)  
movePage.AutomaticCanvasSize = Enum.AutomaticSize.Y  
local mvLayout = Instance.new("UIListLayout", movePage)  
mvLayout.Padding = UDim.new(0, 6)  
mvLayout.SortOrder = Enum.SortOrder.LayoutOrder  
local mvPad = Instance.new("UIPadding", movePage)  
mvPad.PaddingTop = UDim.new(0, 10)  
mvPad.PaddingLeft = UDim.new(0, 10)  
mvPad.PaddingRight = UDim.new(0, 10)  
  
-- Keybind row helper for move tab  
local function makeMoveKbRow(parent, labelTxt, defaultKey, lo, onActivate, kbEntry)  
	local row = Instance.new("Frame", parent)  
	row.Size = UDim2.new(1, 0, 0, 38)  
	row.BackgroundColor3 = CARD  
	row.BorderSizePixel = 0  
	row.LayoutOrder = lo  
	Instance.new("UICorner", row).CornerRadius = UDim.new(0, 8)
	Instance.new("UIStroke", row).Color = BORDER  
	local lbl = Instance.new("TextLabel", row)  
	lbl.Size = UDim2.new(0.6, 0, 1, 0)  
	lbl.Position = UDim2.new(0, 10, 0, 0)  
	lbl.BackgroundTransparency = 1  
	lbl.Text = labelTxt  
	lbl.TextColor3 = TEXT  
	lbl.Font = Enum.Font.GothamBold  
	lbl.TextSize = 11  
	lbl.TextXAlignment = Enum.TextXAlignment.Left  
	local keyEntry = {key = defaultKey}  
	local box = Instance.new("TextButton", row)  
	box.Size = UDim2.new(0, 36, 0, 26)  
	box.Position = UDim2.new(1, -46, 0.5, -13)  
	box.BackgroundColor3 = Color3.fromRGB(10, 10, 10)  
	box.BorderSizePixel = 0  
	box.Text = defaultKey.Name  
	box.TextColor3 = WHITE  
	box.Font = Enum.Font.GothamBold  
	box.TextSize = 11  
	box.ZIndex = 5  
	Instance.new("UICorner", box).CornerRadius = UDim.new(0, 5)  
	Instance.new("UIStroke", box).Color = BORDER  
	local listening = false  
	local kbC  
	box.MouseButton1Click:Connect(function()  
		if listening then return end  
		listening = true; box.Text = "..."; box.TextColor3 = PINK  
		if kbC then kbC:Disconnect() end  
		kbC = UserInputService.InputBegan:Connect(function(inp)  
			if not isBindableInput(inp) then return end  
			if inp.UserInputType==Enum.UserInputType.Keyboard and UIS:GetFocusedTextBox() then return end  
			if true then  
				if inp.KeyCode == Enum.KeyCode.Escape then  
					listening = false; box.Text = keyEntry.key.Name; box.TextColor3 = WHITE  
					if kbC then kbC:Disconnect(); kbC = nil end; return  
				end  
				keyEntry.key = inp.KeyCode; if kbEntry then kbEntry.kb = inp.KeyCode end  
				box.Text = inp.KeyCode.Name; box.TextColor3 = WHITE  
				listening = false; if kbC then kbC:Disconnect(); kbC = nil end  
				saveConfig()  
			end  
		end)  
	end)  
	UIS.InputBegan:Connect(function(inp,gpe)  
		if _anyKeyListening or listening then return end  
		if inp.UserInputType==Enum.UserInputType.Keyboard then if gpe or UIS:GetFocusedTextBox() then return end elseif not isGamepadInput(inp) then return end  
		if not isBindableInput(inp) then return end  
		if kbMatch(keyEntry,inp.KeyCode) or inp.KeyCode==keyEntry.key then onActivate() end  
	end)
	keyEntry.setBox = function(kc) box.Text = kc.Name end  
	return keyEntry  
end  
  
-- Toggle row helper for move tab (Auto Left / Auto Right)  
local function makeMoveToggleRow(parent, labelTxt, defaultKey, lo, onToggle, kbEntry)  
	local row = Instance.new("Frame", parent)  
	row.Size = UDim2.new(1, 0, 0, 38)  
	row.BackgroundColor3 = CARD  
	row.BorderSizePixel = 0  
	row.LayoutOrder = lo  
	Instance.new("UICorner", row).CornerRadius = UDim.new(0, 8)
	Instance.new("UIStroke", row).Color = BORDER  
	local lbl = Instance.new("TextLabel", row)  
	lbl.Size = UDim2.new(0.45, 0, 1, 0)  
	lbl.Position = UDim2.new(0, 10, 0, 0)  
	lbl.BackgroundTransparency = 1  
	lbl.Text = labelTxt  
	lbl.TextColor3 = TEXT  
	lbl.Font = Enum.Font.GothamBold  
	lbl.TextSize = 11  
	lbl.TextXAlignment = Enum.TextXAlignment.Left  
	local keyEntry = {key = defaultKey}  
	local kbBox = Instance.new("TextButton", row)  
	kbBox.Size = UDim2.new(0, 36, 0, 26)  
	kbBox.Position = UDim2.new(1, -92, 0.5, -13)  
	kbBox.BackgroundColor3 = Color3.fromRGB(10, 10, 10)  
	kbBox.BorderSizePixel = 0  
	kbBox.Text = defaultKey.Name  
	kbBox.TextColor3 = WHITE  
	kbBox.Font = Enum.Font.GothamBold  
	kbBox.TextSize = 11  
	kbBox.ZIndex = 10  
	Instance.new("UICorner", kbBox).CornerRadius = UDim.new(0, 5)  
	Instance.new("UIStroke", kbBox).Color = BORDER  
	local pillBg4 = Instance.new("Frame", row)  
	pillBg4.Size = UDim2.new(0, 36, 0, 20)  
	pillBg4.Position = UDim2.new(1, -46, 0.5, -10)  
	pillBg4.BackgroundColor3 = Color3.fromRGB(38, 10, 26)  
	pillBg4.BorderSizePixel = 0  
	Instance.new("UICorner", pillBg4).CornerRadius = UDim.new(1, 0)  
	local dot4 = Instance.new("Frame", pillBg4)  
	dot4.Size = UDim2.new(0, 14, 0, 14)  
	dot4.Position = UDim2.new(0, 3, 0.5, -7)  
	dot4.BackgroundColor3 = Color3.fromRGB(160, 160, 180)  
	dot4.BorderSizePixel = 0  
	Instance.new("UICorner", dot4).CornerRadius = UDim.new(1, 0)  
	local isOn = false  
	local function setV(on)  
		isOn = on  
		TweenService:Create(pillBg4, TweenInfo.new(0.18), {BackgroundColor3 = on and PINK or Color3.fromRGB(38,10,26)}):Play()  
		TweenService:Create(dot4, TweenInfo.new(0.18, Enum.EasingStyle.Back), {  
			Position = on and UDim2.new(1,-17,0.5,-7) or UDim2.new(0,3,0.5,-7),  
			BackgroundColor3 = on and WHITE or Color3.fromRGB(160,160,180)  
		}):Play()  
	end  
	local listening = false; local kbC  
	kbBox.MouseButton1Click:Connect(function()  
		if listening then return end  
		listening = true; kbBox.Text = "..."; kbBox.TextColor3 = PINK  
		if kbC then kbC:Disconnect() end  
		kbC = UserInputService.InputBegan:Connect(function(inp)  
			if not isBindableInput(inp) then return end  
			if inp.UserInputType==Enum.UserInputType.Keyboard and UIS:GetFocusedTextBox() then return end  
			if true then  
				if inp.KeyCode == Enum.KeyCode.Escape then  
					listening = false; kbBox.Text = keyEntry.key.Name; kbBox.TextColor3 = WHITE  
					if kbC then kbC:Disconnect(); kbC = nil end; return  
				end  
				if isGamepadInput(inp) then keyEntry.gp=inp.KeyCode;keyEntry.kb=nil;if kbEntry then kbEntry.gp=inp.KeyCode;kbEntry.kb=nil end else keyEntry.kb=inp.KeyCode;keyEntry.gp=nil;if kbEntry then kbEntry.kb=inp.KeyCode;kbEntry.gp=nil end end  
				keyEntry.key = inp.KeyCode; kbBox.Text = inp.KeyCode.Name; kbBox.TextColor3 = WHITE  
				listening = false; if kbC then kbC:Disconnect(); kbC = nil end  
				saveConfig()  
			end  
		end)  
	end)  
	local clk = Instance.new("TextButton", row)  
	clk.Size = UDim2.new(0.45, 0, 1, 0)  
	clk.Position = UDim2.new(0, 0, 0, 0)  
	clk.BackgroundTransparency = 1; clk.Text = ""; clk.ZIndex = 4  
	clk.MouseButton1Click:Connect(function()  
		isOn = not isOn; setV(isOn); onToggle(isOn)  
	end)  
	UIS.InputBegan:Connect(function(inp,gpe)  
		if _anyKeyListening or listening then return end  
		if inp.UserInputType==Enum.UserInputType.Keyboard then if gpe or UIS:GetFocusedTextBox() then return end elseif not isGamepadInput(inp) then return end  
		if not isBindableInput(inp) then return end  
		if kbMatch(keyEntry,inp.KeyCode) or inp.KeyCode==keyEntry.key then isOn=not isOn;setV(isOn);onToggle(isOn) end  
	end)  
	keyEntry.setBox = function(kc) kbBox.Text = kc.Name end  
	return setV, keyEntry  
end  
  
-- TELEPORT section  
makeSecHdr(movePage, "Teleport", 1)  
_kbTPDown = makeMoveKbRow(movePage, "TP Down", Enum.KeyCode.F, 2, function() runTPDown() end, KB.TPFloor)  


  
-- DROP section  
makeSecHdr(movePage, "Drop", 3)  
_kbDrop = makeMoveKbRow(movePage, "Drop Brainrot", Enum.KeyCode.X, 4, function() runDrop() end, KB.DropBrainrot)

-- AUTO section  
autoLeftSetVisual, _kbAutoLeft = makeMoveToggleRow(movePage, "Auto Left", Enum.KeyCode.Z, 6, function(on)  
	autoLeftEnabled = on  
	if on then  
		if autoRightEnabled then autoRightEnabled=false; if autoRightSetVisual then autoRightSetVisual(false) end; stopAutoRight() end  
		if autoBatEnabled then autoBatEnabled=false; if autoBatSetVisual then autoBatSetVisual(false) end; stopBatAimbot() end  
		startAutoLeft()  
	else stopAutoLeft() end  
end, KB.AutoLeft)  
autoRightSetVisual, _kbAutoRight = makeMoveToggleRow(movePage, "Auto Right", Enum.KeyCode.C, 7, function(on)  
	autoRightEnabled = on  
	if on then  
		if autoLeftEnabled then autoLeftEnabled=false; if autoLeftSetVisual then autoLeftSetVisual(false) end; stopAutoLeft() end  
		if autoBatEnabled then autoBatEnabled=false; if autoBatSetVisual then autoBatSetVisual(false) end; stopBatAimbot() end  
		startAutoRight()  
	else stopAutoRight() end  
end, KB.AutoRight)  
end -- end Move do block  
  
-- ===== PERFORMANCE PAGE =====  
do  
	makePage("Performance")  
	local perfPage = Instance.new("ScrollingFrame", body)  
	perfPage.Size = UDim2.new(1, 0, 1, 0); perfPage.Position = UDim2.new(0,0,0,0)  
	perfPage.Visible = false; perfPage.BackgroundTransparency = 1  
	perfPage.BorderSizePixel = 0; perfPage.ScrollBarThickness = 3  
	perfPage.ScrollBarImageColor3 = PURPLE; perfPage.CanvasSize = UDim2.new(0,0,0,0)  
	perfPage.AutomaticCanvasSize = Enum.AutomaticSize.Y  
	scrollPages["Performance"] = perfPage  
	local ppLL = Instance.new("UIListLayout", perfPage)  
	ppLL.Padding = UDim.new(0,6); ppLL.SortOrder = Enum.SortOrder.LayoutOrder  
	local ppPad = Instance.new("UIPadding", perfPage)  
	ppPad.PaddingTop=UDim.new(0,10); ppPad.PaddingLeft=UDim.new(0,10); ppPad.PaddingRight=UDim.new(0,10)  
  
	local function makePerfToggle(labelTxt, lo_val, onToggle)  
		local row = Instance.new("Frame", perfPage)  
		row.Size=UDim2.new(1,0,0,38); row.BackgroundColor3=CARD  
		row.BorderSizePixel=0; row.LayoutOrder=lo_val  
		Instance.new("UICorner", row).CornerRadius = UDim.new(0, 8)
		Instance.new("UIStroke",row).Color=BORDER  
		local lbl = Instance.new("TextLabel",row)  
		lbl.Size=UDim2.new(0.6,0,1,0); lbl.Position=UDim2.new(0,10,0,0)  
		lbl.BackgroundTransparency=1; lbl.Text=labelTxt  
		lbl.TextColor3=TEXT; lbl.Font=Enum.Font.GothamBold  
		lbl.TextSize=11; lbl.TextXAlignment=Enum.TextXAlignment.Left  
		local pill = Instance.new("Frame",row)  
		pill.Size=UDim2.new(0,36,0,20); pill.Position=UDim2.new(1,-46,0.5,-10)  
		pill.BackgroundColor3=Color3.fromRGB(38,10,26); pill.BorderSizePixel=0  
		Instance.new("UICorner",pill).CornerRadius=UDim.new(1,0)  
		local dot = Instance.new("Frame",pill)  
		dot.Size=UDim2.new(0,14,0,14); dot.Position=UDim2.new(0,3,0.5,-7)  
		dot.BackgroundColor3=Color3.fromRGB(160,160,180); dot.BorderSizePixel=0  
		Instance.new("UICorner",dot).CornerRadius=UDim.new(1,0)  
		local isOn = false  
		local function setV(on)  
			isOn=on  
			TweenService:Create(pill,TweenInfo.new(0.18),{BackgroundColor3=on and PINK or Color3.fromRGB(38,10,26)}):Play()  
			TweenService:Create(dot,TweenInfo.new(0.18,Enum.EasingStyle.Back),{  
				Position=on and UDim2.new(1,-17,0.5,-7) or UDim2.new(0,3,0.5,-7),  
				BackgroundColor3=on and WHITE or Color3.fromRGB(160,160,180)  
			}):Play()  
		end  
		local clk = Instance.new("TextButton",row)  
		clk.Size=UDim2.new(1,0,1,0); clk.BackgroundTransparency=1; clk.Text=""; clk.ZIndex=5  
		clk.MouseButton1Click:Connect(function()  
			isOn=not isOn; setV(isOn); onToggle(isOn); saveConfig()  
		end)  
		return setV  
	end  
  
	makeSecHdr(perfPage, "Performance", 0)  
	local perfSetAntiLag    = makePerfToggle("Anti Lag",           1, function(on) if on then enableAntiLag() else disableAntiLag() end end)  
	local perfSetRemoveAccs = makePerfToggle("Remove Accessories", 2, function(on) if on then enableRemoveAccessories() else disableRemoveAccessories() end end)  
	local perfSetStretchRez = makePerfToggle("Stretch Rez",        3, function(on) if on then enableStretchRez() else disableStretchRez() end end)  
	makeSecHdr(perfPage, "Blossomfy", 4)  
	local perfSetBlossomfy  = makePerfToggle("Blossomfy",          5, function(on) applyNightMode(on) end)  
  
	-- expose to loadConfig  
	miscSetVisuals["__antiLag"]    = perfSetAntiLag  
	miscSetVisuals["__removeAccs"] = perfSetRemoveAccs  
	miscSetVisuals["__stretchRez"] = perfSetStretchRez  
	miscSetVisuals["__blossomfy"]  = perfSetBlossomfy  
end  
  
-- ═══ LOAD CONFIG ═══════════════════════════════════════════════════════════  
  
  

do -- mobile panel  
	local gui = sg  -- alias for compatibility  
	local CARD_BG = Color3.fromRGB(34,  10, 24)   -- #F897AF theme card  
	local CARD_HOV= Color3.fromRGB(50,  14, 34)  
	local BORDER  = Color3.fromRGB(248, 151, 175)  -- outline pink  
	local DARK_ACC= Color3.fromRGB(200, 100, 140)  
	local WHITE   = Color3.fromRGB(255, 235, 242)  
	local DIM     = Color3.fromRGB(200, 140, 165)  
	local ACCENT  = Color3.fromRGB(248, 151, 175)  -- #F897AF  
	local OFF_BG  = Color3.fromRGB(34,  10, 24)  
	local BG      = Color3.fromRGB(18,   5, 12)  
	local cfp     = settingsPage2  
	local pageLOs = {}  
	for _,t in ipairs({"Speed","Combat","Misc","Move","Performance","Settings"}) do pageLOs[t]=200 end  
	local function lo(t) pageLOs[t]=pageLOs[t]+1; return pageLOs[t] end  
	-- use top-level _anyKeyListening (not a new local)  

-- ===== MOBILE BUTTONS =====================================================  
local MBTN_SIZE  = 54  
local MBTN_GAP   = 6  
local mbVisible  = true  
local mbLocked   = false  
local mb2Locked  = false   -- Panel 2 drag lock (set by Settings pill)  
mb2Buttons = {}  
  
-- helper: make a standalone draggable button (used by Panel 2)  
local function makeFreeDragBtn(label, defX, defY, onTap, isToggle)  
	local fr = Instance.new("Frame", gui)  
	fr.Size = UDim2.new(0, MBTN_SIZE, 0, MBTN_SIZE)  
	fr.Position = UDim2.new(0, defX, 0, defY)  
	fr.BackgroundColor3 = Color3.fromRGB(24, 24, 31)  
	fr.BorderSizePixel = 0  
	fr.Active = true  
	fr.Visible = false  
	fr.ZIndex = 20  
	Instance.new("UICorner", fr).CornerRadius = UDim.new(0, 12)  
	local stroke = Instance.new("UIStroke", fr)  
	stroke.Color = BORDER; stroke.Thickness = 1.5  
  
	local lbl2 = Instance.new("TextLabel", fr)  
	lbl2.Size = UDim2.new(1, 0, 1, 0)  
	lbl2.BackgroundTransparency = 1  
	lbl2.Text = label  
	lbl2.TextColor3 = WHITE  
	lbl2.Font = Enum.Font.GothamBlack  
	lbl2.TextSize = 10  
	lbl2.TextWrapped = true  
	lbl2.ZIndex = 21  
  
	local isOn = false  
	local function setOn(v)  
		isOn = v  
		fr.BackgroundColor3 = v and Color3.fromRGB(248, 151, 175) or Color3.fromRGB(24, 24, 31)  
		stroke.Color = v and Color3.fromRGB(248, 151, 175) or BORDER  
	end  
  
	local _d, _di, _ds, _sp = false, nil, nil, nil  
	fr.InputBegan:Connect(function(inp)  
		if inp.UserInputType == Enum.UserInputType.MouseButton1 or inp.UserInputType == Enum.UserInputType.Touch then  
			if not mb2Locked and not uiLocked then  
				_d=true; _ds=inp.Position; _sp=fr.Position  
				inp.Changed:Connect(function()
					if inp.UserInputState==Enum.UserInputState.End then
						_d=false
						task.defer(function() pcall(saveConfig) end)
					end
				end)
			end  
		end  
	end)  
	fr.InputChanged:Connect(function(inp)  
		if inp.UserInputType==Enum.UserInputType.MouseMovement or inp.UserInputType==Enum.UserInputType.Touch then _di=inp end  
	end)  
	UIS.InputChanged:Connect(function(inp)  
		if inp==_di and _d and not mb2Locked and not uiLocked then  
			local d=inp.Position-_ds  
			fr.Position=UDim2.new(_sp.X.Scale,_sp.X.Offset+d.X,_sp.Y.Scale,_sp.Y.Offset+d.Y)  
		end  
	end)  
  
	local btn2 = Instance.new("TextButton", fr)  
	btn2.Size = UDim2.new(1,0,1,0)  
	btn2.BackgroundTransparency = 1  
	btn2.Text = ""; btn2.ZIndex = 22  
	btn2.MouseButton1Click:Connect(function()  
		if isToggle then setOn(not isOn) end  
		if onTap then onTap(isOn) end  
	end)  
  
	table.insert(mb2Buttons, {fr=fr, setOn=setOn})  
	return fr, setOn  
end  
  
-- ── PANEL 1: single draggable container ──────────────────────────────────  
local BTN_SIZE = 58  
local BTN_GAP  = 14  
local PADDING  = 6  
local COLS = 2  
local ROWS = 4  
local CONT_W = PADDING*2 + COLS*BTN_SIZE + (COLS-1)*BTN_GAP  
local CONT_H = PADDING*2 + ROWS*BTN_SIZE + (ROWS-1)*BTN_GAP  
mbContainer = Instance.new("Frame", gui)  
mbContainer.Name = "MobileButtonsPanel"  
mbContainer.Size = UDim2.new(0, CONT_W, 0, CONT_H)  
mbContainer.Position = UDim2.new(1, -(CONT_W+10), 1, -(CONT_H+10))  
mbContainer.BackgroundColor3 = Color3.fromRGB(0, 0, 0)  
mbContainer.BackgroundTransparency = 1  
mbContainer.BorderSizePixel = 0  
mbContainer.Active = true  
mbContainer.Visible = true  
mbContainer.ZIndex = 95  
  
-- Drag via mbContainer with a move-threshold so quick taps reach buttons
-- but sustained drags still reposition the panel.
local DRAG_THRESHOLD = 6  -- pixels of movement before drag activates
local _mbDragging, _mbDragInput, _mbDragStart, _mbStartPos = false, nil, nil, nil
mbContainer.InputBegan:Connect(function(inp)
	if inp.UserInputType==Enum.UserInputType.MouseButton1 or inp.UserInputType==Enum.UserInputType.Touch then
		if not mbLocked and not uiLocked then
			_mbDragStart=inp.Position; _mbStartPos=mbContainer.Position; _mbDragging=false
		end
	end
end)
mbContainer.InputChanged:Connect(function(inp)
	if inp.UserInputType==Enum.UserInputType.MouseMovement or inp.UserInputType==Enum.UserInputType.Touch then _mbDragInput=inp end
end)
mbContainer.InputEnded:Connect(function(inp)
	if inp.UserInputType==Enum.UserInputType.MouseButton1 or inp.UserInputType==Enum.UserInputType.Touch then
		if _mbDragging then
			task.defer(function() pcall(saveConfig) end)
		end
		_mbDragging=false; _mbDragStart=nil
	end
end)
UIS.InputChanged:Connect(function(inp)
	if inp==_mbDragInput and _mbDragStart and not mbLocked and not uiLocked then
		local d=inp.Position-_mbDragStart
		if not _mbDragging then
			if math.abs(d.X)>DRAG_THRESHOLD or math.abs(d.Y)>DRAG_THRESHOLD then
				_mbDragging=true
			end
		end
		if _mbDragging then
			mbContainer.Position=UDim2.new(_mbStartPos.X.Scale,_mbStartPos.X.Offset+d.X,_mbStartPos.Y.Scale,_mbStartPos.Y.Offset+d.Y)
		end
	end
end)

  
local Q_OFF      = Color3.fromRGB(34,  10, 24)  
local Q_ON       = Color3.fromRGB(248, 151, 175)  -- #F897AF  
local Q_TEXT_OFF = Color3.fromRGB(200, 140, 165)  
local Q_TEXT_ON  = Color3.fromRGB(255, 235, 242)  
  
local function makeMobileBtn(label, col, row, onTap, isToggle)  
	local xPos = PADDING + col*(BTN_SIZE+BTN_GAP)  
	local yPos = PADDING + row*(BTN_SIZE+BTN_GAP)  
	local btn = Instance.new("TextButton", mbContainer)  
	btn.Name = "Btn_"..label  
	btn.Size = UDim2.new(0, BTN_SIZE, 0, BTN_SIZE)  
	btn.Position = UDim2.new(0, xPos, 0, yPos)  
	btn.BackgroundColor3 = Q_OFF  
	btn.Text = label; btn.TextColor3 = Q_TEXT_OFF  
	btn.TextScaled = false; btn.TextSize = 11  
	btn.Font = Enum.Font.GothamBold  
	btn.TextWrapped = true; btn.LineHeight = 1.2  
	btn.BorderSizePixel = 0; btn.AutoButtonColor = false  
	btn.ZIndex = 99  
	Instance.new("UICorner", btn).CornerRadius = UDim.new(0, 12)  
	local stroke = Instance.new("UIStroke", btn)  
	stroke.Color = Color3.fromRGB(30, 30, 42); stroke.Thickness = 1  
	local isOn = false  
	local function setOn(v)  
		isOn = v  
		TS:Create(btn, TweenInfo.new(0.15), {  
			BackgroundColor3 = v and Q_ON or Q_OFF,  
			TextColor3       = v and Q_TEXT_ON or Q_TEXT_OFF,  
		}):Play()  
		TS:Create(stroke, TweenInfo.new(0.15), {  
			Color = v and Color3.fromRGB(248,151,175) or Color3.fromRGB(30,30,42),  
		}):Play()  
	end  
	local function flash()  
		TS:Create(btn, TweenInfo.new(0.08), {BackgroundColor3=Color3.fromRGB(248,151,175), TextColor3=Color3.fromRGB(255,255,255)}):Play()  
		task.delay(0.22, function()  
			TS:Create(btn, TweenInfo.new(0.15), {BackgroundColor3=Q_OFF, TextColor3=Q_TEXT_OFF}):Play()  
		end)  
	end  
	btn.MouseButton1Click:Connect(function()  
		if isToggle then isOn=not isOn; setOn(isOn); if onTap then onTap(isOn) end  
		else flash(); if onTap then onTap() end end  
	end)  
	return btn, setOn  
end  
  
local function flashBtn(fr, _)  
	TS:Create(fr, TweenInfo.new(0.08), {BackgroundColor3=Color3.fromRGB(248,151,175), TextColor3=Color3.fromRGB(255,255,255)}):Play()  
	task.delay(0.22, function()  
		TS:Create(fr, TweenInfo.new(0.15), {BackgroundColor3=Q_OFF, TextColor3=Q_TEXT_OFF}):Play()  
	end)  
end  
  
do local btn,_=makeMobileBtn("DROP\nBR",0,0,function() runDrop() end,false)
end  
local _,setAutoLMb=makeMobileBtn("AUTO\nLEFT",1,0,function(on)  
	autoLeftEnabled=on  
	if on then  
		if autoRightEnabled then autoRightEnabled=false;if autoRightSetVisual then autoRightSetVisual(false) end;stopAutoRight() end  
		if autoBatEnabled then autoBatEnabled=false;if autoBatSetVisual then autoBatSetVisual(false) end end  
		startAutoLeft()  
	else stopAutoLeft() end  
	if autoLeftSetVisual then autoLeftSetVisual(on) end  
end,true)  
local _,setAutoBatMb=makeMobileBtn("BAT\nAIMBOT",0,1,function(on)  
	autoBatEnabled=on  
	if on then  
		if autoLeftEnabled then autoLeftEnabled=false;if autoLeftSetVisual then autoLeftSetVisual(false) end;stopAutoLeft() end  
		if autoRightEnabled then autoRightEnabled=false;if autoRightSetVisual then autoRightSetVisual(false) end;stopAutoRight() end  
		startBatAimbot()  
	else stopBatAimbot() end  
	if autoBatSetVisual then autoBatSetVisual(on) end  
end,true)  
local _,setAutoRMb=makeMobileBtn("AUTO\nRIGHT",1,1,function(on)  
	autoRightEnabled=on  
	if on then  
		if autoLeftEnabled then autoLeftEnabled=false;if autoLeftSetVisual then autoLeftSetVisual(false) end;stopAutoLeft() end  
		if autoBatEnabled then autoBatEnabled=false;if autoBatSetVisual then autoBatSetVisual(false) end end  
		startAutoRight()  
	else stopAutoRight() end  
	if autoRightSetVisual then autoRightSetVisual(on) end  
end,true)  
do local btn,_=makeMobileBtn("TP\nDOWN",0,2,function() runTPDown() end,false)
end
-- Carry toggle button: toggles carry ON/OFF for whichever mode
-- (Normal or Lagger) is currently active. Does NOT change the mode.
local _,setSpeedMb=makeMobileBtn("CARRY\nSPD",1,2,function(on)
	toggleCarry()
end,true)

-- Lagger Mode toggle: switches activeSpeedMode between "normal" and
-- "lagger". Carry on/off state is preserved across the switch, so if
-- carry was on in Normal mode it stays on (now using Lagger Carry speed),
-- and if carry was off it stays off (now using Lagger Normal speed).
do local btn,_=makeMobileBtn("INSTA\nRESET",0,3,function(on)
	instaReset()
	task.delay(0.1, function() if btn then btn.BackgroundColor3 = Color3.fromRGB(34,10,24) end end)
end, false)
end

local _,setLaggerMb=makeMobileBtn("LAGGER\nMODE",1,3,function(on)
	if on then switchMode("lagger") else switchMode("normal") end
	if setLaggerModeVisual then setLaggerModeVisual(activeSpeedMode=="lagger") end
end,true)
  
-- Panel 1 visibility helper  
local _p1SetVis  
_p1SetVis = function(v)  
	mbVisible=v; mbContainer.Visible=v  
end  
  
-- Chain mobile button visuals into the global setVisual functions so  
-- when a feature auto-completes it resets BOTH the pill AND the mobile button.  
do  
	local _origAL=autoLeftSetVisual  
	autoLeftSetVisual=function(v) if _origAL then _origAL(v) end; setAutoLMb(v) end  
	local _origAR=autoRightSetVisual  
	autoRightSetVisual=function(v) if _origAR then _origAR(v) end; setAutoRMb(v) end  
	local _origAB=autoBatSetVisual  
	autoBatSetVisual=function(v) if _origAB then _origAB(v) end; setAutoBatMb(v) end  
	-- source: chain visual setters only (updateModeButtons already set by speed page)
	local _origUMB2 = updateModeButtons
	updateModeButtons = function()
		if _origUMB2 then _origUMB2() end
		-- CARRY SPD button reflects whether carry is currently ON (in either mode)
		setSpeedMb(carryActive)
		-- LAGGER MODE button reflects whether Lagger mode is currently active
		setLaggerMb(activeSpeedMode=="lagger")
	end
end  
  
-- ── SETTINGS: panels section (at the TOP of Settings) ────────────────────  
	local function makeSecHdr(page,_,txt)  
		local f=Instance.new("Frame",page);f.Size=UDim2.new(1,0,0,18);f.BackgroundTransparency=1;f.BorderSizePixel=0  
		f.LayoutOrder=lo("Settings");f.ZIndex=4  
		local t=Instance.new("TextLabel",f);t.Size=UDim2.new(1,0,1,0);t.BackgroundTransparency=1  
		t.Text=txt:upper();t.TextColor3=WHITE;t.Font=Enum.Font.GothamBold;t.TextSize=8  
		t.TextXAlignment=Enum.TextXAlignment.Left;t.ZIndex=5  
		local line=Instance.new("Frame",f);line.Size=UDim2.new(1,0,0,1);line.Position=UDim2.new(0,0,1,-1)  
		line.BackgroundColor3=BORDER;line.BorderSizePixel=0;line.ZIndex=4  
	end  
	local function makePill(page,_,labelTxt,defOn,onToggle)  
		local c=Instance.new("Frame",page);c.Size=UDim2.new(1,0,0,38);c.BackgroundColor3=CARD_BG;c.BorderSizePixel=0  
		c.LayoutOrder=lo("Settings");c.ZIndex=4  
		Instance.new("UICorner",c).CornerRadius=UDim.new(0,7);Instance.new("UIStroke",c).Color=BORDER  
		local lbl=Instance.new("TextLabel",c);lbl.Size=UDim2.new(0,160,1,0);lbl.Position=UDim2.new(0,10,0,0)  
		lbl.BackgroundTransparency=1;lbl.Text=labelTxt;lbl.TextColor3=WHITE  
		lbl.Font=Enum.Font.GothamBold;lbl.TextSize=11;lbl.TextXAlignment=Enum.TextXAlignment.Left;lbl.ZIndex=10  
		local pbg=Instance.new("Frame",c);pbg.Size=UDim2.new(0,36,0,19);pbg.Position=UDim2.new(1,-46,0.5,-9)  
		pbg.BackgroundColor3=defOn and ACCENT or Color3.fromRGB(30,30,42);pbg.BorderSizePixel=0;pbg.ZIndex=8  
		Instance.new("UICorner",pbg).CornerRadius=UDim.new(0,10)  
		local ps=Instance.new("UIStroke",pbg);ps.Color=defOn and ACCENT or BORDER  
		local dot=Instance.new("Frame",pbg);dot.Size=UDim2.new(0,13,0,13)  
		dot.Position=defOn and UDim2.new(1,-15,0.5,-6) or UDim2.new(0,2,0.5,-6)  
		dot.BackgroundColor3=defOn and WHITE or DIM;dot.BorderSizePixel=0;dot.ZIndex=9  
		Instance.new("UICorner",dot).CornerRadius=UDim.new(0,4)  
		local isOn=defOn or false  
		local function setV(on)  
			isOn=on  
			TS:Create(pbg,TweenInfo.new(0.18),{BackgroundColor3=on and ACCENT or Color3.fromRGB(30,30,42)}):Play()  
			TS:Create(ps,TweenInfo.new(0.18),{Color=on and ACCENT or BORDER}):Play()  
			TS:Create(dot,TweenInfo.new(0.18,Enum.EasingStyle.Back),{Position=on and UDim2.new(1,-15,0.5,-6) or UDim2.new(0,2,0.5,-6),BackgroundColor3=on and WHITE or DIM}):Play()  
		end  
		local clk=Instance.new("TextButton",c);clk.Size=UDim2.new(1,0,1,0);clk.BackgroundTransparency=1;clk.Text="";clk.ZIndex=6  
		clk.MouseButton1Click:Connect(function()  
			isOn=not isOn;setV(isOn);if onToggle then onToggle(isOn) end;saveConfig()  
		end)  
		return setV  
	end  
	-- use top-level _anyKeyListening  
	local function makeKbRow(page,_,labelTxt,kbEntry)  
		local c=Instance.new("Frame",page);c.Size=UDim2.new(1,0,0,38);c.BackgroundColor3=CARD_BG;c.BorderSizePixel=0  
		c.LayoutOrder=lo("Settings");c.ZIndex=4  
		Instance.new("UICorner",c).CornerRadius=UDim.new(0,7);Instance.new("UIStroke",c).Color=BORDER  
		local lbl=Instance.new("TextLabel",c);lbl.Size=UDim2.new(0,160,1,0);lbl.Position=UDim2.new(0,10,0,0)  
		lbl.BackgroundTransparency=1;lbl.Text=labelTxt;lbl.TextColor3=WHITE  
		lbl.Font=Enum.Font.GothamBold;lbl.TextSize=11;lbl.TextXAlignment=Enum.TextXAlignment.Left;lbl.ZIndex=10  
		local kb=Instance.new("TextButton",c);kb.Size=UDim2.new(0,44,0,20);kb.Position=UDim2.new(1,-54,0.5,-10)  
		kb.BackgroundColor3=Color3.fromRGB(10,10,18);kb.BorderSizePixel=0  
		kb.Text=(kbEntry.kb or kbEntry.gp or Enum.KeyCode.Unknown).Name  
		kb.TextColor3=WHITE;kb.Font=Enum.Font.GothamBold;kb.TextSize=8;kb.ZIndex=11  
		Instance.new("UICorner",kb).CornerRadius=UDim.new(0,5);Instance.new("UIStroke",kb).Color=BORDER  
		local li=false;local lc2;local pv=kb.Text  
		kb.MouseButton1Click:Connect(function()  
			if li then li=false;_anyKeyListening=false;if lc2 then lc2:Disconnect();lc2=nil end;kb.Text=pv;return end  
			pv=kb.Text;li=true;_anyKeyListening=true;kb.Text="..."  
			lc2=UIS.InputBegan:Connect(function(inp)  
				if not li then return end  
				local t2=inp.UserInputType  
				if t2==Enum.UserInputType.Keyboard or t2==Enum.UserInputType.Gamepad1 then  
					if inp.KeyCode==Enum.KeyCode.Escape then li=false;_anyKeyListening=false;if lc2 then lc2:Disconnect();lc2=nil end;kb.Text=pv;return end  
					if tostring(inp.KeyCode):find("Button") then kbEntry.gp=inp.KeyCode;kbEntry.kb=nil else kbEntry.kb=inp.KeyCode;kbEntry.gp=nil end  
					kb.Text=inp.KeyCode.Name;pv=kb.Text;li=false;_anyKeyListening=false;if lc2 then lc2:Disconnect();lc2=nil end;saveConfig()  
				end  
			end)  
		end)  
	end  
  
makeSecHdr(cfp,"Settings","Panels")  
  
-- Panel 1  
makeSecHdr(cfp,"Settings","Panel 1")  
local _p1ShowPill; _p1ShowPill = makePill(cfp,"Settings","Show Panel 1",true,function(on)  
	_p1SetVis(on)  
end)  
makePill(cfp,"Settings","Lock Panel 1 Drag",false,function(on) mbLocked=on end)  
  
-- Panel 2  
makeSecHdr(cfp,"Settings","Panel 2")  
makePill(cfp,"Settings","Show Panel 2",false,function(on)  
	for _,entry in ipairs(mb2Buttons) do  
		entry.fr.Visible = on  
	end  
end)  
makePill(cfp,"Settings","Lock Panel 2 Drag",false,function(on) mb2Locked=on end)  
  
-- Per-button position controls for Panel 2  
-- Each button gets its own row with X/Y offset inputs  
do  
	local BTN_DEFS = {  
		{label="DROP\nBR",     key="DropBR"},  
		{label="AUTO\nLEFT",   key="AutoLeft"},  
		{label="BAT\nAIMBOT",  key="AutoBat"},  
		{label="AUTO\nRIGHT",  key="AutoRight"},  
		{label="TP\nDOWN",     key="TPDown"},  
		{label="CARRY\nSPD",   key="CarrySpd"},
	}  
  
	for i, def in ipairs(BTN_DEFS) do  
		local entry = mb2Buttons[i]  
		if not entry then break end  
		local fr = entry.fr  
  
		-- Row container  
		local row = Instance.new("Frame", cfp)  
		row.Size = UDim2.new(1, 0, 0, 50)  
		row.BackgroundColor3 = CARD_BG  
		row.BorderSizePixel = 0  
		row.LayoutOrder = lo("Settings")  
		row.ZIndex = 4  
		Instance.new("UICorner", row).CornerRadius = UDim.new(0, 7)  
		Instance.new("UIStroke", row).Color = BORDER  
  
		-- Button name label  
		local lbl = Instance.new("TextLabel", row)  
		lbl.Size = UDim2.new(0, 90, 0, 16)  
		lbl.Position = UDim2.new(0, 10, 0, 4)  
		lbl.BackgroundTransparency = 1  
		lbl.Text = def.label:gsub("\n"," ")  
		lbl.TextColor3 = WHITE  
		lbl.Font = Enum.Font.GothamBold  
		lbl.TextSize = 10  
		lbl.TextXAlignment = Enum.TextXAlignment.Left  
		lbl.ZIndex = 10  
  
		-- X label  
		local xLbl = Instance.new("TextLabel", row)  
		xLbl.Size = UDim2.new(0, 14, 0, 20)  
		xLbl.Position = UDim2.new(0, 10, 0, 24)  
		xLbl.BackgroundTransparency = 1  
		xLbl.Text = "X"  
		xLbl.TextColor3 = ACCENT  
		xLbl.Font = Enum.Font.GothamBold  
		xLbl.TextSize = 9  
		xLbl.ZIndex = 10  
  
		-- X input  
		local xBox = Instance.new("TextBox", row)  
		xBox.Size = UDim2.new(0, 52, 0, 20)  
		xBox.Position = UDim2.new(0, 26, 0, 24)  
		xBox.BackgroundColor3 = Color3.fromRGB(10, 10, 18)  
		xBox.BorderSizePixel = 0  
		xBox.Text = tostring(math.floor(fr.Position.X.Offset))  
		xBox.TextColor3 = WHITE  
		xBox.Font = Enum.Font.GothamBold  
		xBox.TextSize = 9  
		xBox.ClearTextOnFocus = false  
		xBox.ZIndex = 11  
		Instance.new("UICorner", xBox).CornerRadius = UDim.new(0, 4)  
		Instance.new("UIStroke", xBox).Color = BORDER  
  
		-- Y label  
		local yLbl = Instance.new("TextLabel", row)  
		yLbl.Size = UDim2.new(0, 14, 0, 20)  
		yLbl.Position = UDim2.new(0, 86, 0, 24)  
		yLbl.BackgroundTransparency = 1  
		yLbl.Text = "Y"  
		yLbl.TextColor3 = ACCENT  
		yLbl.Font = Enum.Font.GothamBold  
		yLbl.TextSize = 9  
		yLbl.ZIndex = 10  
  
		-- Y input  
		local yBox = Instance.new("TextBox", row)  
		yBox.Size = UDim2.new(0, 52, 0, 20)  
		yBox.Position = UDim2.new(0, 100, 0, 24)  
		yBox.BackgroundColor3 = Color3.fromRGB(10, 10, 18)  
		yBox.BorderSizePixel = 0  
		yBox.Text = tostring(math.floor(fr.Position.Y.Offset))  
		yBox.TextColor3 = WHITE  
		yBox.Font = Enum.Font.GothamBold  
		yBox.TextSize = 9  
		yBox.ClearTextOnFocus = false  
		yBox.ZIndex = 11  
		Instance.new("UICorner", yBox).CornerRadius = UDim.new(0, 4)  
		Instance.new("UIStroke", yBox).Color = BORDER  
  
		-- SET button  
		local setBtn = Instance.new("TextButton", row)  
		setBtn.Size = UDim2.new(0, 42, 0, 20)  
		setBtn.Position = UDim2.new(1, -52, 0.5, -2)  
		setBtn.BackgroundColor3 = ACCENT  
		setBtn.BorderSizePixel = 0  
		setBtn.Text = "SET"  
		setBtn.TextColor3 = Color3.fromRGB(20, 5, 15)  
		setBtn.Font = Enum.Font.GothamBlack  
		setBtn.TextSize = 9  
		setBtn.ZIndex = 11  
		Instance.new("UICorner", setBtn).CornerRadius = UDim.new(0, 4)  
  
		-- also update X/Y boxes when the button is dragged  
		local _posConn  
		_posConn = fr:GetPropertyChangedSignal("Position"):Connect(function()  
			xBox.Text = tostring(math.floor(fr.Position.X.Offset))  
			yBox.Text = tostring(math.floor(fr.Position.Y.Offset))  
		end)  
  
		setBtn.MouseButton1Click:Connect(function()  
			local x = tonumber(xBox.Text)  
			local y = tonumber(yBox.Text)  
			if x and y then  
				fr.Position = UDim2.new(0, x, 0, y)  
				TS:Create(setBtn, TweenInfo.new(0.1), {BackgroundColor3=Color3.fromRGB(100,220,120)}):Play()  
				task.delay(0.4, function()  
					TS:Create(setBtn, TweenInfo.new(0.2), {BackgroundColor3=ACCENT}):Play()  
				end)  
			end  
		end)  
	end  
end  
  
-- GUI Size  
makeSecHdr(cfp,"Settings","Interface")  
do  
	local c=Instance.new("Frame",cfp)  
	c.Size=UDim2.new(1,0,0,38);c.BackgroundColor3=CARD_BG;c.BorderSizePixel=0  
	c.LayoutOrder=lo("Settings");c.ZIndex=4  
	Instance.new("UICorner",c).CornerRadius=UDim.new(0,7)  
	Instance.new("UIStroke",c).Color=BORDER  
	local lbl=Instance.new("TextLabel",c)  
	lbl.Size=UDim2.new(0,130,1,0);lbl.Position=UDim2.new(0,10,0,0)  
	lbl.BackgroundTransparency=1;lbl.Text="Button Size";lbl.TextColor3=WHITE  
	lbl.Font=Enum.Font.GothamBold;lbl.TextSize=11;lbl.TextXAlignment=Enum.TextXAlignment.Left;lbl.ZIndex=10  
	local box=Instance.new("TextBox",c)  
	box.Size=UDim2.new(0,64,0,24);box.Position=UDim2.new(1,-74,0.5,-12)  
	box.BackgroundColor3=Color3.fromRGB(10,10,10);box.BorderSizePixel=0  
	box.Text=tostring(MBTN_SIZE);box.TextColor3=WHITE  
	box.Font=Enum.Font.GothamBold;box.TextSize=11;box.ClearTextOnFocus=false;box.ZIndex=11  
	Instance.new("UICorner",box).CornerRadius=UDim.new(0,5)  
	Instance.new("UIStroke",box).Color=BORDER  
	box.FocusLost:Connect(function()  
		local v=tonumber(box.Text)  
		if v and v>0 then  
			-- resize buttons in panel 1  
			if mbContainer then  
				local idx=0  
				for _,ch in ipairs(mbContainer:GetChildren()) do  
					if ch:IsA("Frame") then  
						ch.Size=UDim2.new(0,v,0,v)  
						local col2=idx%2;local row2=math.floor(idx/2)  
						ch.Position=UDim2.new(0,PADDING+col2*(v+MBTN_GAP),0,PADDING+row2*(v+MBTN_GAP))  
						idx=idx+1  
					end  
				end  
				local nrows=math.ceil(idx/2)  
				mbContainer.Size=UDim2.new(0,2*v+MBTN_GAP+PADDING*2,0,nrows*v+(nrows-1)*MBTN_GAP+PADDING*2)  
			end  
			MBTN_SIZE=v  
			box.Text=tostring(v)  
		else box.Text=tostring(MBTN_SIZE) end  
	end)  
end  
-- GUI Size (main menu — single scale value, scales W & H proportionally)  
do  
	local baseW,baseH=W,H  -- capture original dimensions as the 100% baseline  
	local c=Instance.new("Frame",cfp)  
	c.Size=UDim2.new(1,0,0,38);c.BackgroundColor3=CARD_BG;c.BorderSizePixel=0  
	c.LayoutOrder=lo("Settings");c.ZIndex=4  
	Instance.new("UICorner",c).CornerRadius=UDim.new(0,7)  
	Instance.new("UIStroke",c).Color=BORDER  
	local lbl=Instance.new("TextLabel",c)  
	lbl.Size=UDim2.new(0,130,1,0);lbl.Position=UDim2.new(0,10,0,0)  
	lbl.BackgroundTransparency=1;lbl.Text="GUI Size";lbl.TextColor3=WHITE  
	lbl.Font=Enum.Font.GothamBold;lbl.TextSize=11;lbl.TextXAlignment=Enum.TextXAlignment.Left;lbl.ZIndex=10  
	local box=Instance.new("TextBox",c)  
	box.Size=UDim2.new(0,64,0,24);box.Position=UDim2.new(1,-74,0.5,-12)  
	box.BackgroundColor3=Color3.fromRGB(10,10,10);box.BorderSizePixel=0  
	box.Text=tostring(baseW);box.TextColor3=WHITE  
	box.Font=Enum.Font.GothamBold;box.TextSize=11;box.ClearTextOnFocus=false;box.ZIndex=11  
	Instance.new("UICorner",box).CornerRadius=UDim.new(0,5)  
	Instance.new("UIStroke",box).Color=BORDER  
	box.FocusLost:Connect(function()  
		local v=tonumber(box.Text)  
		if v and v>0 then  
			local scale=v/baseW  
			main.Size=UDim2.new(0,v,0,math.floor(baseH*scale))  
			box.Text=tostring(v)  
		else box.Text=tostring(math.floor(main.Size.X.Offset)) end  
	end)  
	-- Reset GUI Size button  
	do  
		local rc=Instance.new("Frame",cfp)  
		rc.Size=UDim2.new(1,0,0,38);rc.BackgroundColor3=CARD_BG;rc.BorderSizePixel=0  
		rc.LayoutOrder=lo("Settings");rc.ZIndex=4  
		Instance.new("UICorner",rc).CornerRadius=UDim.new(0,7)  
		Instance.new("UIStroke",rc).Color=BORDER  
		local rlbl=Instance.new("TextLabel",rc)  
		rlbl.Size=UDim2.new(0,160,1,0);rlbl.Position=UDim2.new(0,10,0,0)  
		rlbl.BackgroundTransparency=1;rlbl.Text="Reset GUI Size";rlbl.TextColor3=WHITE  
		rlbl.Font=Enum.Font.GothamBold;rlbl.TextSize=11;rlbl.TextXAlignment=Enum.TextXAlignment.Left;rlbl.ZIndex=10  
		local rbtn=Instance.new("TextButton",rc)  
		rbtn.Size=UDim2.new(0,56,0,22);rbtn.Position=UDim2.new(1,-66,0.5,-11)  
		rbtn.BackgroundColor3=Color3.fromRGB(38,10,28);rbtn.BorderSizePixel=0  
		rbtn.Text="RESET";rbtn.TextColor3=WHITE  
		rbtn.Font=Enum.Font.GothamBlack;rbtn.TextSize=9;rbtn.ZIndex=11  
		Instance.new("UICorner",rbtn).CornerRadius=UDim.new(0,5)  
		Instance.new("UIStroke",rbtn).Color=BORDER  
		rbtn.MouseButton1Click:Connect(function()  
			main.Size=UDim2.new(0,baseW,0,baseH)  
			box.Text=tostring(baseW)  
			TS:Create(rbtn,TweenInfo.new(0.1),{BackgroundColor3=ACCENT}):Play()  
			task.delay(0.4,function() TS:Create(rbtn,TweenInfo.new(0.2),{BackgroundColor3=Color3.fromRGB(38,10,28)}):Play() end)  
		end)  
	end  
end  
  
-- Speed Customizer sub-GUI  
makeSecHdr(cfp,"Settings","Speed Customizer")  
do  
	-- Build a small floating speed customizer GUI  
	local scGui=Instance.new("ScreenGui")  
	scGui.Name="BlossomSpeedCustomizer";scGui.ResetOnSpawn=false;scGui.DisplayOrder=15;scGui.IgnoreGuiInset=true  
	if not pcall(function() scGui.Parent=game:GetService("CoreGui") end) then scGui.Parent=LP:WaitForChild("PlayerGui") end  
  
	local SC_W,SC_H=200,276  -- expanded for 6 speed rows
	local scFrame=Instance.new("Frame",scGui)  
	scFrame.Name="SpeedCustomizerFrame"  
	scFrame.Size=UDim2.new(0,SC_W,0,SC_H)  
	scFrame.Position=UDim2.new(0.5,-SC_W/2,0.5,80)  
	scFrame.BackgroundColor3=Color3.fromRGB(20,5,15);scFrame.BorderSizePixel=0;scFrame.Active=true  
	scFrame.Visible=false  
	Instance.new("UICorner",scFrame).CornerRadius=UDim.new(0,12)  
	local scStroke=Instance.new("UIStroke",scFrame);scStroke.Color=BORDER;scStroke.Thickness=1.5  
  
	-- Title bar  
	local scTop=Instance.new("Frame",scFrame)  
	scTop.Size=UDim2.new(1,0,0,28);scTop.BackgroundColor3=Color3.fromRGB(28,8,22);scTop.BorderSizePixel=0  
	Instance.new("UICorner",scTop).CornerRadius=UDim.new(0,12)  
	local scTopPatch=Instance.new("Frame",scTop)  
	scTopPatch.Size=UDim2.new(1,0,0,12);scTopPatch.Position=UDim2.new(0,0,1,-12)  
	scTopPatch.BackgroundColor3=Color3.fromRGB(28,8,22);scTopPatch.BorderSizePixel=0  
	local scTitle=Instance.new("TextLabel",scTop)  
	scTitle.Size=UDim2.new(1,-28,1,0);scTitle.Position=UDim2.new(0,8,0,0)  
	scTitle.BackgroundTransparency=1;scTitle.Text="Speed Customizer"  
	scTitle.TextColor3=WHITE;scTitle.Font=Enum.Font.GothamBold;scTitle.TextSize=10  
	scTitle.TextXAlignment=Enum.TextXAlignment.Left  
  
	-- Close button  
	local scClose=Instance.new("TextButton",scTop)  
	scClose.Size=UDim2.new(0,18,0,18);scClose.Position=UDim2.new(1,-22,0.5,-9)  
	scClose.BackgroundColor3=Color3.fromRGB(38,10,28);scClose.BorderSizePixel=0  
	scClose.Text="×";scClose.TextColor3=WHITE;scClose.Font=Enum.Font.GothamBlack;scClose.TextSize=12  
	Instance.new("UICorner",scClose).CornerRadius=UDim.new(0,4)  
	scClose.MouseButton1Click:Connect(function() scFrame.Visible=false end)  
  
	-- Draggable  
	local _scd,_scdi,_scds,_scsp=false,nil,nil,nil  
	scFrame.InputBegan:Connect(function(inp)  
		if inp.UserInputType==Enum.UserInputType.MouseButton1 or inp.UserInputType==Enum.UserInputType.Touch then  
			_scd=true;_scds=inp.Position;_scsp=scFrame.Position  
			inp.Changed:Connect(function() if inp.UserInputState==Enum.UserInputState.End then _scd=false end end)  
		end  
	end)  
	scFrame.InputChanged:Connect(function(inp)  
		if inp.UserInputType==Enum.UserInputType.MouseMovement or inp.UserInputType==Enum.UserInputType.Touch then _scdi=inp end  
	end)  
	UIS.InputChanged:Connect(function(inp)  
		if inp==_scdi and _scd then  
			local d=inp.Position-_scds  
			scFrame.Position=UDim2.new(_scsp.X.Scale,_scsp.X.Offset+d.X,_scsp.Y.Scale,_scsp.Y.Offset+d.Y)  
		end  
	end)  
  
	-- Speed rows inside the small GUI  
	local function makeScRow(parent,yOff,labelTxt,getValue,setValue)  
		local row=Instance.new("Frame",parent)  
		row.Size=UDim2.new(1,-16,0,34);row.Position=UDim2.new(0,8,0,yOff)  
		row.BackgroundColor3=Color3.fromRGB(32,8,22);row.BorderSizePixel=0  
		Instance.new("UICorner",row).CornerRadius=UDim.new(0,7)  
		Instance.new("UIStroke",row).Color=BORDER  
		local lbl=Instance.new("TextLabel",row)  
		lbl.Size=UDim2.new(0.55,0,1,0);lbl.Position=UDim2.new(0,8,0,0)  
		lbl.BackgroundTransparency=1;lbl.Text=labelTxt;lbl.TextColor3=WHITE  
		lbl.Font=Enum.Font.GothamBold;lbl.TextSize=10;lbl.TextXAlignment=Enum.TextXAlignment.Left  
		local tb=Instance.new("TextBox",row)  
		tb.Size=UDim2.new(0,52,0,22);tb.Position=UDim2.new(1,-60,0.5,-11)  
		tb.BackgroundColor3=Color3.fromRGB(10,10,10);tb.BorderSizePixel=0  
		tb.Text=tostring(getValue());tb.TextColor3=WHITE  
		tb.Font=Enum.Font.GothamBold;tb.TextSize=10;tb.ClearTextOnFocus=false  
		Instance.new("UICorner",tb).CornerRadius=UDim.new(0,5)  
		Instance.new("UIStroke",tb).Color=BORDER  
		tb.FocusLost:Connect(function()  
			local v=tonumber(tb.Text)  
			if v and v>0 then  
				setValue(v);tb.Text=tostring(v)  
				-- sync with main speed boxes if they exist  
				if labelTxt=="Normal" and normalBox then normalBox.Text=tostring(v) end  
				if labelTxt=="Carry" and carryBox then carryBox.Text=tostring(v) end  
				if labelTxt=="Lagger" and laggerBox then laggerBox.Text=tostring(v) end  
				saveConfig()  
			else tb.Text=tostring(getValue()) end  
		end)  
		return tb  
	end  
  
	local scNormalTb  = makeScRow(scFrame, 32, "Normal (N)",     function() return NS  end, function(v) NS =v end)
	local scCarryTb   = makeScRow(scFrame, 72, "Normal Carry",   function() return CS  end, function(v) CS =v end)
	local scLaggerTb  = makeScRow(scFrame,112, "Lagger (L)",     function() return LS  end, function(v) LS =v end)
	local scLaggerC   = makeScRow(scFrame,152, "Lagger Carry",   function() return LS2 end, function(v) LS2=v end)
	local scDesyncTb  = makeScRow(scFrame,192, "Desync (D)",     function() return DS  end, function(v) DS =v end)
	local scDesyncC   = makeScRow(scFrame,232, "Desync Carry",   function() return DS2 end, function(v) DS2=v end)
  
	-- Button in Settings to open the Speed Customizer  
	do  
		local c=Instance.new("Frame",cfp)  
		c.Size=UDim2.new(1,0,0,38);c.BackgroundColor3=CARD_BG;c.BorderSizePixel=0  
		c.LayoutOrder=lo("Settings");c.ZIndex=4  
		Instance.new("UICorner",c).CornerRadius=UDim.new(0,7)  
		Instance.new("UIStroke",c).Color=BORDER  
		local lbl=Instance.new("TextLabel",c)  
		lbl.Size=UDim2.new(0,160,1,0);lbl.Position=UDim2.new(0,10,0,0)  
		lbl.BackgroundTransparency=1;lbl.Text="Speed Customizer";lbl.TextColor3=WHITE  
		lbl.Font=Enum.Font.GothamBold;lbl.TextSize=11;lbl.TextXAlignment=Enum.TextXAlignment.Left;lbl.ZIndex=10  
		local openBtn=Instance.new("TextButton",c)  
		openBtn.Size=UDim2.new(0,50,0,22);openBtn.Position=UDim2.new(1,-60,0.5,-11)  
		openBtn.BackgroundColor3=ACCENT;openBtn.BorderSizePixel=0  
		openBtn.Text="OPEN";openBtn.TextColor3=Color3.fromRGB(20,5,15)  
		openBtn.Font=Enum.Font.GothamBlack;openBtn.TextSize=9;openBtn.ZIndex=11  
		Instance.new("UICorner",openBtn).CornerRadius=UDim.new(0,5)  
		openBtn.MouseButton1Click:Connect(function()  
			scFrame.Visible=not scFrame.Visible
			-- refresh values when opening
			scNormalTb.Text=tostring(NS);  scCarryTb.Text=tostring(CS)
			scLaggerTb.Text=tostring(LS);  scLaggerC.Text=tostring(LS2)
			scDesyncTb.Text=tostring(DS);  scDesyncC.Text=tostring(DS2)
		end)  
	end  
end  
  
makeKbRow(cfp,"Settings","Hide / Show GUI",KB.GuiHide)  
makeKbRow(cfp,"Settings","Normal/Lagger Mode Key",KB.ModeToggle)
makeKbRow(cfp,"Settings","Speed Toggle Key",KB.SpeedToggle)
  
end -- end mobile panel  
end -- end buildGui
local function preloadKeybinds()  
	local hasFile = false; pcall(function() hasFile = _isfile(CONFIG_FILE) end)  
	if not hasFile then return end  
	local raw; pcall(function() raw = _readfile(CONFIG_FILE) end)  
	if not raw then return end  
	local ok,cfg=pcall(function() return HS:JSONDecode(raw) end)  
	if not ok or not cfg then return end  
	local function lk(entryName,data)  
		local entry=KB[entryName];if not data then return end  
		if type(data)=="table" then  
			if data.kb and Enum.KeyCode[data.kb] then entry.kb=Enum.KeyCode[data.kb] end  
			if data.gp and Enum.KeyCode[data.gp] then entry.gp=Enum.KeyCode[data.gp] end  
		end  
	end  
	lk("DropBrainrot",cfg.dropBrainrotKey);lk("TPFloor",cfg.tpFloorKey);lk("AutoLeft",cfg.autoLeftKey);lk("AutoRight",cfg.autoRightKey)  
	lk("SpeedToggle",cfg.speedToggleKey)
	if cfg.normalSpeed      then NS  = cfg.normalSpeed      end
	if cfg.carrySpeed       then CS  = cfg.carrySpeed       end
	if cfg.laggerSpeed      then LS  = cfg.laggerSpeed      end
	if cfg.laggerCarrySpeed then LS2 = cfg.laggerCarrySpeed end
end  
  
preloadKeybinds()  
buildGui()  
  
-- ===== MOBILE PANEL (re-injected) =====  
  
  
-- ─── Auto Steal Progress Bar (message 28 style) ─────────────────────────────
local function _buildPB()
	local _pbGui = Instance.new("ScreenGui")
	_pbGui.Name="BlossomProgressBar";_pbGui.ResetOnSpawn=false;_pbGui.DisplayOrder=20;_pbGui.IgnoreGuiInset=true
	if not pcall(function() _pbGui.Parent=game:GetService("CoreGui") end) then _pbGui.Parent=LP:WaitForChild("PlayerGui") end

	-- Outer container (draggable) — dark pill wrapping bar + radius label
	local pbFrame = Instance.new("Frame", _pbGui)
	_savedPbFrame = pbFrame
	pbFrame.Size = UDim2.new(0, 270, 0, 52)
	pbFrame.Position = UDim2.new(0.5, -135, 1, -72)
	pbFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
	pbFrame.BackgroundTransparency = 0.3
	pbFrame.BorderSizePixel = 0
	pbFrame.Active = true
	pbFrame.Name = "MainContainer"
	Instance.new("UICorner", pbFrame).CornerRadius = UDim.new(0, 10)
	local pbFrameStroke = Instance.new("UIStroke", pbFrame)
	pbFrameStroke.Color = Color3.fromRGB(248, 151, 175)
	pbFrameStroke.Thickness = 1

	-- Dragging — uses InputEnded for reliable save on mobile
	local _pbDragging,_pbDragInput,_pbDragStart,_pbStartPos=false,nil,nil,nil
	pbFrame.InputBegan:Connect(function(inp)
		if uiLocked then return end
		if inp.UserInputType==Enum.UserInputType.MouseButton1 or inp.UserInputType==Enum.UserInputType.Touch then
			_pbDragging=true;_pbDragStart=inp.Position;_pbStartPos=pbFrame.Position
		end
	end)
	pbFrame.InputChanged:Connect(function(inp)
		if uiLocked then _pbDragging=false; return end
		if inp.UserInputType==Enum.UserInputType.MouseMovement or inp.UserInputType==Enum.UserInputType.Touch then _pbDragInput=inp end
	end)
	pbFrame.InputEnded:Connect(function(inp)
		if inp.UserInputType==Enum.UserInputType.MouseButton1 or inp.UserInputType==Enum.UserInputType.Touch then
			if _pbDragging then
				_pbDragging=false
				task.defer(function() pcall(saveConfig) end)
			end
		end
	end)
	UIS.InputChanged:Connect(function(inp)
		if uiLocked then _pbDragging=false; return end
		if inp==_pbDragInput and _pbDragging then
			local d=inp.Position-_pbDragStart
			pbFrame.Position=UDim2.new(_pbStartPos.X.Scale,_pbStartPos.X.Offset+d.X,_pbStartPos.Y.Scale,_pbStartPos.Y.Offset+d.Y)
		end
	end)

	-- Progress bar background (inside the dark pill, with padding)
	local pbBg = Instance.new("Frame", pbFrame)
	pbBg.Size = UDim2.new(1, -16, 0, 20)
	pbBg.Position = UDim2.new(0, 8, 0, 8)
	pbBg.BackgroundColor3 = Color3.fromRGB(30, 10, 30)
	pbBg.BackgroundTransparency = 0.25
	pbBg.BorderSizePixel = 0
	Instance.new("UICorner", pbBg).CornerRadius = UDim.new(0, 10)
	local pbBgStroke = Instance.new("UIStroke", pbBg)
	pbBgStroke.Color = Color3.fromRGB(248, 151, 175)
	pbBgStroke.Thickness = 1.5

	-- Fill bar
	progressFill = Instance.new("Frame", pbBg)
	progressFill.Size = UDim2.new(0, 0, 1, 0)
	progressFill.BackgroundColor3 = Color3.fromRGB(248, 151, 175)
	progressFill.BorderSizePixel = 0
	Instance.new("UICorner", progressFill).CornerRadius = UDim.new(0, 10)

	-- Percent label (centered inside bar)
	progressPct = Instance.new("TextLabel", pbBg)
	progressPct.Size = UDim2.new(1, 0, 1, 0)
	progressPct.BackgroundTransparency = 1
	progressPct.Text = ""
	progressPct.TextColor3 = Color3.fromRGB(255, 235, 242)
	progressPct.Font = Enum.Font.GothamBold
	progressPct.TextSize = 12
	progressPct.ZIndex = 5

	-- Radius label inside the dark pill below the bar
	progressRadLbl = Instance.new("TextLabel", pbFrame)
	progressRadLbl.Size = UDim2.new(1, -16, 0, 14)
	progressRadLbl.Position = UDim2.new(0, 8, 0, 32)
	progressRadLbl.BackgroundTransparency = 1
	progressRadLbl.Text = "Radius: "..Steal.StealRadius
	progressRadLbl.TextColor3 = Color3.fromRGB(200, 140, 165)
	progressRadLbl.Font = Enum.Font.GothamSemibold
	progressRadLbl.TextSize = 10
	progressRadLbl.TextXAlignment = Enum.TextXAlignment.Right
	progressRadLbl.ZIndex = 5
end
_buildPB()
-- ─── End Auto Steal Progress Bar ─────────────────────────────────────────────  
loadConfig()  
task.spawn(function() while task.wait(10) do saveConfig() end end)  
  
print("italozin Loaded")
end)()